// piobox-customizer/data/catalog.js
var DEFAULT_CATALOG = [
  {
    "id": "mercht-001",
    "name": "Relaxed Tee",
    "styleNumber": "BLNKUP-01",
    "category": "Tees",
    "fit": "Relaxed",
    "fabricWeight": "185-200 GSM / 5.45oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-relaxed.webp",
      "frontMask": "high-quality-relaxed-black.webp",
      "frontBlack": "high-quality-relaxed-black.webp",
      "back": "relaxed-tee-back-white.webp",
      "backBlack": "relaxed-tee-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-relaxed.webp",
      "jet-black": "high-quality-relaxed-black.webp"
    },
    "backColorwayImages": {
      "pfd": "relaxed-tee-back-white.webp",
      "jet-black": "relaxed-tee-back-black.webp"
    }
  },
  {
    "id": "mbt009-r",
    "name": "Classic Fit Tee",
    "styleNumber": "BLNKUP-02",
    "category": "Tees",
    "fit": "Classic",
    "fabricWeight": "210-220 GSM / 6.2oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-classic-tee.webp",
      "frontMask": "high-quality-classic-tee-black.webp",
      "frontBlack": "high-quality-classic-tee-black.webp",
      "back": "classic-tee-back-white.webp",
      "backBlack": "classic-tee-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-classic-tee.webp",
      "jet-black": "high-quality-classic-tee-black.webp"
    },
    "backColorwayImages": {
      "pfd": "classic-tee-back-white.webp",
      "jet-black": "classic-tee-back-black.webp"
    }
  },
  {
    "id": "mbt049-c230",
    "name": "Drop Shoulder Tee",
    "styleNumber": "BLNKUP-03",
    "category": "Tees",
    "fit": "Oversized",
    "fabricWeight": "230-250 GSM / 6.8oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-drop-tee.webp",
      "frontMask": "high-quality-drop-tee-2.webp",
      "frontBlack": "high-quality-drop-tee-2.webp",
      "back": "drop-shoulder-tee-back-white.webp",
      "backBlack": "drop-shoulder-tee-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-drop-tee.webp",
      "jet-black": "high-quality-drop-tee-2.webp"
    },
    "backColorwayImages": {
      "pfd": "drop-shoulder-tee-back-white.webp",
      "jet-black": "drop-shoulder-tee-back-black.webp"
    }
  },
  {
    "id": "mbt026-c220",
    "name": "Long Sleeve T-Shirt",
    "styleNumber": "BLNKUP-04",
    "category": "Tees",
    "fit": "Classic",
    "fabricWeight": "220 GSM / 6.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "real-white-long-sleeve-t-shirt.webp",
      "frontMask": "rael-black-long-sleeve-t-shirt.webp",
      "frontBlack": "rael-black-long-sleeve-t-shirt.webp",
      "back": "long-sleeve-tee-back-white.webp",
      "backBlack": "long-sleeve-tee-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "real-white-long-sleeve-t-shirt.webp",
      "jet-black": "rael-black-long-sleeve-t-shirt.webp"
    },
    "backColorwayImages": {
      "pfd": "long-sleeve-tee-back-white.webp",
      "jet-black": "long-sleeve-tee-back-black.webp"
    }
  },
  {
    "id": "mbh010-c380",
    "name": "Oversized Hoodie",
    "styleNumber": "BLNKUP-05",
    "category": "Fleece",
    "fit": "Oversized",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "oversized-hoodie-white-v2.webp",
      "frontMask": "oversized-hoodie-black-v2.webp",
      "frontBlack": "oversized-hoodie-black-v2.webp",
      "back": "oversized-hoodie-back-white.webp",
      "backBlack": "oversized-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "oversized-hoodie-white-v2.webp",
      "jet-black": "oversized-hoodie-black-v2.webp"
    },
    "backColorwayImages": {
      "pfd": "oversized-hoodie-back-white.webp",
      "jet-black": "oversized-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh099-c380",
    "name": "Oversized Crop Hoodie",
    "styleNumber": "BLNKUP-06",
    "category": "Fleece",
    "fit": "Oversized",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "oversized-crop-hoodie-white.webp",
      "frontMask": "oversized-crop-hoodie-black.webp",
      "frontBlack": "oversized-crop-hoodie-black.webp",
      "back": "oversized-crop-hoodie-back-white.webp",
      "backBlack": "oversized-crop-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "oversized-crop-hoodie-white.webp",
      "jet-black": "oversized-crop-hoodie-black.webp"
    },
    "backColorwayImages": {
      "pfd": "oversized-crop-hoodie-back-white.webp",
      "jet-black": "oversized-crop-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh045-c380",
    "name": "Side Pocket Hoodie",
    "styleNumber": "BLNKUP-07",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "side-pocket-hoodie-white.webp",
      "frontMask": "side-pocket-hoodie-black.webp",
      "frontBlack": "side-pocket-hoodie-black.webp",
      "back": "side-pocket-hoodie-back-white.webp",
      "backBlack": "side-pocket-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "side-pocket-hoodie-white.webp",
      "jet-black": "side-pocket-hoodie-black.webp"
    },
    "backColorwayImages": {
      "pfd": "side-pocket-hoodie-back-white.webp",
      "jet-black": "side-pocket-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh048-c380",
    "name": "Zip Hoodie",
    "styleNumber": "BLNKUP-08",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-zip-1.webp",
      "frontMask": "high-quality-zip.webp",
      "frontBlack": "high-quality-zip.webp",
      "back": "zip-hoodie-back-white.webp",
      "backBlack": "zip-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-zip-1.webp",
      "jet-black": "high-quality-zip.webp"
    },
    "backColorwayImages": {
      "pfd": "zip-hoodie-back-white.webp",
      "jet-black": "zip-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh088-c380",
    "name": "Crop Zip Hoodie",
    "styleNumber": "BLNKUP-09",
    "category": "Fleece",
    "fit": "Relaxed",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "crop-zip-up-white.webp",
      "frontMask": "crop-zip-up-black.webp",
      "frontBlack": "crop-zip-up-black.webp",
      "back": "crop-zip-hoodie-back-white.webp",
      "backBlack": "crop-zip-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "crop-zip-up-white.webp",
      "jet-black": "crop-zip-up-black.webp"
    },
    "backColorwayImages": {
      "pfd": "crop-zip-hoodie-back-white.webp",
      "jet-black": "crop-zip-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbc015-c380",
    "name": "Crewneck",
    "styleNumber": "BLNKUP-10",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "crewneck-white-v2.webp",
      "frontMask": "crewneck-normal-black.webp",
      "frontBlack": "crewneck-normal-black.webp",
      "back": "crewneck-back-white.webp",
      "backBlack": "crewneck-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "crewneck-white-v2.webp",
      "jet-black": "crewneck-normal-black.webp"
    },
    "backColorwayImages": {
      "pfd": "crewneck-back-white.webp",
      "jet-black": "crewneck-back-black.webp"
    }
  },
  {
    "id": "mbh021-c470",
    "name": "Heavy Hoodie",
    "styleNumber": "BLNKUP-11",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "470 GSM / 14oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "heavy-hoodie-pfd.webp",
      "frontMask": "heavy-hoodie-jet-black.webp",
      "frontBlack": "heavy-hoodie-jet-black.webp",
      "back": "heavy-hoodie-back-white.webp",
      "backBlack": "heavy-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "heavy-hoodie-pfd.webp",
      "jet-black": "heavy-hoodie-jet-black.webp"
    },
    "backColorwayImages": {
      "pfd": "heavy-hoodie-back-white.webp",
      "jet-black": "heavy-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh048-c470",
    "name": "Heavy Zip Hoodie",
    "styleNumber": "BLNKUP-12",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "470 GSM / 14oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "zip-up-high-quality.webp",
      "frontMask": "high-quality-zip.webp",
      "frontBlack": "high-quality-zip.webp",
      "back": "heavy-zip-hoodie-back-white.webp",
      "backBlack": "heavy-zip-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "zip-up-high-quality.webp",
      "jet-black": "high-quality-zip.webp"
    },
    "backColorwayImages": {
      "pfd": "heavy-zip-hoodie-back-white.webp",
      "jet-black": "heavy-zip-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbc015-c470",
    "name": "Heavy Crewneck",
    "styleNumber": "BLNKUP-13",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "470 GSM / 14oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "heavy-crewneck-pfd.webp",
      "frontMask": "heavy-crewneck-jet-black.webp",
      "frontBlack": "heavy-crewneck-jet-black.webp",
      "back": "heavy-crewneck-back-white.webp",
      "backBlack": "heavy-crewneck-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "heavy-crewneck-pfd.webp",
      "jet-black": "heavy-crewneck-jet-black.webp"
    },
    "backColorwayImages": {
      "pfd": "heavy-crewneck-back-white.webp",
      "jet-black": "heavy-crewneck-back-black.webp"
    }
  },
  {
    "id": "mbh077-c330",
    "name": "Merch Hoodie",
    "styleNumber": "BLNKUP-14",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "330 GSM / 9.75oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-merch-hoodie-1.webp",
      "frontMask": "high-quality-merch-hoodie.webp",
      "frontBlack": "high-quality-merch-hoodie.webp",
      "back": "merch-hoodie-back-white.webp",
      "backBlack": "merch-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-merch-hoodie-1.webp",
      "jet-black": "high-quality-merch-hoodie.webp"
    },
    "backColorwayImages": {
      "pfd": "merch-hoodie-back-white.webp",
      "jet-black": "merch-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbj018-c380",
    "name": "Jogger Sweatpant",
    "styleNumber": "BLNKUP-15",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-jogger.webp",
      "frontMask": "high-quality-jogger-2.webp",
      "frontBlack": "high-quality-jogger-2.webp",
      "back": "jogger-sweatpant-back-white.webp",
      "backBlack": "jogger-sweatpant-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-jogger.webp",
      "jet-black": "high-quality-jogger-2.webp"
    },
    "backColorwayImages": {
      "pfd": "jogger-sweatpant-back-white.webp",
      "jet-black": "jogger-sweatpant-back-black.webp"
    }
  },
  {
    "id": "mbs022-c470",
    "name": "Heavy Jogger Sweatpant",
    "styleNumber": "BLNKUP-16",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "470 GSM / 14oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "heavy-jogger-sweatpants-white.webp",
      "frontMask": "black-jogger-sweatpant.webp",
      "frontBlack": "black-jogger-sweatpant.webp",
      "back": "heavy-jogger-sweatpant-back-white.webp",
      "backBlack": "heavy-jogger-sweatpant-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "heavy-jogger-sweatpants-white.webp",
      "jet-black": "black-jogger-sweatpant.webp"
    },
    "backColorwayImages": {
      "pfd": "heavy-jogger-sweatpant-back-white.webp",
      "jet-black": "heavy-jogger-sweatpant-back-black.webp"
    }
  },
  {
    "id": "mbs037-c380",
    "name": "Open Leg Sweatpant",
    "styleNumber": "BLNKUP-17",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-open-leg.webp",
      "frontMask": "high-quality-open-leg-2.webp",
      "frontBlack": "high-quality-open-leg-2.webp",
      "back": "open-leg-sweatpant-back-white.webp",
      "backBlack": "open-leg-sweatpant-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-open-leg.webp",
      "jet-black": "high-quality-open-leg-2.webp"
    },
    "backColorwayImages": {
      "pfd": "open-leg-sweatpant-back-white.webp",
      "jet-black": "open-leg-sweatpant-back-black.webp"
    }
  },
  {
    "id": "mbs024-c470",
    "name": "Heavy Open Leg Sweatpant",
    "styleNumber": "BLNKUP-18",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "470 GSM / 14oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "pfd-heavy-open-leg-sweatpant.webp",
      "frontMask": "heavy-open-leg-sweatpant-jet-black.webp",
      "frontBlack": "heavy-open-leg-sweatpant-jet-black.webp",
      "back": "heavy-open-leg-sweatpant-back-white.webp",
      "backBlack": "heavy-open-leg-sweatpant-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "pfd-heavy-open-leg-sweatpant.webp",
      "jet-black": "heavy-open-leg-sweatpant-jet-black.webp"
    },
    "backColorwayImages": {
      "pfd": "heavy-open-leg-sweatpant-back-white.webp",
      "jet-black": "heavy-open-leg-sweatpant-back-black.webp"
    }
  },
  {
    "id": "mbs017-c380",
    "name": "Sweat Short",
    "styleNumber": "BLNKUP-19",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "sweat-short-white-v2.webp",
      "frontMask": "sweat-short-black-v2.webp",
      "frontBlack": "sweat-short-black-v2.webp",
      "back": "sweat-short-back-white.webp",
      "backBlack": "sweat-short-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "sweat-short-white-v2.webp",
      "jet-black": "sweat-short-black-v2.webp"
    },
    "backColorwayImages": {
      "pfd": "sweat-short-back-white.webp",
      "jet-black": "sweat-short-back-black.webp"
    }
  },
  {
    "id": "mbh021-c500",
    "name": "Premium Hoodie",
    "styleNumber": "BLNKUP-20",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "500 GSM / 14.7oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "premium-hoodie-white.webp",
      "frontMask": "premium-hoodie-black.webp",
      "frontBlack": "premium-hoodie-black.webp",
      "back": "premium-hoodie-back-white.webp",
      "backBlack": "premium-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "premium-hoodie-white.webp",
      "jet-black": "premium-hoodie-black.webp"
    },
    "backColorwayImages": {
      "pfd": "premium-hoodie-back-white.webp",
      "jet-black": "premium-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbh103-c500",
    "name": "Premium Zip Hoodie",
    "styleNumber": "BLNKUP-21",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "500 GSM / 14.7oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "zip-up-high-quality.webp",
      "frontMask": "high-quality-zip.webp",
      "frontBlack": "high-quality-zip.webp",
      "back": "premium-zip-hoodie-back-white.webp",
      "backBlack": "premium-zip-hoodie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "zip-up-high-quality.webp",
      "jet-black": "high-quality-zip.webp"
    },
    "backColorwayImages": {
      "pfd": "premium-zip-hoodie-back-white.webp",
      "jet-black": "premium-zip-hoodie-back-black.webp"
    }
  },
  {
    "id": "mbc102-c500",
    "name": "Premium Crewneck",
    "styleNumber": "BLNKUP-22",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "500 GSM / 14.7oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "high-quality-crewneck.webp",
      "frontMask": "high-quality-crewneck-2.webp",
      "frontBlack": "high-quality-crewneck-2.webp",
      "back": "premium-crewneck-back-white.webp",
      "backBlack": "premium-crewneck-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "high-quality-crewneck.webp",
      "jet-black": "high-quality-crewneck-2.webp"
    },
    "backColorwayImages": {
      "pfd": "premium-crewneck-back-white.webp",
      "jet-black": "premium-crewneck-back-black.webp"
    }
  },
  {
    "id": "mbs101-c500",
    "name": "Premium Sweatpant",
    "styleNumber": "BLNKUP-23",
    "category": "Bottoms",
    "fit": "Classic",
    "fabricWeight": "500 GSM / 14.7oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "premium-sweatpant-pfd.webp",
      "frontMask": "premium-sweatpant-jet-black.webp",
      "frontBlack": "premium-sweatpant-jet-black.webp",
      "back": "premium-sweatpant-back-white.webp",
      "backBlack": "premium-sweatpant-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "premium-sweatpant-pfd.webp",
      "jet-black": "premium-sweatpant-jet-black.webp"
    },
    "backColorwayImages": {
      "pfd": "premium-sweatpant-back-white.webp",
      "jet-black": "premium-sweatpant-back-black.webp"
    }
  },
  {
    "id": "flc-quarter-zip",
    "name": "Quarter Zip",
    "styleNumber": "BLNKUP-F01",
    "category": "Fleece",
    "fit": "Classic",
    "fabricWeight": "380 GSM / 11.5oz",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "quarter-zip-white-v2.webp",
      "frontMask": "quarter-zip-black-v2.webp",
      "frontBlack": "quarter-zip-black-v2.webp",
      "back": "quarter-zip-back-white.webp",
      "backBlack": "quarter-zip-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "quarter-zip-white-v2.webp",
      "jet-black": "quarter-zip-black-v2.webp"
    },
    "backColorwayImages": {
      "pfd": "quarter-zip-back-white.webp",
      "jet-black": "quarter-zip-back-black.webp"
    }
  },
  {
    "id": "acc-plush-blanket",
    "name": "Plush Blanket",
    "styleNumber": "BLNKUP-A01",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Plush microfleece",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "blanket-white.webp",
      "frontMask": "blanket-black.webp",
      "frontBlack": "blanket-black.webp",
      "back": "blanket-white.webp",
      "backBlack": "blanket-black.webp"
    },
    "colorwayImages": {
      "pfd": "blanket-white-folded.webp",
      "jet-black": "blanket-black-folded.webp"
    },
    "backColorwayImages": {
      "pfd": "blanket-white.webp",
      "jet-black": "blanket-black.webp"
    },
    "mockupImages": {
      "pfd": "blanket-white.webp",
      "jet-black": "blanket-black.webp"
    }
  },
  {
    "id": "acc-canvas-tote",
    "name": "Canvas Tote Bag",
    "styleNumber": "BLNKUP-A02",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "12 oz canvas",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "tote-bag-white.webp",
      "frontMask": "tote-bag-black.webp",
      "frontBlack": "tote-bag-black.webp",
      "back": "canvas-tote-back-white.webp",
      "backBlack": "canvas-tote-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "tote-bag-white.webp",
      "jet-black": "tote-bag-black.webp"
    },
    "backColorwayImages": {
      "pfd": "canvas-tote-back-white.webp",
      "jet-black": "canvas-tote-back-black.webp"
    }
  },
  {
    "id": "acc-windbreaker",
    "name": "Windbreaker",
    "styleNumber": "BLNKUP-A03",
    "category": "Outerwear",
    "fit": "Athletic",
    "fabricWeight": "Ripstop nylon",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "windbreaker-white.webp",
      "frontMask": "windbreaker-black.webp",
      "frontBlack": "windbreaker-black.webp",
      "back": "windbreaker-back-white.webp",
      "backBlack": "windbreaker-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "windbreaker-white.webp",
      "jet-black": "windbreaker-black.webp"
    },
    "backColorwayImages": {
      "pfd": "windbreaker-back-white.webp",
      "jet-black": "windbreaker-back-black.webp"
    }
  },
  {
    "id": "acc-blank-keycap",
    "name": "Blank Keycap",
    "styleNumber": "BLNKUP-A04",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Single keycap",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "keycap-white.webp",
      "frontMask": "keycap-black.webp",
      "frontBlack": "keycap-black.webp"
    },
    "colorwayImages": {
      "white": "keycap-white.webp",
      "jet-black": "keycap-black.webp"
    },
    "backColorwayImages": {}
  },
  {
    "id": "acc-insulated-water-bottle",
    "name": "Insulated Water Bottle",
    "styleNumber": "BLNKUP-A05",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Insulated drinkware",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "insulated-water-bottle-white.webp",
      "frontMask": "insulated-water-bottle-black.webp",
      "frontBlack": "insulated-water-bottle-black.webp",
      "back": "insulated-water-bottle-white.webp",
      "backBlack": "insulated-water-bottle-black.webp"
    },
    "colorwayImages": {
      "white": "insulated-water-bottle-white.webp",
      "jet-black": "insulated-water-bottle-black.webp"
    },
    "backColorwayImages": {
      "white": "insulated-water-bottle-white.webp",
      "jet-black": "insulated-water-bottle-black.webp"
    }
  },
  {
    "id": "acc-clear-water-bottle",
    "name": "Clear Water Bottle",
    "styleNumber": "BLNKUP-A06",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Reusable drinkware",
    "colors": [
      {
        "id": "clear",
        "label": "Clear",
        "swatchHex": "#f8fafc"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "clear-water-bottle.webp",
      "frontMask": "clear-water-bottle-black.webp",
      "frontBlack": "clear-water-bottle-black.webp",
      "back": "clear-water-bottle.webp",
      "backBlack": "clear-water-bottle-black.webp"
    },
    "colorwayImages": {
      "clear": "clear-water-bottle.webp",
      "jet-black": "clear-water-bottle-black.webp"
    },
    "backColorwayImages": {
      "clear": "clear-water-bottle.webp",
      "jet-black": "clear-water-bottle-black.webp"
    },
    "recolorMode": "cap"
  },
  {
    "id": "acc-insulated-tumbler",
    "name": "Insulated Tumbler",
    "styleNumber": "BLNKUP-A07",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Insulated drinkware",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "tumbler-white.webp",
      "frontMask": "tumbler-black.webp",
      "frontBlack": "tumbler-black.webp",
      "back": "tumbler-white.webp",
      "backBlack": "tumbler-black.webp"
    },
    "colorwayImages": {
      "white": "tumbler-white.webp",
      "jet-black": "tumbler-black.webp"
    },
    "backColorwayImages": {
      "white": "tumbler-white.webp",
      "jet-black": "tumbler-black.webp"
    }
  },
  {
    "id": "acc-crew-socks",
    "name": "Crew Socks",
    "styleNumber": "BLNKUP-A08",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Crew sock pair",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "crew-sock-white.webp",
      "frontMask": "crew-sock-black.webp",
      "frontBlack": "crew-sock-black.webp",
      "back": "crew-sock-white.webp",
      "backBlack": "crew-sock-black.webp"
    },
    "colorwayImages": {
      "white": "crew-sock-white.webp",
      "jet-black": "crew-sock-black.webp"
    },
    "backColorwayImages": {
      "white": "crew-sock-white.webp",
      "jet-black": "crew-sock-black.webp"
    }
  },
  {
    "id": "acc-lanyard",
    "name": "Lanyard",
    "styleNumber": "BLNKUP-A09",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Woven strap",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "lanyard-white.webp",
      "frontMask": "lanyard-black.webp",
      "frontBlack": "lanyard-black.webp",
      "back": "lanyard-white.webp",
      "backBlack": "lanyard-black.webp"
    },
    "colorwayImages": {
      "white": "lanyard-white.webp",
      "jet-black": "lanyard-black.webp"
    },
    "backColorwayImages": {
      "white": "lanyard-white.webp",
      "jet-black": "lanyard-black.webp"
    }
  },
  {
    "id": "acc-badge-holder",
    "name": "Badge Holder",
    "styleNumber": "BLNKUP-A10",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Badge sleeve",
    "colors": [
      {
        "id": "clear",
        "label": "Clear",
        "swatchHex": "#f8fafc"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "badge-holder-clear.webp",
      "frontMask": "badge-holder-black.webp",
      "frontBlack": "badge-holder-black.webp",
      "back": "badge-holder-clear.webp",
      "backBlack": "badge-holder-black.webp"
    },
    "colorwayImages": {
      "clear": "badge-holder-clear.webp",
      "jet-black": "badge-holder-black.webp"
    },
    "backColorwayImages": {
      "clear": "badge-holder-clear.webp",
      "jet-black": "badge-holder-black.webp"
    }
  },
  {
    "id": "acc-custom-pin",
    "name": "Custom Pin",
    "styleNumber": "BLNKUP-A11",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Lapel pin",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "heart-pin-white.webp",
      "frontMask": "heart-pin-black.webp",
      "frontBlack": "heart-pin-black.webp",
      "back": "heart-pin-white.webp",
      "backBlack": "heart-pin-black.webp"
    },
    "colorwayImages": {
      "white": "heart-pin-white.webp",
      "jet-black": "heart-pin-black.webp"
    },
    "backColorwayImages": {
      "white": "heart-pin-white.webp",
      "jet-black": "heart-pin-black.webp"
    }
  },
  {
    "id": "acc-bandana",
    "name": "Bandana",
    "styleNumber": "BLNKUP-A12",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Cotton bandana",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "bandana-white.webp",
      "frontMask": "bandana-black.webp",
      "frontBlack": "bandana-black.webp",
      "back": "bandana-white.webp",
      "backBlack": "bandana-black.webp"
    },
    "colorwayImages": {
      "white": "bandana-white.webp",
      "jet-black": "bandana-black.webp"
    },
    "backColorwayImages": {
      "white": "bandana-white.webp",
      "jet-black": "bandana-black.webp"
    }
  },
  {
    "id": "acc-vinyl-record",
    "name": "Vinyl Record",
    "styleNumber": "BLNKUP-A13",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Vinyl disc",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "vinyl-record-white.webp",
      "frontMask": "vinyl-record-black.webp",
      "frontBlack": "vinyl-record-black.webp",
      "back": "vinyl-record-white.webp",
      "backBlack": "vinyl-record-black.webp"
    },
    "colorwayImages": {
      "white": "vinyl-record-white.webp",
      "jet-black": "vinyl-record-black.webp"
    },
    "backColorwayImages": {
      "white": "vinyl-record-white.webp",
      "jet-black": "vinyl-record-black.webp"
    }
  },
  {
    "id": "acc-luggage-tag",
    "name": "Luggage Tag",
    "styleNumber": "BLNKUP-A15",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Luggage tag",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "luggage-tag-white.webp",
      "frontMask": "luggage-tag-black.webp",
      "frontBlack": "luggage-tag-black.webp",
      "back": "luggage-tag-white.webp",
      "backBlack": "luggage-tag-black.webp"
    },
    "colorwayImages": {
      "white": "luggage-tag-white.webp",
      "jet-black": "luggage-tag-black.webp"
    },
    "backColorwayImages": {
      "white": "luggage-tag-white.webp",
      "jet-black": "luggage-tag-black.webp"
    }
  },
  {
    "id": "acc-backpack",
    "name": "Backpack",
    "styleNumber": "BLNKUP-A16",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Durable synthetic",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "backpack-white.webp",
      "frontMask": "backpack-black.webp",
      "frontBlack": "backpack-black.webp"
    },
    "colorwayImages": {
      "pfd": "backpack-white.webp",
      "jet-black": "backpack-black.webp"
    },
    "backColorwayImages": {}
  },
  {
    "id": "acc-apron",
    "name": "Apron",
    "styleNumber": "BLNKUP-A17",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Cotton blend",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "apron-white.webp",
      "frontMask": "apron-black.webp",
      "frontBlack": "apron-black.webp"
    },
    "colorwayImages": {
      "pfd": "apron-white.webp",
      "jet-black": "apron-black.webp"
    },
    "backColorwayImages": {}
  },
  {
    "id": "acc-fanny-pack",
    "name": "Fanny Pack",
    "styleNumber": "BLNKUP-A18",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Durable synthetic",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "fanny-pack-white.webp",
      "frontMask": "fanny-pack-black.webp",
      "frontBlack": "fanny-pack-black.webp"
    },
    "colorwayImages": {
      "pfd": "fanny-pack-white.webp",
      "jet-black": "fanny-pack-black.webp"
    },
    "backColorwayImages": {}
  },
  {
    "id": "acc-mousepad",
    "name": "Mousepad",
    "styleNumber": "BLNKUP-A19",
    "category": "Accessories",
    "fit": "Classic",
    "fabricWeight": "Cloth mousepad",
    "colors": [
      {
        "id": "white",
        "label": "White",
        "swatchHex": "#f5f5f4"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "mousepad-white.webp",
      "frontMask": "mousepad-black.webp",
      "frontBlack": "mousepad-black.webp",
      "back": "mousepad-white.webp",
      "backBlack": "mousepad-black.webp"
    },
    "colorwayImages": {
      "white": "mousepad-white.webp",
      "jet-black": "mousepad-black.webp"
    },
    "backColorwayImages": {
      "white": "mousepad-white.webp",
      "jet-black": "mousepad-black.webp"
    }
  },
  {
    "id": "hw-classic-6panel",
    "name": "Classic 6-Panel Cap",
    "styleNumber": "BLNKUP-H01",
    "category": "Headwear",
    "fit": "Classic",
    "fabricWeight": "Cotton twill",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "cap-white.webp",
      "frontMask": "cap-black.webp",
      "frontBlack": "cap-black.webp",
      "back": "six-panel-cap-back-white.webp",
      "backBlack": "six-panel-cap-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "cap-white.webp",
      "jet-black": "cap-black.webp"
    },
    "backColorwayImages": {
      "pfd": "six-panel-cap-back-white.webp",
      "jet-black": "six-panel-cap-back-black.webp"
    },
    "mockupImages": {
      "pfd": "cap-white.webp",
      "jet-black": "cap-black.webp"
    }
  },
  {
    "id": "hw-ribbed-beanie",
    "name": "Ribbed Beanie",
    "styleNumber": "BLNKUP-H02",
    "category": "Headwear",
    "fit": "Classic",
    "fabricWeight": "Acrylic rib knit",
    "colors": [
      {
        "id": "pfd",
        "label": "PFD",
        "swatchHex": "#f4f4f5"
      },
      {
        "id": "jet-black",
        "label": "Jet black",
        "swatchHex": "#000000"
      }
    ],
    "photos": {
      "front": "beanie-white.webp",
      "frontMask": "beanie-black.webp",
      "frontBlack": "beanie-black.webp",
      "back": "ribbed-beanie-back-white.webp",
      "backBlack": "ribbed-beanie-back-black.webp"
    },
    "colorwayImages": {
      "pfd": "beanie-white.webp",
      "jet-black": "beanie-black.webp"
    },
    "backColorwayImages": {
      "pfd": "ribbed-beanie-back-white.webp",
      "jet-black": "ribbed-beanie-back-black.webp"
    }
  }
];

// piobox-customizer/data/photo-frames.js
var PHOTO_FRAMES = {
  "apron-black.webp": {
    "w": 0.3984,
    "h": 0.9844,
    "cx": 0.5,
    "cy": 0.4961,
    "ar": 1
  },
  "apron-white.webp": {
    "w": 0.4414,
    "h": 0.9805,
    "cx": 0.498,
    "cy": 0.498,
    "ar": 1
  },
  "backpack-black.webp": {
    "w": 0.6602,
    "h": 0.9453,
    "cx": 0.4941,
    "cy": 0.4961,
    "ar": 1
  },
  "backpack-white.webp": {
    "w": 0.7656,
    "h": 0.8906,
    "cx": 0.4922,
    "cy": 0.5,
    "ar": 1
  },
  "badge-holder-black.webp": {
    "w": 0.5078,
    "h": 0.8359,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "badge-holder-clear.webp": {
    "w": 0.5391,
    "h": 0.8828,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "bandana-black.webp": {
    "w": 0.7656,
    "h": 0.8828,
    "cx": 0.5234,
    "cy": 0.5156,
    "ar": 1
  },
  "bandana-white.webp": {
    "w": 0.7266,
    "h": 0.875,
    "cx": 0.5039,
    "cy": 0.5117,
    "ar": 1
  },
  "beanie-black.webp": {
    "w": 0.6953,
    "h": 0.793,
    "cx": 0.5078,
    "cy": 0.498,
    "ar": 1
  },
  "beanie-white.webp": {
    "w": 0.7344,
    "h": 0.8281,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "black-jogger-sweatpant.webp": {
    "w": 0.5078,
    "h": 0.9492,
    "cx": 0.5,
    "cy": 0.5059,
    "ar": 1
  },
  "blanket-black.webp": {
    "w": 0.7852,
    "h": 0.9219,
    "cx": 0.502,
    "cy": 0.5,
    "ar": 1
  },
  "blanket-white.webp": {
    "w": 0.7813,
    "h": 0.918,
    "cx": 0.5039,
    "cy": 0.5059,
    "ar": 1
  },
  "canvas-tote-back-black.webp": {
    "w": 0.5273,
    "h": 0.8945,
    "cx": 0.498,
    "cy": 0.4863,
    "ar": 1
  },
  "canvas-tote-back-white.webp": {
    "w": 0.5313,
    "h": 0.8945,
    "cx": 0.5,
    "cy": 0.4941,
    "ar": 1
  },
  "cap-black.webp": {
    "w": 0.7617,
    "h": 0.7578,
    "cx": 0.498,
    "cy": 0.4727,
    "ar": 1
  },
  "cap-white.webp": {
    "w": 0.7617,
    "h": 0.7656,
    "cx": 0.498,
    "cy": 0.4727,
    "ar": 1
  },
  "classic-tee-back-black.webp": {
    "w": 0.9258,
    "h": 0.9297,
    "cx": 0.498,
    "cy": 0.4961,
    "ar": 1
  },
  "classic-tee-back-white.webp": {
    "w": 0.9141,
    "h": 0.9102,
    "cx": 0.5,
    "cy": 0.4941,
    "ar": 1
  },
  "clear-water-bottle-black.webp": {
    "w": 0.2695,
    "h": 0.9375,
    "cx": 0.498,
    "cy": 0.5,
    "ar": 1
  },
  "clear-water-bottle.webp": {
    "w": 0.2578,
    "h": 0.9258,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "crew-sock-black.webp": {
    "w": 0.4648,
    "h": 0.8945,
    "cx": 0.5332,
    "cy": 0.5059,
    "ar": 1
  },
  "crew-sock-white.webp": {
    "w": 0.5078,
    "h": 0.9414,
    "cx": 0.5391,
    "cy": 0.498,
    "ar": 1
  },
  "crewneck-back-black.webp": {
    "w": 0.9141,
    "h": 0.8516,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "crewneck-back-white.webp": {
    "w": 0.9023,
    "h": 0.8477,
    "cx": 0.498,
    "cy": 0.4941,
    "ar": 1
  },
  "crewneck-normal-black.webp": {
    "w": 0.875,
    "h": 0.9336,
    "cx": 0.5,
    "cy": 0.4941,
    "ar": 1.2407
  },
  "crewneck-white-v2.webp": {
    "w": 0.875,
    "h": 0.8438,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "crop-zip-hoodie-back-black.webp": {
    "w": 0.8398,
    "h": 0.7422,
    "cx": 0.498,
    "cy": 0.4922,
    "ar": 0.8003
  },
  "crop-zip-hoodie-back-white.webp": {
    "w": 0.8906,
    "h": 0.7422,
    "cx": 0.5117,
    "cy": 0.4922,
    "ar": 0.75
  },
  "crop-zip-up-black.webp": {
    "w": 0.8555,
    "h": 0.75,
    "cx": 0.498,
    "cy": 0.5078,
    "ar": 0.7998
  },
  "crop-zip-up-white.webp": {
    "w": 0.832,
    "h": 0.7266,
    "cx": 0.498,
    "cy": 0.4961,
    "ar": 0.7998
  },
  "drop-shoulder-tee-back-black.webp": {
    "w": 0.9531,
    "h": 0.8516,
    "cx": 0.5,
    "cy": 0.4961,
    "ar": 1
  },
  "drop-shoulder-tee-back-white.webp": {
    "w": 0.9414,
    "h": 0.8633,
    "cx": 0.502,
    "cy": 0.502,
    "ar": 1
  },
  "fanny-pack-black.webp": {
    "w": 0.9492,
    "h": 0.5117,
    "cx": 0.502,
    "cy": 0.502,
    "ar": 1
  },
  "fanny-pack-white.webp": {
    "w": 0.9609,
    "h": 0.3789,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "heart-pin-black.webp": {
    "w": 0.7461,
    "h": 0.7422,
    "cx": 0.498,
    "cy": 0.5039,
    "ar": 1
  },
  "heart-pin-white.webp": {
    "w": 0.7578,
    "h": 0.7344,
    "cx": 0.4961,
    "cy": 0.5078,
    "ar": 1
  },
  "heavy-crewneck-back-black.webp": {
    "w": 0.8789,
    "h": 0.6563,
    "cx": 0.498,
    "cy": 0.4883,
    "ar": 0.8003
  },
  "heavy-crewneck-back-white.webp": {
    "w": 0.9531,
    "h": 0.7383,
    "cx": 0.5,
    "cy": 0.4824,
    "ar": 0.7998
  },
  "heavy-crewneck-jet-black.webp": {
    "w": 0.8867,
    "h": 0.8711,
    "cx": 0.5059,
    "cy": 0.4902,
    "ar": 1
  },
  "heavy-crewneck-pfd.webp": {
    "w": 0.875,
    "h": 0.8438,
    "cx": 0.5,
    "cy": 0.4805,
    "ar": 1
  },
  "heavy-hoodie-back-black.webp": {
    "w": 0.8516,
    "h": 0.8125,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 0.8003
  },
  "heavy-hoodie-back-white.webp": {
    "w": 0.8633,
    "h": 0.8125,
    "cx": 0.502,
    "cy": 0.4961,
    "ar": 0.7998
  },
  "heavy-hoodie-jet-black.webp": {
    "w": 0.8438,
    "h": 0.8633,
    "cx": 0.5,
    "cy": 0.5059,
    "ar": 1
  },
  "heavy-hoodie-pfd.webp": {
    "w": 0.8438,
    "h": 0.8633,
    "cx": 0.5,
    "cy": 0.5059,
    "ar": 1
  },
  "heavy-jogger-sweatpant-back-black.webp": {
    "w": 0.5117,
    "h": 0.9609,
    "cx": 0.498,
    "cy": 0.5,
    "ar": 1
  },
  "heavy-jogger-sweatpant-back-white.webp": {
    "w": 0.5078,
    "h": 0.9492,
    "cx": 0.4961,
    "cy": 0.498,
    "ar": 1
  },
  "heavy-jogger-sweatpants-white.webp": {
    "w": 0.5078,
    "h": 0.957,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "heavy-open-leg-sweatpant-back-black.webp": {
    "w": 0.5234,
    "h": 0.9336,
    "cx": 0.4961,
    "cy": 0.502,
    "ar": 1
  },
  "heavy-open-leg-sweatpant-back-white.webp": {
    "w": 0.5664,
    "h": 0.9414,
    "cx": 0.498,
    "cy": 0.502,
    "ar": 1
  },
  "heavy-open-leg-sweatpant-jet-black.webp": {
    "w": 0.5469,
    "h": 0.9297,
    "cx": 0.4961,
    "cy": 0.5039,
    "ar": 1
  },
  "heavy-zip-hoodie-back-black.webp": {
    "w": 0.8359,
    "h": 0.7578,
    "cx": 0.5,
    "cy": 0.5039,
    "ar": 0.8003
  },
  "heavy-zip-hoodie-back-white.webp": {
    "w": 0.8398,
    "h": 0.7578,
    "cx": 0.498,
    "cy": 0.5039,
    "ar": 0.7998
  },
  "high-quality-classic-tee-black.webp": {
    "w": 0.9258,
    "h": 0.9297,
    "cx": 0.502,
    "cy": 0.5039,
    "ar": 1
  },
  "high-quality-classic-tee.webp": {
    "w": 0.9102,
    "h": 0.9102,
    "cx": 0.498,
    "cy": 0.498,
    "ar": 1
  },
  "high-quality-crewneck-2.webp": {
    "w": 0.8828,
    "h": 0.8672,
    "cx": 0.5039,
    "cy": 0.5,
    "ar": 1
  },
  "high-quality-crewneck.webp": {
    "w": 0.8789,
    "h": 0.8516,
    "cx": 0.502,
    "cy": 0.4961,
    "ar": 1
  },
  "high-quality-drop-tee-2.webp": {
    "w": 0.9531,
    "h": 0.8477,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "high-quality-drop-tee.webp": {
    "w": 0.9375,
    "h": 0.8398,
    "cx": 0.5039,
    "cy": 0.498,
    "ar": 1
  },
  "high-quality-jogger-2.webp": {
    "w": 0.4766,
    "h": 0.8203,
    "cx": 0.5,
    "cy": 0.5039,
    "ar": 0.8003
  },
  "high-quality-jogger.webp": {
    "w": 0.4922,
    "h": 0.8242,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 0.8003
  },
  "high-quality-merch-hoodie-1.webp": {
    "w": 0.9336,
    "h": 0.7773,
    "cx": 0.5059,
    "cy": 0.502,
    "ar": 0.7495
  },
  "high-quality-merch-hoodie.webp": {
    "w": 0.9453,
    "h": 0.7773,
    "cx": 0.5117,
    "cy": 0.5059,
    "ar": 0.75
  },
  "high-quality-open-leg-2.webp": {
    "w": 0.4805,
    "h": 0.9336,
    "cx": 0.502,
    "cy": 0.502,
    "ar": 1
  },
  "high-quality-open-leg.webp": {
    "w": 0.4688,
    "h": 0.9258,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "high-quality-relaxed-black.webp": {
    "w": 0.9609,
    "h": 0.9609,
    "cx": 0.4961,
    "cy": 0.5039,
    "ar": 1
  },
  "high-quality-relaxed.webp": {
    "w": 0.9766,
    "h": 0.9414,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 1
  },
  "high-quality-zip-1.webp": {
    "w": 0.8828,
    "h": 0.7852,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 0.8003
  },
  "high-quality-zip.webp": {
    "w": 0.875,
    "h": 0.8125,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 0.8003
  },
  "insulated-water-bottle-black.webp": {
    "w": 0.2813,
    "h": 0.9453,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "insulated-water-bottle-white.webp": {
    "w": 0.2813,
    "h": 0.9453,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "jogger-sweatpant-back-black.webp": {
    "w": 0.4922,
    "h": 0.8398,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 0.8003
  },
  "jogger-sweatpant-back-white.webp": {
    "w": 0.5078,
    "h": 0.8281,
    "cx": 0.4961,
    "cy": 0.5,
    "ar": 0.7998
  },
  "keycap-black.webp": {
    "w": 0.7109,
    "h": 0.6836,
    "cx": 0.5391,
    "cy": 0.5254,
    "ar": 1
  },
  "keycap-white.webp": {
    "w": 0.6289,
    "h": 0.625,
    "cx": 0.502,
    "cy": 0.4961,
    "ar": 1
  },
  "lanyard-black.webp": {
    "w": 0.2617,
    "h": 0.9648,
    "cx": 0.498,
    "cy": 0.502,
    "ar": 1
  },
  "lanyard-white.webp": {
    "w": 0.3594,
    "h": 0.9727,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 1
  },
  "long-sleeve-tee-back-black.webp": {
    "w": 0.8359,
    "h": 0.8672,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "long-sleeve-tee-back-white.webp": {
    "w": 0.8281,
    "h": 0.8594,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "luggage-tag-black.webp": {
    "w": 0.4805,
    "h": 0.9063,
    "cx": 0.541,
    "cy": 0.5,
    "ar": 1
  },
  "luggage-tag-white.webp": {
    "w": 0.4141,
    "h": 0.9102,
    "cx": 0.5156,
    "cy": 0.502,
    "ar": 1
  },
  "merch-hoodie-back-black.webp": {
    "w": 0.8984,
    "h": 0.8164,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 0.8003
  },
  "merch-hoodie-back-white.webp": {
    "w": 0.8594,
    "h": 0.7578,
    "cx": 0.5,
    "cy": 0.5156,
    "ar": 0.7998
  },
  "mousepad-black.webp": {
    "w": 0.8516,
    "h": 0.707,
    "cx": 0.5039,
    "cy": 0.498,
    "ar": 1
  },
  "mousepad-white.webp": {
    "w": 0.8438,
    "h": 0.7227,
    "cx": 0.5039,
    "cy": 0.502,
    "ar": 1
  },
  "open-leg-sweatpant-back-black.webp": {
    "w": 0.4297,
    "h": 0.9063,
    "cx": 0.5,
    "cy": 0.5039,
    "ar": 1
  },
  "open-leg-sweatpant-back-white.webp": {
    "w": 0.4922,
    "h": 0.9375,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "oversized-crop-hoodie-back-black.webp": {
    "w": 0.8633,
    "h": 0.7852,
    "cx": 0.498,
    "cy": 0.5137,
    "ar": 1
  },
  "oversized-crop-hoodie-back-white.webp": {
    "w": 0.8359,
    "h": 0.7305,
    "cx": 0.5,
    "cy": 0.4941,
    "ar": 0.7998
  },
  "oversized-crop-hoodie-black.webp": {
    "w": 0.8867,
    "h": 0.7461,
    "cx": 0.498,
    "cy": 0.502,
    "ar": 1
  },
  "oversized-crop-hoodie-white.webp": {
    "w": 0.8438,
    "h": 0.7578,
    "cx": 0.5,
    "cy": 0.5156,
    "ar": 1
  },
  "oversized-hoodie-back-black.webp": {
    "w": 0.875,
    "h": 0.8477,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "oversized-hoodie-back-white.webp": {
    "w": 0.875,
    "h": 0.8594,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "oversized-hoodie-black-v2.webp": {
    "w": 0.9766,
    "h": 0.8672,
    "cx": 0.5,
    "cy": 0.4883,
    "ar": 1
  },
  "oversized-hoodie-white-v2.webp": {
    "w": 0.9766,
    "h": 0.875,
    "cx": 0.5,
    "cy": 0.4883,
    "ar": 1
  },
  "pfd-heavy-open-leg-sweatpant.webp": {
    "w": 0.5234,
    "h": 0.9219,
    "cx": 0.5,
    "cy": 0.5039,
    "ar": 1
  },
  "premium-crewneck-back-black.webp": {
    "w": 0.8945,
    "h": 0.832,
    "cx": 0.498,
    "cy": 0.4941,
    "ar": 1
  },
  "premium-crewneck-back-white.webp": {
    "w": 0.8906,
    "h": 0.8242,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 1
  },
  "premium-hoodie-back-black.webp": {
    "w": 0.8516,
    "h": 0.9023,
    "cx": 0.5,
    "cy": 0.5098,
    "ar": 1
  },
  "premium-hoodie-back-white.webp": {
    "w": 0.8633,
    "h": 0.9258,
    "cx": 0.498,
    "cy": 0.502,
    "ar": 1
  },
  "premium-hoodie-black.webp": {
    "w": 0.9141,
    "h": 0.8242,
    "cx": 0.5,
    "cy": 0.5215,
    "ar": 0.7998
  },
  "premium-hoodie-white.webp": {
    "w": 0.8711,
    "h": 0.7969,
    "cx": 0.498,
    "cy": 0.5313,
    "ar": 0.7998
  },
  "premium-sweatpant-back-black.webp": {
    "w": 0.4102,
    "h": 0.832,
    "cx": 0.498,
    "cy": 0.5059,
    "ar": 1
  },
  "premium-sweatpant-back-white.webp": {
    "w": 0.4336,
    "h": 0.8594,
    "cx": 0.502,
    "cy": 0.5,
    "ar": 1
  },
  "premium-sweatpant-jet-black.webp": {
    "w": 0.4844,
    "h": 0.8086,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 0.7998
  },
  "premium-sweatpant-pfd.webp": {
    "w": 0.4766,
    "h": 0.8047,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 0.7998
  },
  "premium-zip-hoodie-back-black.webp": {
    "w": 0.8125,
    "h": 0.832,
    "cx": 0.5,
    "cy": 0.5137,
    "ar": 1
  },
  "premium-zip-hoodie-back-white.webp": {
    "w": 0.7656,
    "h": 0.832,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "quarter-zip-back-black.webp": {
    "w": 0.8516,
    "h": 0.8281,
    "cx": 0.5,
    "cy": 0.5078,
    "ar": 1
  },
  "quarter-zip-back-white.webp": {
    "w": 0.832,
    "h": 0.8477,
    "cx": 0.498,
    "cy": 0.5098,
    "ar": 1
  },
  "quarter-zip-black-v2.webp": {
    "w": 0.8867,
    "h": 0.9102,
    "cx": 0.502,
    "cy": 0.5137,
    "ar": 1
  },
  "quarter-zip-white-v2.webp": {
    "w": 0.9063,
    "h": 0.9258,
    "cx": 0.5,
    "cy": 0.5137,
    "ar": 1
  },
  "rael-black-long-sleeve-t-shirt.webp": {
    "w": 0.8672,
    "h": 0.9063,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "real-white-long-sleeve-t-shirt.webp": {
    "w": 0.8242,
    "h": 0.8516,
    "cx": 0.498,
    "cy": 0.4922,
    "ar": 1
  },
  "relaxed-tee-back-black.webp": {
    "w": 0.9844,
    "h": 0.9492,
    "cx": 0.5,
    "cy": 0.498,
    "ar": 1
  },
  "relaxed-tee-back-white.webp": {
    "w": 0.9766,
    "h": 0.9414,
    "cx": 0.5039,
    "cy": 0.498,
    "ar": 1
  },
  "ribbed-beanie-back-black.webp": {
    "w": 0.7539,
    "h": 0.8242,
    "cx": 0.502,
    "cy": 0.4902,
    "ar": 1
  },
  "ribbed-beanie-back-white.webp": {
    "w": 0.7656,
    "h": 0.8047,
    "cx": 0.5,
    "cy": 0.5,
    "ar": 1
  },
  "side-pocket-hoodie-back-black.webp": {
    "w": 0.9375,
    "h": 0.8477,
    "cx": 0.5,
    "cy": 0.5098,
    "ar": 1
  },
  "side-pocket-hoodie-back-white.webp": {
    "w": 0.9258,
    "h": 0.8477,
    "cx": 0.4941,
    "cy": 0.5098,
    "ar": 1
  },
  "side-pocket-hoodie-black.webp": {
    "w": 0.9453,
    "h": 0.8672,
    "cx": 0.5,
    "cy": 0.5234,
    "ar": 1
  },
  "side-pocket-hoodie-white.webp": {
    "w": 0.9258,
    "h": 0.8398,
    "cx": 0.498,
    "cy": 0.5098,
    "ar": 1
  },
  "six-panel-cap-back-black.webp": {
    "w": 0.7656,
    "h": 0.6563,
    "cx": 0.5,
    "cy": 0.4688,
    "ar": 1
  },
  "six-panel-cap-back-white.webp": {
    "w": 0.8125,
    "h": 0.6797,
    "cx": 0.5,
    "cy": 0.4609,
    "ar": 1
  },
  "sweat-short-back-black.webp": {
    "w": 0.6797,
    "h": 0.7383,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "sweat-short-back-white.webp": {
    "w": 0.7383,
    "h": 0.8516,
    "cx": 0.502,
    "cy": 0.4922,
    "ar": 1
  },
  "sweat-short-black-v2.webp": {
    "w": 0.7578,
    "h": 0.8008,
    "cx": 0.5039,
    "cy": 0.502,
    "ar": 1
  },
  "sweat-short-white-v2.webp": {
    "w": 0.8164,
    "h": 0.7656,
    "cx": 0.502,
    "cy": 0.4883,
    "ar": 1
  },
  "tote-bag-black.webp": {
    "w": 0.5781,
    "h": 0.9063,
    "cx": 0.543,
    "cy": 0.4922,
    "ar": 1
  },
  "tote-bag-white.webp": {
    "w": 0.5234,
    "h": 0.9141,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "tumbler-black.webp": {
    "w": 0.332,
    "h": 0.918,
    "cx": 0.4941,
    "cy": 0.5137,
    "ar": 1
  },
  "tumbler-white.webp": {
    "w": 0.3281,
    "h": 0.8164,
    "cx": 0.5,
    "cy": 0.502,
    "ar": 1
  },
  "vinyl-record-black.webp": {
    "w": 0.9023,
    "h": 0.9063,
    "cx": 0.5059,
    "cy": 0.5039,
    "ar": 1
  },
  "vinyl-record-white.webp": {
    "w": 0.875,
    "h": 0.8867,
    "cx": 0.5,
    "cy": 0.4902,
    "ar": 1
  },
  "windbreaker-back-black.webp": {
    "w": 0.7344,
    "h": 0.9063,
    "cx": 0.5,
    "cy": 0.4961,
    "ar": 1
  },
  "windbreaker-back-white.webp": {
    "w": 0.7266,
    "h": 0.9102,
    "cx": 0.4961,
    "cy": 0.4941,
    "ar": 1
  },
  "windbreaker-black.webp": {
    "w": 0.7344,
    "h": 0.9141,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "windbreaker-white.webp": {
    "w": 0.7344,
    "h": 0.9141,
    "cx": 0.5,
    "cy": 0.4922,
    "ar": 1
  },
  "zip-hoodie-back-black.webp": {
    "w": 0.875,
    "h": 0.75,
    "cx": 0.5,
    "cy": 0.5039,
    "ar": 0.75
  },
  "zip-hoodie-back-white.webp": {
    "w": 0.9492,
    "h": 0.8633,
    "cx": 0.498,
    "cy": 0.5098,
    "ar": 1
  },
  "zip-up-high-quality.webp": {
    "w": 0.8008,
    "h": 0.7383,
    "cx": 0.498,
    "cy": 0.502,
    "ar": 0.8003
  }
};

// piobox-customizer/ui/dom.js
var BOOL_PROPS = /* @__PURE__ */ new Set(["disabled", "checked", "selected", "hidden", "readOnly", "readonly", "multiple", "required"]);
var VALUE_TAGS = /* @__PURE__ */ new Set(["INPUT", "TEXTAREA", "SELECT"]);
function h(tag, props, ...children) {
  const el = document.createElement(tag);
  if (props) {
    for (const [k, v] of Object.entries(props)) {
      if (v == null || v === false) continue;
      if (k === "class" || k === "className") {
        if (v) el.className = Array.isArray(v) ? v.filter(Boolean).join(" ") : String(v);
      } else if (k === "style") {
        if (typeof v === "string") el.setAttribute("style", v);
        else for (const [p, val] of Object.entries(v)) {
          if (val != null) {
            if (p.startsWith("--")) el.style.setProperty(p, val);
            else el.style[p] = val;
          }
        }
      } else if (k === "key") el.setAttribute("data-key", String(v));
      else if (k === "dataset") {
        for (const [d, val] of Object.entries(v)) if (val != null) el.dataset[d] = String(val);
      } else if (k.startsWith("on") && typeof v === "function") {
        (el.__handlers ??= {})[k] = v;
        el[k] = v;
      } else if (k === "value" && VALUE_TAGS.has(el.tagName)) {
        el.value = String(v);
        el.setAttribute("value", String(v));
      } else if (BOOL_PROPS.has(k)) {
        el[k] = !!v;
        if (v) el.setAttribute(k, "");
      } else if (k === "html") el.innerHTML = String(v);
      else if (k === "ref" && typeof v === "function") v(el);
      else el.setAttribute(k, v === true ? "" : String(v));
    }
  }
  append(el, children);
  return el;
}
function append(el, children) {
  for (const c of children) {
    if (c == null || c === false || c === true) continue;
    if (Array.isArray(c)) {
      append(el, c);
      continue;
    }
    el.appendChild(c instanceof Node ? c : document.createTextNode(String(c)));
  }
}
function svg(markup, cls) {
  const span = document.createElement("span");
  span.className = `pc-icon${cls ? " " + cls : ""}`;
  span.setAttribute("aria-hidden", "true");
  span.innerHTML = markup;
  return span;
}
var keyOf = (n) => n.nodeType === 1 ? n.getAttribute("data-key") : null;
function morph(from, to) {
  if (from.nodeType !== to.nodeType || from.nodeName !== to.nodeName) {
    from.replaceWith(to);
    return to;
  }
  if (from.nodeType === 3) {
    if (from.data !== to.data) from.data = to.data;
    return from;
  }
  if (from.nodeType !== 1) return from;
  for (const a of Array.from(to.attributes)) if (from.getAttribute(a.name) !== a.value) from.setAttribute(a.name, a.value);
  for (const a of Array.from(from.attributes)) if (!to.hasAttribute(a.name)) from.removeAttribute(a.name);
  const oldH = from.__handlers ?? {}, newH = to.__handlers ?? {};
  for (const k of Object.keys(oldH)) if (!newH[k]) {
    from[k] = null;
  }
  for (const [k, fn] of Object.entries(newH)) from[k] = fn;
  from.__handlers = newH;
  const active = document.activeElement === from;
  if (VALUE_TAGS.has(from.tagName)) {
    if (from.type === "checkbox" || from.type === "radio") {
      if (from.checked !== to.checked) from.checked = to.checked;
    } else if (!active || from.type === "range" || from.type === "color") {
      if (to.hasAttribute("value") && from.value !== to.value) from.value = to.value;
    }
  }
  for (const p of ["disabled", "hidden", "readOnly", "multiple", "selected"]) if (p in to && from[p] !== to[p]) from[p] = to[p];
  if (from.tagName === "IMG" && from.getAttribute("src") !== to.getAttribute("src")) from.src = to.getAttribute("src") ?? "";
  if (to.hasAttribute("data-keep")) return from;
  const oldKids = Array.from(from.childNodes), newKids = Array.from(to.childNodes);
  const oldByKey = /* @__PURE__ */ new Map();
  for (const k of oldKids) {
    const key = keyOf(k);
    if (key) oldByKey.set(key, k);
  }
  const used = /* @__PURE__ */ new Set();
  let cursor = 0;
  for (const nk of newKids) {
    const key = keyOf(nk);
    let match = null;
    if (key) {
      match = oldByKey.get(key) ?? null;
    } else {
      for (let i = cursor; i < oldKids.length; i++) {
        const ok = oldKids[i];
        if (used.has(ok) || keyOf(ok)) continue;
        if (ok.nodeType === nk.nodeType && ok.nodeName === nk.nodeName) {
          match = ok;
          break;
        }
        break;
      }
    }
    if (match && !used.has(match)) {
      used.add(match);
      const node = morph(match, nk);
      const ref = from.childNodes[cursor];
      if (ref !== node) from.insertBefore(node, ref ?? null);
    } else {
      from.insertBefore(nk, from.childNodes[cursor] ?? null);
    }
    cursor++;
  }
  while (from.childNodes.length > cursor) {
    const last = from.lastChild;
    if (!last || last.parentNode !== from) break;
    from.removeChild(last);
  }
  return from;
}

// piobox-customizer/core/placements.js
var STAGE_PAD_FRAC = 12 / 544;
var STAGE_INCHES = 22;
var APPAREL_TOP_CATEGORIES = ["Tees", "Fleece", "Outerwear"];
var isApparelTopCategory = (c) => APPAREL_TOP_CATEGORIES.includes(c);
var clamp = (v, lo, hi) => Math.min(hi, Math.max(lo, v));
var FRONT_TOP = [
  { id: "center-chest", label: "Center chest", cx: 0.5, cy: 0.4, width: 0.26 },
  { id: "full-front", label: "Full front", cx: 0.5, cy: 0.46, width: 0.38 },
  { id: "left-chest", label: "Left chest", cx: 0.595, cy: 0.335, width: 0.105 },
  { id: "right-chest", label: "Right chest", cx: 0.405, cy: 0.335, width: 0.105 }
];
var FRONT_BOTTOMS = [{ id: "thigh", label: "Thigh", cx: 0.605, cy: 0.42, width: 0.12 }, { id: "hip", label: "Hip", cx: 0.41, cy: 0.36, width: 0.1 }];
var FRONT_HEADWEAR = [{ id: "front-center", label: "Front center", cx: 0.5, cy: 0.43, width: 0.22 }, { id: "front-small", label: "Small front", cx: 0.5, cy: 0.42, width: 0.14 }, { id: "left-panel", label: "Left panel", cx: 0.4, cy: 0.44, width: 0.12 }];
var FRONT_ACCESSORY = [{ id: "center-front", label: "Center front", cx: 0.5, cy: 0.48, width: 0.32 }, { id: "small-front", label: "Small front", cx: 0.5, cy: 0.42, width: 0.18 }, { id: "full-face", label: "Full face", cx: 0.5, cy: 0.5, width: 0.42 }];
var BACK_TOP = [{ id: "full-back", label: "Full back", cx: 0.5, cy: 0.46, width: 0.5 }, { id: "center-back", label: "Center back", cx: 0.5, cy: 0.5, width: 0.36 }, { id: "upper-back", label: "Upper back", cx: 0.5, cy: 0.33, width: 0.3 }];
var BACK_BOTTOMS = [{ id: "back-hip", label: "Back hip", cx: 0.5, cy: 0.4, width: 0.14 }, { id: "seat", label: "Seat", cx: 0.5, cy: 0.5, width: 0.18 }];
var frontPresets = (cat) => cat === "Headwear" ? FRONT_HEADWEAR : cat === "Accessories" ? FRONT_ACCESSORY : cat === "Bottoms" ? FRONT_BOTTOMS : FRONT_TOP;
function placementPresetsForSide(category, side) {
  if (side === "front") return frontPresets(category);
  if (isApparelTopCategory(category)) return BACK_TOP;
  if (category === "Bottoms") return BACK_BOTTOMS;
  return frontPresets(category);
}
function printZoneForSide(category, side) {
  if (side === "back" && isApparelTopCategory(category)) return { x: 0.24, y: 0.18, w: 0.52, h: 0.54 };
  if (category === "Headwear") return { x: 0.34, y: 0.3, w: 0.32, h: 0.22 };
  if (category === "Accessories") return { x: 0.28, y: 0.32, w: 0.44, h: 0.36 };
  if (category === "Bottoms") return { x: 0.34, y: 0.28, w: 0.38, h: 0.34 };
  return { x: 0.3, y: 0.26, w: 0.4, h: 0.38 };
}
function constrainPlacementToZone(p, rect, aspect) {
  const ar = Number.isFinite(aspect) && aspect > 0 ? aspect : 1;
  const rad = p.rotation * Math.PI / 180, c = Math.abs(Math.cos(rad)), s = Math.abs(Math.sin(rad));
  const maxW = Math.max(8e-3, Math.min(0.92, rect.w / (c + ar * s || 1), rect.h / (s + ar * c || 1)));
  const width = clamp(p.width, Math.min(0.04, maxW), maxW);
  const hw = (width * c + width * ar * s) / 2, hh = (width * s + width * ar * c) / 2;
  const x0 = rect.x + hw, x1 = rect.x + rect.w - hw, y0 = rect.y + hh, y1 = rect.y + rect.h - hh;
  return { ...p, width, cx: x0 <= x1 ? clamp(p.cx, x0, x1) : rect.x + rect.w / 2, cy: y0 <= y1 ? clamp(p.cy, y0, y1) : rect.y + rect.h / 2 };
}
function estimateArtworkQuality(naturalW, width, isVector) {
  if (isVector) return { quality: "vector", dpi: null };
  const dpi = Math.round(naturalW / Math.max(0.5, STAGE_INCHES * width));
  return dpi >= 150 ? { quality: "great", dpi } : dpi >= 90 ? { quality: "good", dpi } : { quality: "low", dpi };
}
var panel = (label, x, y, w, h2, spots) => ({ label, x, y, w, h: h2, spots });
var ACCESSORY_PANELS = {
  bottle: panel("Bottle panel", 0.17, 0.28, 0.66, 0.4, [{ id: "bottle-center", label: "Center mark", x: 0.5, y: 0.43, w: 0.54 }, { id: "bottle-small", label: "Small mark", x: 0.5, y: 0.39, w: 0.38 }]),
  tote: panel("Tote panel", 0.15, 0.45, 0.7, 0.39, [{ id: "tote-center", label: "Center front", x: 0.5, y: 0.58, w: 0.5 }, { id: "tote-large", label: "Large front", x: 0.5, y: 0.62, w: 0.64 }]),
  backpack: panel("Front panel", 0.2, 0.28, 0.6, 0.48, [{ id: "backpack-center", label: "Center front", x: 0.5, y: 0.48, w: 0.42 }, { id: "backpack-small", label: "Small front", x: 0.5, y: 0.42, w: 0.28 }]),
  apron: panel("Front panel", 0.22, 0.18, 0.56, 0.58, [{ id: "apron-chest", label: "Chest mark", x: 0.5, y: 0.34, w: 0.36 }, { id: "apron-center", label: "Center front", x: 0.5, y: 0.48, w: 0.44 }]),
  fanny: panel("Front panel", 0.22, 0.28, 0.56, 0.38, [{ id: "fanny-center", label: "Center front", x: 0.5, y: 0.46, w: 0.4 }, { id: "fanny-small", label: "Small front", x: 0.5, y: 0.42, w: 0.26 }]),
  blanket: panel("Blanket face", 0.09, 0.09, 0.82, 0.82, [{ id: "blanket-center", label: "Center", x: 0.5, y: 0.5, w: 0.46 }, { id: "blanket-corner", label: "Lower corner", x: 0.7, y: 0.72, w: 0.2 }]),
  keycap: panel("Keycap face", 0.2, 0.2, 0.6, 0.48, [{ id: "keycap-center", label: "Center", x: 0.5, y: 0.43, w: 0.38 }, { id: "keycap-small", label: "Small mark", x: 0.5, y: 0.41, w: 0.25 }]),
  cap: panel("Crown", 0.22, 0.22, 0.56, 0.3, [{ id: "front-center", label: "Front center", x: 0.5, y: 0.37, w: 0.42 }, { id: "front-small", label: "Small front", x: 0.5, y: 0.35, w: 0.28 }]),
  beanie: panel("Cuff", 0.23, 0.48, 0.54, 0.22, [{ id: "cuff-center", label: "Cuff center", x: 0.5, y: 0.59, w: 0.34 }, { id: "cuff-small", label: "Small cuff", x: 0.5, y: 0.58, w: 0.24 }]),
  generic: panel("Front panel", 0.14, 0.22, 0.72, 0.58, [{ id: "center-front", label: "Center front", x: 0.5, y: 0.5, w: 0.48 }, { id: "small-front", label: "Small front", x: 0.5, y: 0.43, w: 0.3 }]),
  tumbler: panel("Body wrap", 0.22, 0.14, 0.56, 0.7, [{ id: "tumbler-center", label: "Center wrap", x: 0.5, y: 0.45, w: 0.5 }, { id: "tumbler-small", label: "Small mark", x: 0.5, y: 0.38, w: 0.34 }]),
  sock: panel("Leg", 0.16, 0.06, 0.33, 0.35, [{ id: "sock-leg", label: "Leg mark", x: 0.325, y: 0.26, w: 0.24 }, { id: "sock-cuff", label: "Cuff mark", x: 0.325, y: 0.11, w: 0.2 }]),
  lanyard: panel("Strap face", 0.41, 0.63, 0.18, 0.14, [{ id: "lanyard-center", label: "Center strap", x: 0.5, y: 0.7, w: 0.14 }, { id: "lanyard-small", label: "Small mark", x: 0.5, y: 0.67, w: 0.1 }]),
  badge: panel("Card face", 0.12, 0.2, 0.76, 0.66, [{ id: "badge-center", label: "Card center", x: 0.5, y: 0.52, w: 0.56 }, { id: "badge-top", label: "Top strip", x: 0.5, y: 0.31, w: 0.44 }]),
  pin: panel("Face", 0.23, 0.19, 0.48, 0.4, [{ id: "pin-center", label: "Center", x: 0.47, y: 0.39, w: 0.32 }, { id: "pin-small", label: "Small mark", x: 0.47, y: 0.36, w: 0.22 }]),
  bandana: panel("Front panel", 0.18, 0.28, 0.64, 0.42, [{ id: "bandana-center", label: "Center", x: 0.5, y: 0.52, w: 0.42 }, { id: "bandana-small", label: "Small mark", x: 0.5, y: 0.44, w: 0.28 }]),
  vinyl: panel("Label", 0.32, 0.32, 0.36, 0.36, [{ id: "vinyl-label", label: "Label center", x: 0.5, y: 0.5, w: 0.28 }, { id: "vinyl-small", label: "Small mark", x: 0.5, y: 0.5, w: 0.18 }]),
  mousepad: panel("Pad face", 0.06, 0.08, 0.88, 0.84, [{ id: "mousepad-center", label: "Center", x: 0.5, y: 0.5, w: 0.62 }, { id: "mousepad-small", label: "Small mark", x: 0.5, y: 0.45, w: 0.36 }]),
  luggage: panel("Tag face", 0.19, 0.33, 0.54, 0.46, [{ id: "tag-center", label: "Center", x: 0.5, y: 0.52, w: 0.44 }, { id: "tag-small", label: "Small mark", x: 0.5, y: 0.46, w: 0.3 }])
};
function accessoryPanelFor(category, name) {
  const n = name.toLowerCase();
  if (category === "Headwear") return /beanie|cuff|knit/.test(n) ? ACCESSORY_PANELS.beanie : ACCESSORY_PANELS.cap;
  if (category !== "Accessories") return null;
  if (/tumbler/.test(n)) return ACCESSORY_PANELS.tumbler;
  if (/water bottle|bottle|drinkware/.test(n)) return ACCESSORY_PANELS.bottle;
  if (/backpack/.test(n)) return ACCESSORY_PANELS.backpack;
  if (/fanny|waist bag|belt bag/.test(n)) return ACCESSORY_PANELS.fanny;
  if (/apron/.test(n)) return ACCESSORY_PANELS.apron;
  if (/tote|\bbag\b/.test(n)) return ACCESSORY_PANELS.tote;
  if (/blanket|throw/.test(n)) return ACCESSORY_PANELS.blanket;
  if (/keycap|key cap/.test(n)) return ACCESSORY_PANELS.keycap;
  if (/sock/.test(n)) return ACCESSORY_PANELS.sock;
  if (/lanyard/.test(n)) return ACCESSORY_PANELS.lanyard;
  if (/badge/.test(n)) return ACCESSORY_PANELS.badge;
  if (/pin/.test(n)) return ACCESSORY_PANELS.pin;
  if (/bandana/.test(n)) return ACCESSORY_PANELS.bandana;
  if (/vinyl|record/.test(n)) return ACCESSORY_PANELS.vinyl;
  if (/mousepad|mouse pad/.test(n)) return ACCESSORY_PANELS.mousepad;
  if (/luggage/.test(n)) return ACCESSORY_PANELS.luggage;
  return ACCESSORY_PANELS.generic;
}
var mirrorSlot = (z) => ({ ...z, hand: "viewer-right", u: 1 - z.u - z.uw, spots: z.spots.map((s) => ({ ...s, u: 1 - s.u })) });
var BODY_FRONT_SHORT = { slot: "body", kind: "body", u: 0.235, v: 0.15, uw: 0.53, vh: 0.59, spots: [
  { id: "left-chest", label: "Left chest", u: 0.618, v: 0.304, uw: 0.128 },
  { id: "center-chest", label: "Center chest", u: 0.5, v: 0.381, uw: 0.317 },
  { id: "right-chest", label: "Right chest", u: 0.382, v: 0.304, uw: 0.128 },
  { id: "full-front", label: "Full front", u: 0.5, v: 0.452, uw: 0.463 }
] };
var BODY_BACK_SHORT = { slot: "body", kind: "body", u: 0.235, v: 0.11, uw: 0.53, vh: 0.65, spots: [
  { id: "full-back", label: "Full back", u: 0.5, v: 0.44, uw: 0.5 },
  { id: "center-back", label: "Center back", u: 0.5, v: 0.46, uw: 0.42 },
  { id: "upper-back", label: "Upper back", u: 0.5, v: 0.27, uw: 0.34 },
  { id: "back-neck", label: "Back neck", u: 0.5, v: 0.15, uw: 0.12 }
] };
var SLEEVE_LONG = { slot: "sleeve", kind: "sleeve", hand: "viewer-left", u: 0.105, v: 0.665, uw: 0.06, vh: 0.09, spots: [{ id: "sleeve", label: "Sleeve", u: 0.135, v: 0.71, uw: 0.05 }] };
var BODY_FRONT_LONG = { slot: "body", kind: "body", u: 0.255, v: 0.2, uw: 0.49, vh: 0.475, spots: [
  { id: "left-chest", label: "Left chest", u: 0.625, v: 0.3, uw: 0.13 },
  { id: "center-chest", label: "Center chest", u: 0.5, v: 0.35, uw: 0.3 },
  { id: "right-chest", label: "Right chest", u: 0.375, v: 0.3, uw: 0.13 },
  { id: "full-front", label: "Full front", u: 0.5, v: 0.44, uw: 0.44 }
] };
var BODY_BACK_LONG = { slot: "body", kind: "body", u: 0.25, v: 0.2, uw: 0.5, vh: 0.58, spots: [
  { id: "full-back", label: "Full back", u: 0.5, v: 0.48, uw: 0.46 },
  { id: "center-back", label: "Center back", u: 0.5, v: 0.5, uw: 0.36 },
  { id: "upper-back", label: "Upper back", u: 0.5, v: 0.31, uw: 0.32 },
  { id: "back-neck", label: "Back neck", u: 0.5, v: 0.25, uw: 0.11 }
] };
var BODY_BACK_CROP = { ...BODY_BACK_LONG, vh: 0.52, spots: BODY_BACK_LONG.spots.map((s) => s.id === "full-back" ? { ...s, v: 0.45, uw: 0.44 } : s.id === "center-back" ? { ...s, v: 0.46 } : s) };
var LEG_LONG = { slot: "leg", kind: "leg", hand: "viewer-left", u: 0.16, v: 0.18, uw: 0.26, vh: 0.34, spots: [{ id: "thigh", label: "Thigh", u: 0.28, v: 0.4, uw: 0.15 }, { id: "hip", label: "Hip", u: 0.26, v: 0.26, uw: 0.12 }] };
var LEG_SHORT = { slot: "leg", kind: "leg", hand: "viewer-left", u: 0.13, v: 0.13, uw: 0.3, vh: 0.68, spots: [{ id: "thigh", label: "Thigh", u: 0.27, v: 0.56, uw: 0.17 }, { id: "hip", label: "Hip", u: 0.24, v: 0.22, uw: 0.12 }] };
var topLayout = (sleeve, front, back) => ({ front: [front, sleeve, mirrorSlot(sleeve)], back: [back, sleeve, mirrorSlot(sleeve)] });
var LAYOUTS = {
  "top-short-sleeve": topLayout({ slot: "sleeve", kind: "sleeve", hand: "viewer-left", u: 0.095, v: 0.27, uw: 0.085, vh: 0.07, spots: [{ id: "sleeve", label: "Sleeve", u: 0.1375, v: 0.305, uw: 0.06 }] }, BODY_FRONT_SHORT, BODY_BACK_SHORT),
  "top-drop-sleeve": topLayout({ slot: "sleeve", kind: "sleeve", hand: "viewer-left", u: 0.1, v: 0.31, uw: 0.07, vh: 0.08, spots: [{ id: "sleeve", label: "Sleeve", u: 0.135, v: 0.35, uw: 0.05 }] }, BODY_FRONT_SHORT, BODY_BACK_SHORT),
  "top-long-sleeve": topLayout(SLEEVE_LONG, BODY_FRONT_LONG, BODY_BACK_LONG),
  "top-long-sleeve-crop": topLayout(SLEEVE_LONG, BODY_FRONT_LONG, BODY_BACK_CROP),
  "bottoms-long": { front: [LEG_LONG, mirrorSlot(LEG_LONG)], back: [LEG_LONG, mirrorSlot(LEG_LONG)] },
  "bottoms-short": { front: [LEG_SHORT, mirrorSlot(LEG_SHORT)], back: [LEG_SHORT, mirrorSlot(LEG_SHORT)] }
};
function layoutKeyFor(category, name, fit) {
  const n = name.toLowerCase();
  if (category === "Bottoms") return /short/.test(n) ? "bottoms-short" : "bottoms-long";
  if (category === "Fleece" || category === "Outerwear") return /crop/.test(n) ? "top-long-sleeve-crop" : "top-long-sleeve";
  if (category === "Tees") return /long[ -]?sleeve/.test(n) ? "top-long-sleeve" : /drop[ -]?shoulder/.test(n) || fit === "Oversized" ? "top-drop-sleeve" : "top-short-sleeve";
  return null;
}
function stageBoxFromFrame(frame) {
  if (!frame) return null;
  const inner = 1 - 2 * STAGE_PAD_FRAC, ar = frame.ar > 0 ? frame.ar : 1;
  const iw = ar >= 1 ? inner : inner * ar, ih = ar >= 1 ? inner / ar : inner;
  const ox = STAGE_PAD_FRAC + (inner - iw) / 2, oy = STAGE_PAD_FRAC + (inner - ih) / 2;
  return { x: ox + (frame.cx - frame.w / 2) * iw, y: oy + (frame.cy - frame.h / 2) * ih, w: frame.w * iw, h: frame.h * ih };
}
function fallbackLayout(category, side) {
  return {
    areas: [{ id: side, label: side === "back" ? "Back" : "Front", kind: "body", rect: printZoneForSide(category, side) }],
    spots: placementPresetsForSide(category, side).map((s) => ({ ...s, areaId: side }))
  };
}
function garmentPrintLayout({ category, name, fit, side, frame, mirrored = false }) {
  const key = layoutKeyFor(category, name, fit);
  const box = stageBoxFromFrame(frame);
  if (!box) return fallbackLayout(category, side);
  if (!key) {
    const p = accessoryPanelFor(category, name);
    if (!p) return fallbackLayout(category, side);
    const rect = { x: box.x + p.x * box.w, y: box.y + p.y * box.h, w: p.w * box.w, h: p.h * box.h };
    return {
      areas: [{ id: side, label: `${side === "back" ? "Back" : "Front"} ${p.label.toLowerCase()}`, kind: "body", rect }],
      spots: p.spots.map((s) => ({ id: side === "back" ? `${s.id}-back` : s.id, label: side === "back" ? `${s.label} \xB7 back` : s.label, areaId: side, cx: box.x + s.x * box.w, cy: box.y + s.y * box.h, width: s.w * box.w }))
    };
  }
  const areas = [], spots = [];
  for (const zone of LAYOUTS[key][side]) {
    const u = mirrored ? 1 - zone.u - zone.uw : zone.u;
    let area;
    if (zone.slot === "body") area = { id: side, label: side === "back" ? "Back" : "Front" };
    else {
      const hand = mirrored ? zone.hand === "viewer-left" ? "viewer-right" : "viewer-left" : zone.hand ?? "viewer-left";
      const isLeft = side === "back" ? hand === "viewer-left" : hand === "viewer-right";
      const kind = zone.slot === "sleeve" ? "sleeve" : "leg";
      area = { id: `${kind}-${isLeft ? "left" : "right"}`, label: `${isLeft ? "Left" : "Right"} ${kind}` };
    }
    areas.push({ id: area.id, label: area.label, kind: zone.kind, rect: { x: box.x + u * box.w, y: box.y + zone.v * box.h, w: zone.uw * box.w, h: zone.vh * box.h } });
    for (const s of zone.spots) {
      const su = mirrored ? 1 - s.u : s.u;
      spots.push({ id: zone.slot === "body" ? s.id : `${area.id}-${s.id}`, label: zone.slot === "body" ? s.label : area.label, areaId: area.id, cx: box.x + su * box.w, cy: box.y + s.v * box.h, width: s.uw * box.w });
    }
  }
  return { areas, spots };
}
var inside = (r, x, y) => x >= r.x && x <= r.x + r.w && y >= r.y && y <= r.y + r.h;
var dist2 = (r, x, y) => {
  const dx = Math.max(r.x - x, 0, x - (r.x + r.w)), dy = Math.max(r.y - y, 0, y - (r.y + r.h));
  return dx * dx + dy * dy;
};
function resolvePlacementArea(areas, p) {
  if (!areas.length) return null;
  const current = areas.find((a) => a.id === p.area) ?? null;
  if (current && inside(current.rect, p.cx, p.cy)) return current;
  const other = areas.find((a) => a.id !== current?.id && inside(a.rect, p.cx, p.cy));
  if (other) return other;
  if (current) return current;
  const pool = areas.some((a) => a.kind === "body") ? areas.filter((a) => a.kind === "body") : areas;
  return pool.reduce((best, a) => dist2(a.rect, p.cx, p.cy) < dist2(best.rect, p.cx, p.cy) ? a : best);
}
function constrainPlacementToAreas(p, areas, aspect) {
  const area = resolvePlacementArea(areas, p);
  if (!area) return p;
  const c = constrainPlacementToZone(p, area.rect, aspect);
  return c.area === area.id ? c : { ...c, area: area.id };
}
function defaultPlacementForLayout(layout) {
  const spot = layout.spots[0], area = layout.areas[0];
  if (!spot) {
    const r = area?.rect;
    return { cx: r ? r.x + r.w / 2 : 0.5, cy: r ? r.y + r.h / 2 : 0.45, width: r ? 0.7 * Math.min(r.w, r.h) : 0.26, rotation: 0, flipX: false, area: area?.id };
  }
  return { cx: spot.cx, cy: spot.cy, width: spot.width, rotation: 0, flipX: false, area: spot.areaId };
}
function placementAreaLabel(layout, p) {
  return resolvePlacementArea(layout.areas, p)?.label ?? null;
}
function remapPlacementBetweenLayouts(p, from, to, aspect) {
  const src = resolvePlacementArea(from.areas, p);
  const dst = (src ? to.areas.find((a) => a.id === src.id) : null) ?? (src ? to.areas.find((a) => a.kind === src.kind) : null) ?? to.areas.find((a) => a.kind === "body") ?? to.areas[0] ?? null;
  if (!src || !dst || src.rect.w <= 0 || src.rect.h <= 0) return constrainPlacementToAreas(defaultPlacementForLayout(to), to.areas, aspect);
  const fx = clamp((p.cx - src.rect.x) / src.rect.w, 0, 1), fy = clamp((p.cy - src.rect.y) / src.rect.h, 0, 1), fw = p.width / src.rect.w;
  return constrainPlacementToAreas({ ...p, cx: dst.rect.x + fx * dst.rect.w, cy: dst.rect.y + fy * dst.rect.h, width: fw * dst.rect.w, area: dst.id }, to.areas, aspect);
}
function sanitizePlacement(raw) {
  if (!raw || typeof raw !== "object") return null;
  if (typeof raw.cx !== "number" || typeof raw.cy !== "number" || typeof raw.width !== "number" || typeof raw.rotation !== "number") return null;
  return { cx: clamp(raw.cx, 0.05, 0.95), cy: clamp(raw.cy, 0.05, 0.95), width: clamp(raw.width, 0.04, 0.92), rotation: clamp(Math.round(raw.rotation), -180, 180), flipX: raw.flipX === true, area: typeof raw.area === "string" ? raw.area : void 0 };
}

// piobox-customizer/core/artwork.js
var RASTER_EXTS = [".png", ".jpg", ".jpeg", ".webp", ".svg", ".gif"];
var CONVERTIBLE_EXTS = [".pdf", ".ai"];
var ACCEPT_ATTR = [...RASTER_EXTS, ...CONVERTIBLE_EXTS].join(",");
var MAX_ARTWORK_BYTES = 25 * 1024 * 1024;
var PDFJS_DEFAULT = "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs";
var PDFJS_WORKER_DEFAULT = "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs";
var extOf = (name) => {
  const i = name.lastIndexOf(".");
  return i >= 0 ? name.slice(i).toLowerCase() : "";
};
var isConvertibleArtwork = (name) => CONVERTIBLE_EXTS.includes(extOf(name));
var isAcceptedArtwork = (name) => RASTER_EXTS.includes(extOf(name)) || isConvertibleArtwork(name);
var PrintFileError = class extends Error {
};
function loadImage(src) {
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => resolve(img);
    img.onerror = () => reject(new Error(`Failed to load ${src}`));
    img.src = src;
  });
}
async function normalizeSvg(file) {
  let text;
  try {
    text = await file.text();
  } catch {
    return file;
  }
  const hasSize = /<svg[^>]*\swidth\s*=/.test(text) && /<svg[^>]*\sheight\s*=/.test(text);
  if (hasSize) return file;
  const vb = text.match(/viewBox\s*=\s*["']\s*([\d.\-]+)[\s,]+([\d.\-]+)[\s,]+([\d.\-]+)[\s,]+([\d.\-]+)/i);
  if (!vb) return file;
  const w = Number(vb[3]), h2 = Number(vb[4]);
  if (!(w > 0 && h2 > 0)) return file;
  const scale = 1024 / Math.max(w, h2);
  const patched = text.replace(/<svg\b/i, `<svg width="${Math.round(w * scale)}" height="${Math.round(h2 * scale)}"`);
  return new File([patched], file.name, { type: "image/svg+xml" });
}
async function probeArtworkFile(file) {
  const isVector = /\.svg$/i.test(file.name);
  if (isVector) file = await normalizeSvg(file);
  const url = URL.createObjectURL(file);
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => resolve({ file, url, naturalW: img.naturalWidth || 1024, naturalH: img.naturalHeight || 1024, isVector });
    img.onerror = () => {
      URL.revokeObjectURL(url);
      reject(new Error(`Failed to decode ${file.name}`));
    };
    img.src = url;
  });
}
var pdfjsPromise = null;
function loadPdfJs(opts) {
  return pdfjsPromise ??= (async () => {
    const mod = await import(
      /* @vite-ignore */
      opts?.pdfjsUrl ?? PDFJS_DEFAULT
    );
    const lib = mod.default ?? mod;
    lib.GlobalWorkerOptions.workerSrc = opts?.pdfjsWorkerUrl ?? PDFJS_WORKER_DEFAULT;
    return lib;
  })();
}
function inkBounds(ctx, w, h2) {
  const d = ctx.getImageData(0, 0, w, h2).data;
  const scan = (isBlank) => {
    let l = w, t = h2, r = -1, b = -1;
    for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) if (!isBlank((y * w + x) * 4)) {
      if (x < l) l = x;
      if (x > r) r = x;
      if (y < t) t = y;
      if (y > b) b = y;
    }
    return r < 0 ? null : { x: l, y: t, w: r - l + 1, h: b - t + 1 };
  };
  const alphaBox = scan((i) => d[i + 3] <= 8);
  if (!alphaBox) return null;
  if (alphaBox.w >= 0.98 * w && alphaBox.h >= 0.98 * h2) return scan((i) => d[i + 3] <= 8 || d[i] >= 250 && d[i + 1] >= 250 && d[i + 2] >= 250) ?? alphaBox;
  return alphaBox;
}
async function renderPage(page, scale, crop) {
  const viewport = crop ? page.getViewport({ scale, offsetX: -crop.x, offsetY: -crop.y }) : page.getViewport({ scale });
  const canvas = document.createElement("canvas");
  canvas.width = Math.max(1, Math.ceil(crop ? crop.w : viewport.width));
  canvas.height = Math.max(1, Math.ceil(crop ? crop.h : viewport.height));
  const ctx = canvas.getContext("2d");
  if (!ctx) throw new Error("Canvas 2D unavailable");
  await page.render({ canvasContext: ctx, canvas, viewport, background: "rgba(0,0,0,0)" }).promise;
  return canvas;
}
async function convertPrintFileToPng(file, opts) {
  let lib;
  try {
    lib = await loadPdfJs(opts);
  } catch {
    throw new PrintFileError("PDF/AI conversion needs an internet connection to load the converter. Export a PNG or SVG instead.");
  }
  const data = new Uint8Array(await file.arrayBuffer());
  const task = lib.getDocument({ data });
  let doc;
  try {
    doc = await task.promise;
  } catch {
    throw new PrintFileError(/\.ai$/i.test(file.name) ? "We couldn't open that .ai file. Save it with PDF compatibility on (Illustrator's default), or export a PDF, SVG, or PNG." : "We couldn't open that PDF. Re-export it and try again, or send a PNG or SVG.");
  }
  try {
    const page = await doc.getPage(1);
    const base = page.getViewport({ scale: 1 });
    const probe = await renderPage(page, 640 / Math.max(base.width, base.height));
    const box = inkBounds(probe.getContext("2d"), probe.width, probe.height);
    if (!box) throw new PrintFileError("That file looks blank. Check the export and try again.");
    const probeScale = probe.width / base.width;
    const contentPts = Math.max(box.w, box.h) / probeScale;
    const scale = Math.min(2400 / contentPts, 32), k = scale / probeScale;
    const margin = Math.ceil(Math.max(box.w, box.h) * k * 0.05) + 8;
    const cx = Math.max(0, box.x * k - margin), cy = Math.max(0, box.y * k - margin);
    const crop = { x: cx, y: cy, w: Math.min(base.width * scale - cx, box.w * k + 2 * margin), h: Math.min(base.height * scale - cy, box.h * k + 2 * margin) };
    const hi = await renderPage(page, scale, crop);
    const tight = inkBounds(hi.getContext("2d"), hi.width, hi.height) ?? { x: 0, y: 0, w: hi.width, h: hi.height };
    const pad = Math.ceil(0.02 * Math.max(tight.w, tight.h)) + 2;
    const x = Math.max(0, tight.x - pad), y = Math.max(0, tight.y - pad);
    const w = Math.min(hi.width - x, tight.w + 2 * pad), h2 = Math.min(hi.height - y, tight.h + 2 * pad);
    const out = document.createElement("canvas");
    out.width = w;
    out.height = h2;
    out.getContext("2d").drawImage(hi, x, y, w, h2, 0, 0, w, h2);
    const blob = await new Promise((res) => out.toBlob(res, "image/png"));
    if (!blob) throw new PrintFileError("We couldn't convert that file. Try a PNG or SVG export.");
    return new File([blob], `${file.name}.png`, { type: "image/png" });
  } catch (e) {
    if (e instanceof PrintFileError) throw e;
    throw new PrintFileError("We couldn't turn that file into a preview. Please upload a PNG or SVG version.");
  } finally {
    doc.destroy();
  }
}
async function probeAnyArtworkFile(file, opts) {
  return probeArtworkFile(isConvertibleArtwork(file.name) ? await convertPrintFileToPng(file, opts) : file);
}

// piobox-customizer/ui/icons.js
var wrap = (paths) => `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">${paths}</svg>`;
var I = {
  upload: wrap('<path d="M12 13v8"/><path d="M4 14.899A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 2.5 8.242"/><path d="m8 17 4-4 4 4"/>'),
  type: wrap('<path d="M12 4v16"/><path d="M4 7V5a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1v2"/><path d="M9 20h6"/>'),
  imagePlus: wrap('<path d="M16 5h6"/><path d="M19 2v6"/><path d="M21 11.5V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h7.5"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/><circle cx="9" cy="9" r="2"/>'),
  copy: wrap('<rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/>'),
  flip: wrap('<path d="m3 7 5 5-5 5V7"/><path d="m21 7-5 5 5 5V7"/><path d="M12 20v2"/><path d="M12 14v2"/><path d="M12 8v2"/><path d="M12 2v2"/>'),
  reset: wrap('<path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/><path d="M3 3v5h5"/>'),
  trash: wrap('<path d="M10 11v6"/><path d="M14 11v6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/><path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>'),
  sendBack: wrap('<rect x="14" y="14" width="8" height="8" rx="2"/><rect x="2" y="2" width="8" height="8" rx="2"/><path d="M7 14v1a2 2 0 0 0 2 2h1"/><path d="M14 7h1a2 2 0 0 1 2 2v1"/>'),
  bringFront: wrap('<rect x="8" y="8" width="8" height="8" rx="2"/><path d="M4 10a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2"/><path d="M14 20a2 2 0 0 0 2 2h4a2 2 0 0 0 2-2v-4a2 2 0 0 0-2-2"/>'),
  search: wrap('<path d="m21 21-4.34-4.34"/><circle cx="11" cy="11" r="8"/>'),
  x: wrap('<path d="M18 6 6 18"/><path d="m6 6 12 12"/>'),
  plus: wrap('<path d="M5 12h14"/><path d="M12 5v14"/>'),
  check: wrap('<path d="M20 6 9 17l-5-5"/>'),
  chevronDown: wrap('<path d="m6 9 6 6 6-6"/>'),
  chevronLeft: wrap('<path d="m15 18-6-6 6-6"/>'),
  arrowRight: wrap('<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>'),
  eye: wrap('<path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0"/><circle cx="12" cy="12" r="3"/>'),
  link: wrap('<path d="M9 17H7A5 5 0 0 1 7 7h2"/><path d="M15 7h2a5 5 0 1 1 0 10h-2"/><line x1="8" x2="16" y1="12" y2="12"/>'),
  download: wrap('<path d="M12 15V3"/><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="m7 10 5 5 5-5"/>'),
  pipette: wrap('<path d="m12 9-8.414 8.414A2 2 0 0 0 3 18.828v1.344a2 2 0 0 1-.586 1.414A2 2 0 0 1 3.828 21h1.344a2 2 0 0 0 1.414-.586L15 12"/><path d="m18 9 .4.4a1 1 0 1 1-3 3l-3.8-3.8a1 1 0 1 1 3-3l.4.4 3.4-3.4a1 1 0 1 1 3 3z"/><path d="m2 22 .414-.414"/>'),
  loader: wrap('<path d="M21 12a9 9 0 1 1-6.219-8.56"/>'),
  font: wrap('<path d="M4 20h16"/><path d="m6 16 6-12 6 12"/><path d="M8 12h8"/>'),
  layers: wrap('<path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65"/><path d="m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65"/>'),
  palette: wrap('<circle cx="13.5" cy="6.5" r=".5" fill="currentColor"/><circle cx="17.5" cy="10.5" r=".5" fill="currentColor"/><circle cx="8.5" cy="7.5" r=".5" fill="currentColor"/><circle cx="6.5" cy="12.5" r=".5" fill="currentColor"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 0 1 1.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.554C21.965 6.012 17.461 2 12 2z"/>'),
  cart: wrap('<circle cx="8" cy="21" r="1"/><circle cx="19" cy="21" r="1"/><path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"/>'),
  info: wrap('<circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/>')
};

// piobox-customizer/ui/stage.js
var clamp2 = (v, lo, hi) => Math.min(hi, Math.max(lo, v));
var HANDLES = [
  { key: "tl", cls: "pc-handle--tl", cursor: "nwse-resize" },
  { key: "tr", cls: "pc-handle--tr", cursor: "nesw-resize" },
  { key: "bl", cls: "pc-handle--bl", cursor: "nesw-resize" },
  { key: "br", cls: "pc-handle--br", cursor: "nwse-resize" }
];
var measureCanvas = null;
function createStage(cb) {
  const root = document.createElement("div");
  root.className = "pc-stage-wrap";
  const stage = document.createElement("div");
  stage.className = "pc-stage";
  stage.setAttribute("data-keep", "");
  const photo = document.createElement("img");
  photo.className = "pc-stage__photo";
  photo.draggable = false;
  photo.style.padding = `${100 * STAGE_PAD_FRAC}%`;
  const areasHost = document.createElement("div");
  areasHost.className = "pc-stage__areas";
  const guideV = document.createElement("div");
  guideV.className = "pc-guide pc-guide--v";
  guideV.hidden = true;
  const guideH = document.createElement("div");
  guideH.className = "pc-guide pc-guide--h";
  guideH.hidden = true;
  const layersHost = document.createElement("div");
  layersHost.className = "pc-stage__layers";
  const empty = document.createElement("div");
  empty.className = "pc-stage__empty";
  const status = document.createElement("div");
  status.className = "pc-stage__status";
  status.hidden = true;
  status.innerHTML = `<span class="pc-pill pc-pill--status">${I.loader}<span>Preparing your file\u2026</span></span>`;
  const input = document.createElement("input");
  input.type = "file";
  input.multiple = true;
  input.accept = ACCEPT_ATTR;
  input.className = "pc-sr-only";
  input.setAttribute("aria-label", "Upload artwork");
  input.onchange = () => {
    cb.onFiles(Array.from(input.files ?? []));
    input.value = "";
  };
  const caption = document.createElement("p");
  caption.className = "pc-stage__caption";
  stage.append(photo, areasHost, guideV, guideH, layersHost, empty, status, input);
  root.append(stage, caption);
  let view = null;
  let areasKey = "";
  const nodes = /* @__PURE__ */ new Map();
  let editingId = null, editScale = null;
  const drag = { current: null, pinch: null, pointers: /* @__PURE__ */ new Map() };
  stage.addEventListener("dragover", (e) => {
    e.preventDefault();
    stage.classList.add("is-dropping");
  });
  stage.addEventListener("dragleave", () => stage.classList.remove("is-dropping"));
  stage.addEventListener("drop", (e) => {
    e.preventDefault();
    stage.classList.remove("is-dropping");
    cb.onFiles(Array.from(e.dataTransfer?.files ?? []));
  });
  stage.addEventListener("pointerdown", (e) => {
    if (e.target === stage || e.target === photo || e.target === areasHost) {
      if (view?.activeLayerId) cb.onDeselect();
    }
  });
  const stageRect = () => stage.getBoundingClientRect();
  const snapY = () => view?.category === "Headwear" ? 0.43 : 0.38;
  function beginGesture(e, mode, layer) {
    if (view?.disabled) return;
    e.preventDefault();
    e.stopPropagation();
    if (layer.id !== view.activeLayerId) cb.onSelect(layer.id);
    const rect = stageRect();
    if (mode === "move") {
      drag.pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
      if (drag.pointers.size === 2) {
        const [a, b] = [...drag.pointers.values()];
        drag.pinch = { layerId: layer.id, d0: Math.max(8, Math.hypot(b.x - a.x, b.y - a.y)), a0: 180 * Math.atan2(b.y - a.y, b.x - a.x) / Math.PI, width0: layer.placement.width, rot0: layer.placement.rotation };
        drag.current = null;
        e.currentTarget.setPointerCapture(e.pointerId);
        stage.classList.add("is-dragging");
        return;
      }
    }
    const cx = rect.left + layer.placement.cx * rect.width, cy = rect.top + layer.placement.cy * rect.height;
    drag.current = { mode, pointerId: e.pointerId, layerId: layer.id, startX: e.clientX, startY: e.clientY, start: layer.placement, rect, cx, cy, startDist: Math.max(8, Math.hypot(e.clientX - cx, e.clientY - cy)), startAngle: 180 * Math.atan2(e.clientY - cy, e.clientX - cx) / Math.PI - layer.placement.rotation };
    e.currentTarget.setPointerCapture(e.pointerId);
    if (mode === "move") e.currentTarget.focus?.({ preventScroll: true });
    stage.classList.add("is-dragging");
  }
  function moveGesture(e) {
    if (drag.pointers.has(e.pointerId)) drag.pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
    const pinch = drag.pinch;
    if (pinch && drag.pointers.size >= 2) {
      e.preventDefault();
      const [a, b] = [...drag.pointers.values()];
      const d2 = Math.max(8, Math.hypot(b.x - a.x, b.y - a.y)), ang = 180 * Math.atan2(b.y - a.y, b.x - a.x) / Math.PI;
      const width = clamp2(pinch.width0 * (d2 / pinch.d0), 0.04, 0.92);
      let rot2 = pinch.rot0 + (ang - pinch.a0);
      rot2 = (rot2 + 540) % 360 - 180;
      if (Math.abs(rot2) < 3) rot2 = 0;
      cb.onPlacementChange(pinch.layerId, (p) => ({ ...p, width, rotation: Math.round(rot2) }));
      return;
    }
    const d = drag.current;
    if (!d || e.pointerId !== d.pointerId) return;
    e.preventDefault();
    if (d.mode === "move") {
      const dx = (e.clientX - d.startX) / d.rect.width, dy = (e.clientY - d.startY) / d.rect.height;
      let cx = clamp2(d.start.cx + dx, 0.05, 0.95), cy = clamp2(d.start.cy + dy, 0.05, 0.95);
      const snapX = Math.abs(cx - 0.5) < 0.012;
      if (snapX) cx = 0.5;
      const sy = snapY(), snapYHit = Math.abs(cy - sy) < 0.012;
      if (snapYHit) cy = sy;
      guideV.hidden = !snapX;
      guideH.hidden = !snapYHit;
      guideH.style.top = `${100 * sy}%`;
      cb.onPlacementChange(d.layerId, (p) => ({ ...p, cx, cy }));
      return;
    }
    if (d.mode === "resize") {
      const dist = Math.hypot(e.clientX - d.cx, e.clientY - d.cy);
      const width = clamp2(d.start.width * (dist / d.startDist), 0.04, 0.92);
      cb.onPlacementChange(d.layerId, (p) => ({ ...p, width }));
      return;
    }
    let rot = 180 * Math.atan2(e.clientY - d.cy, e.clientX - d.cx) / Math.PI - d.startAngle;
    rot = (rot + 540) % 360 - 180;
    for (const snap of [0, 90, -90, 45, -45]) if (Math.abs(rot - snap) < 4) {
      rot = snap;
      break;
    }
    cb.onPlacementChange(d.layerId, (p) => ({ ...p, rotation: Math.round(rot) }));
  }
  function endGesture(e) {
    drag.pointers.delete(e.pointerId);
    if (drag.pinch && drag.pointers.size < 2) {
      drag.pinch = null;
      stage.classList.remove("is-dragging");
    }
    const d = drag.current;
    if (d?.pointerId === e.pointerId) {
      const moved = Math.hypot(e.clientX - d.startX, e.clientY - d.startY);
      const layer = view.layers.find((l) => l.id === d.layerId);
      if (d.mode === "move" && moved <= 5 && layer?.text && editingId !== d.layerId) startTextEdit(d.layerId);
      drag.current = null;
      stage.classList.remove("is-dragging");
      guideV.hidden = true;
      guideH.hidden = true;
    }
  }
  function textMetrics(text, widthFrac) {
    const stageW = stage.getBoundingClientRect().width;
    if (stageW <= 0) return null;
    measureCanvas ??= document.createElement("canvas");
    const ctx = measureCanvas.getContext("2d");
    if (!ctx) return null;
    const lines = (text.content.trim() ? text.content : "Your text").split("\n").map((l) => l === "" ? " " : l);
    ctx.font = `${text.fontWeight} 100px ${text.fontFamily}`;
    const ascentRef = ctx.measureText("Hg").fontBoundingBoxAscent || 80;
    let widest = 1, ascent = 0, descent = 0;
    lines.forEach((l, i) => {
      const m = ctx.measureText(l);
      const w = (m.actualBoundingBoxLeft ?? 0) + (m.actualBoundingBoxRight ?? m.width);
      widest = Math.max(widest, w);
      if (i === 0) ascent = m.actualBoundingBoxAscent ?? ascentRef;
      descent = m.actualBoundingBoxDescent ?? 20;
    });
    const height = ascent + descent + (lines.length - 1) * 118, pad = 0.02 * Math.max(widest, height);
    const boxW = widest + 2 * pad, boxH = height + 2 * pad;
    const layerPx = stageW * widthFrac;
    const fontPxFit = layerPx / boxW * 100;
    if (editScale == null) editScale = fontPxFit / layerPx;
    let fontPx = Math.min(editScale * layerPx, fontPxFit);
    let heightFrac = fontPx / 100 * boxH / stageW;
    if (heightFrac > 0.95) {
      fontPx *= 0.95 / heightFrac;
      heightFrac = 0.95;
    }
    return { fontPx, heightFrac, boxHeightPx: fontPx / 100 * boxH, lineCount: lines.length };
  }
  function startTextEdit(id) {
    if (view?.disabled) return;
    const layer = view.layers.find((l) => l.id === id);
    if (!layer?.text) return;
    if (id !== view.activeLayerId) cb.onSelect(id);
    editingId = id;
    editScale = null;
    render();
    const n = nodes.get(id);
    n?.textarea?.focus({ preventScroll: true });
    n?.textarea?.select();
  }
  function commitTextEdit(id) {
    if (editingId !== id) return;
    editingId = null;
    editScale = null;
    cb.onTextCommit(id);
    render();
  }
  function createLayerNode(layer) {
    const el = document.createElement("div");
    el.className = "pc-layer";
    el.setAttribute("role", "img");
    el.tabIndex = -1;
    const img = document.createElement("img");
    img.className = "pc-layer__img";
    img.draggable = false;
    img.alt = "";
    const hover = document.createElement("div");
    hover.className = "pc-layer__hover";
    const rotLine = document.createElement("div");
    rotLine.className = "pc-layer__rotline";
    const rot = document.createElement("button");
    rot.type = "button";
    rot.className = "pc-handle pc-handle--rotate";
    rot.setAttribute("aria-label", "Rotate artwork");
    const handles = HANDLES.map((hd) => {
      const b = document.createElement("button");
      b.type = "button";
      b.className = `pc-handle ${hd.cls}`;
      b.style.cursor = hd.cursor;
      b.setAttribute("aria-label", "Resize artwork");
      return b;
    });
    el.append(img, hover, rotLine, rot, ...handles);
    const bind = (target, mode) => {
      target.addEventListener("pointerdown", (e) => {
        const l = current(layer.id);
        if (l) beginGesture(e, mode, l);
      });
      target.addEventListener("pointermove", moveGesture);
      target.addEventListener("pointerup", endGesture);
      target.addEventListener("pointercancel", endGesture);
    };
    bind(el, "move");
    bind(rot, "rotate");
    handles.forEach((b) => bind(b, "resize"));
    el.addEventListener("dblclick", (e) => {
      const l = current(layer.id);
      if (!l || view.disabled) return;
      if (l.text) {
        e.preventDefault();
        e.stopPropagation();
        startTextEdit(l.id);
      } else cb.onPlacementChange(l.id, (p) => ({ ...p, cx: 0.5 }));
    });
    el.addEventListener("keydown", (e) => {
      const l = current(layer.id);
      if (!l) return;
      const step = e.shiftKey ? 0.02 : 5e-3;
      const nudge = (dx, dy) => {
        e.preventDefault();
        cb.onPlacementChange(l.id, (p) => ({ ...p, cx: p.cx + dx, cy: p.cy + dy }));
      };
      if (e.key === "ArrowLeft") nudge(-step, 0);
      else if (e.key === "ArrowRight") nudge(step, 0);
      else if (e.key === "ArrowUp") nudge(0, -step);
      else if (e.key === "ArrowDown") nudge(0, step);
      else if (e.key === "Enter" && l.text) {
        e.preventDefault();
        startTextEdit(l.id);
      } else if (e.key === "Backspace" || e.key === "Delete") {
        e.preventDefault();
        cb.onDelete(l.id);
      }
    });
    el.addEventListener("wheel", (e) => {
      const l = current(layer.id);
      if (!l || l.id !== view.activeLayerId) return;
      e.preventDefault();
      const k = Math.exp(-(15e-4 * e.deltaY));
      cb.onPlacementChange(l.id, (p) => ({ ...p, width: clamp2(p.width * k, 0.04, 0.92) }));
    }, { passive: false });
    return { el, img, handles: [rotLine, rot, ...handles] };
  }
  const current = (id) => view?.layers.find((l) => l.id === id) ?? null;
  function renderAreas() {
    const key = JSON.stringify(view.areas ?? []);
    if (key === areasKey) return;
    areasKey = key;
    areasHost.innerHTML = "";
    for (const a of view.areas ?? []) {
      const d = document.createElement("div");
      d.className = "pc-area";
      d.dataset.area = a.id;
      d.style.left = `${100 * a.rect.x}%`;
      d.style.top = `${100 * a.rect.y}%`;
      d.style.width = `${100 * a.rect.w}%`;
      d.style.height = `${100 * a.rect.h}%`;
      if (a.rect.w >= 0.12) {
        const s = document.createElement("span");
        s.className = "pc-area__label";
        s.textContent = a.label;
        d.appendChild(s);
      }
      areasHost.appendChild(d);
    }
  }
  function render() {
    if (!view) return;
    const activeArea = view.layers.find((l) => l.id === view.activeLayerId)?.placement.area ?? view.side;
    areasHost.querySelectorAll(".pc-area").forEach((a) => a.classList.toggle("is-active", a.dataset.area === activeArea));
    stage.classList.toggle("has-layers", view.layers.length > 0);
    const seen = /* @__PURE__ */ new Set();
    view.layers.forEach((layer, index) => {
      seen.add(layer.id);
      let n = nodes.get(layer.id);
      if (!n) {
        n = createLayerNode(layer);
        nodes.set(layer.id, n);
        layersHost.appendChild(n.el);
      }
      if (layersHost.children[index] !== n.el) layersHost.insertBefore(n.el, layersHost.children[index] ?? null);
      const aspect = layer.naturalW > 0 && layer.naturalH > 0 ? layer.naturalH / layer.naturalW : 1;
      const active = layer.id === view.activeLayerId, editing = active && editingId === layer.id && !!layer.text;
      const metrics = editing ? textMetrics(layer.text, layer.placement.width) : null;
      const hFrac = metrics ? metrics.heightFrac : layer.placement.width * aspect;
      const s = n.el.style;
      s.left = `${(layer.placement.cx - layer.placement.width / 2) * 100}%`;
      s.top = `${(layer.placement.cy - hFrac / 2) * 100}%`;
      s.width = `${100 * layer.placement.width}%`;
      s.height = `${100 * hFrac}%`;
      s.transform = `rotate(${layer.placement.rotation}deg)`;
      s.zIndex = active ? 20 : 10;
      n.el.classList.toggle("is-active", active);
      n.el.classList.toggle("is-text", !!layer.text);
      n.el.classList.toggle("is-editing", editing);
      n.el.classList.toggle("is-compact", layer.placement.width < 0.12);
      n.el.tabIndex = active ? 0 : -1;
      n.el.setAttribute("aria-label", layer.text ? active ? "Text: press Enter or double-click to edit; drag to move" : "Text layer, tap to select" : active ? "Your artwork: drag to move, arrow keys to nudge" : "Artwork layer, tap to select");
      n.el.title = layer.text && !editing ? "Double-click to edit" : "";
      if (n.img.getAttribute("src") !== layer.url) n.img.src = layer.url;
      n.img.style.transform = layer.placement.flipX ? "scaleX(-1)" : "";
      n.img.style.opacity = editing ? "0" : "1";
      n.handles.forEach((hd) => hd.hidden = !active || editing);
      if (editing) {
        if (!n.textarea) {
          const ta2 = document.createElement("textarea");
          ta2.className = "pc-layer__edit";
          ta2.rows = 1;
          ta2.wrap = "off";
          ta2.spellcheck = false;
          ta2.placeholder = "Your text";
          ta2.maxLength = view.textMaxChars ?? 120;
          ta2.setAttribute("aria-label", "Edit text, press Escape when done");
          ta2.addEventListener("pointerdown", (e) => e.stopPropagation());
          ta2.addEventListener("pointerup", (e) => e.stopPropagation());
          ta2.addEventListener("dblclick", (e) => e.stopPropagation());
          ta2.addEventListener("input", () => cb.onTextChange(layer.id, ta2.value));
          ta2.addEventListener("keydown", (e) => {
            e.stopPropagation();
            if (e.key === "Escape" || e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
              e.preventDefault();
              commitTextEdit(layer.id);
            }
          });
          ta2.addEventListener("blur", () => commitTextEdit(layer.id));
          n.el.appendChild(ta2);
          n.textarea = ta2;
        }
        const ta = n.textarea;
        if (ta.value !== layer.text.content) ta.value = layer.text.content;
        ta.style.color = layer.text.color;
        ta.style.caretColor = layer.text.color;
        ta.style.fontFamily = layer.text.fontFamily;
        ta.style.fontWeight = String(layer.text.fontWeight);
        ta.style.fontSize = `${metrics ? metrics.fontPx : 24}px`;
        ta.style.lineHeight = metrics ? metrics.lineCount > 1 ? `${1.18 * metrics.fontPx}px` : `${metrics.boxHeightPx}px` : "1.2";
      } else if (n.textarea) {
        const ta = n.textarea;
        n.textarea = null;
        try {
          ta.remove();
        } catch {
        }
      }
    });
    for (const [id, n] of nodes) if (!seen.has(id)) {
      nodes.delete(id);
      if (editingId === id) {
        editingId = null;
        editScale = null;
      }
      try {
        n.el.remove();
      } catch {
      }
    }
    if (view.layers.length === 0) {
      empty.hidden = false;
      empty.innerHTML = "";
      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = "pc-stage__drop";
      btn.innerHTML = `${I.upload}<span class="pc-stage__drop-title"></span><span class="pc-stage__drop-hint"></span>`;
      btn.querySelector(".pc-stage__drop-title").textContent = view.empty?.title ?? "Drop your art here";
      btn.querySelector(".pc-stage__drop-hint").textContent = view.empty?.hint ?? "PNG, JPG, SVG, PDF, or AI \xB7 select one or several files \xB7 transparent art looks best";
      btn.onclick = () => openFilePicker({ multiple: true });
      empty.appendChild(btn);
    } else empty.hidden = true;
    caption.textContent = view.caption ?? "";
    caption.hidden = !view.caption;
    status.hidden = !view.preparing;
  }
  function update(next) {
    view = next;
    if (photo.getAttribute("src") !== view.mockupSrc) photo.src = view.mockupSrc;
    photo.alt = view.mockupAlt ?? "";
    photo.classList.toggle("is-mirrored", !!view.mirrored);
    stage.classList.toggle("is-disabled", !!view.disabled);
    renderAreas();
    render();
  }
  function openFilePicker({ multiple = true } = {}) {
    input.multiple = multiple;
    input.click();
  }
  return { el: root, update, openFilePicker, startTextEdit, isEditing: () => editingId };
}

// piobox-customizer/data/pantone.js
var PANTONE_LIBRARY = [
  {
    "code": "18-1664 TCX",
    "name": "True Red",
    "hex": "#bf1932",
    "family": "Red"
  },
  {
    "code": "19-1664 TCX",
    "name": "Racing Red",
    "hex": "#bd2333",
    "family": "Red"
  },
  {
    "code": "18-1763 TCX",
    "name": "Rococco Red",
    "hex": "#cb333b",
    "family": "Red"
  },
  {
    "code": "19-1557 TCX",
    "name": "Chili Pepper",
    "hex": "#9b1b30",
    "family": "Red",
    "aliases": [
      "burgundy",
      "maroon",
      "ruby",
      "oxblood"
    ]
  },
  {
    "code": "18-1550 TCX",
    "name": "Aurora Red",
    "hex": "#b33d4c",
    "family": "Red"
  },
  {
    "code": "19-1762 TCX",
    "name": "Crimson",
    "hex": "#a4133c",
    "family": "Red",
    "aliases": [
      "cardinal"
    ]
  },
  {
    "code": "18-1561 TCX",
    "name": "Flame Scarlet",
    "hex": "#cd212a",
    "family": "Red"
  },
  {
    "code": "19-1761 TCX",
    "name": "Ribbon Red",
    "hex": "#b5223b",
    "family": "Red"
  },
  {
    "code": "18-1660 TCX",
    "name": "Tomato",
    "hex": "#c63b46",
    "family": "Red"
  },
  {
    "code": "PANTONE 186 C",
    "name": "Pantone 186",
    "hex": "#c8102e",
    "family": "Red"
  },
  {
    "code": "PANTONE 200 C",
    "name": "Pantone 200",
    "hex": "#ba0c2f",
    "family": "Red"
  },
  {
    "code": "19-1559 TCX",
    "name": "Molten Lava",
    "hex": "#b3322c",
    "family": "Red",
    "aliases": [
      "brick",
      "brick red"
    ]
  },
  {
    "code": "18-2043 TCX",
    "name": "Pink Peacock",
    "hex": "#c62168",
    "family": "Pink"
  },
  {
    "code": "17-2031 TCX",
    "name": "Pink Carnation",
    "hex": "#eb6fa6",
    "family": "Pink"
  },
  {
    "code": "16-1546 TCX",
    "name": "Living Coral",
    "hex": "#fc766f",
    "family": "Pink",
    "aliases": [
      "salmon"
    ]
  },
  {
    "code": "13-1520 TCX",
    "name": "Rose Quartz",
    "hex": "#f7c4c4",
    "family": "Pink",
    "aliases": [
      "blush",
      "baby pink"
    ]
  },
  {
    "code": "17-1937 TCX",
    "name": "Hot Pink",
    "hex": "#e55982",
    "family": "Pink"
  },
  {
    "code": "18-2436 TCX",
    "name": "Fuchsia Red",
    "hex": "#ab3585",
    "family": "Pink"
  },
  {
    "code": "15-1922 TCX",
    "name": "Flamingo Pink",
    "hex": "#f6a3b6",
    "family": "Pink"
  },
  {
    "code": "14-1714 TCX",
    "name": "Candy Pink",
    "hex": "#f4b9c2",
    "family": "Pink"
  },
  {
    "code": "18-1756 TCX",
    "name": "Raspberry",
    "hex": "#cd3163",
    "family": "Pink"
  },
  {
    "code": "PANTONE 213 C",
    "name": "Pantone 213",
    "hex": "#e10098",
    "family": "Pink",
    "aliases": [
      "magenta"
    ]
  },
  {
    "code": "17-1740 TCX",
    "name": "Bubblegum",
    "hex": "#e96a9b",
    "family": "Pink"
  },
  {
    "code": "12-1106 TCX",
    "name": "Sea Salt",
    "hex": "#f1e6dc",
    "family": "Pink"
  },
  {
    "code": "16-1364 TCX",
    "name": "Orange Tiger",
    "hex": "#f96714",
    "family": "Orange",
    "aliases": [
      "pumpkin"
    ]
  },
  {
    "code": "15-1247 TCX",
    "name": "Tangerine",
    "hex": "#f88f58",
    "family": "Orange"
  },
  {
    "code": "16-1448 TCX",
    "name": "Burnt Orange",
    "hex": "#c15a2c",
    "family": "Orange",
    "aliases": [
      "rust",
      "terracotta"
    ]
  },
  {
    "code": "17-1463 TCX",
    "name": "Flame",
    "hex": "#f2552c",
    "family": "Orange"
  },
  {
    "code": "15-1335 TCX",
    "name": "Cantaloupe",
    "hex": "#f7a978",
    "family": "Orange",
    "aliases": [
      "peach"
    ]
  },
  {
    "code": "16-1359 TCX",
    "name": "Persimmon",
    "hex": "#e8703a",
    "family": "Orange"
  },
  {
    "code": "PANTONE 1655 C",
    "name": "Pantone 1655",
    "hex": "#fc4c02",
    "family": "Orange"
  },
  {
    "code": "PANTONE 021 C",
    "name": "Pantone Orange 021",
    "hex": "#fe5000",
    "family": "Orange"
  },
  {
    "code": "14-1064 TCX",
    "name": "Saffron",
    "hex": "#f7b449",
    "family": "Orange",
    "aliases": [
      "honey"
    ]
  },
  {
    "code": "16-1257 TCX",
    "name": "Apricot",
    "hex": "#ee9a52",
    "family": "Orange"
  },
  {
    "code": "18-1248 TCX",
    "name": "Adobe",
    "hex": "#9e4624",
    "family": "Orange",
    "aliases": [
      "rust"
    ]
  },
  {
    "code": "13-0647 TCX",
    "name": "Illuminating",
    "hex": "#f5df4d",
    "family": "Yellow"
  },
  {
    "code": "14-0756 TCX",
    "name": "Lemon Zest",
    "hex": "#f4d35e",
    "family": "Yellow",
    "aliases": [
      "lemon"
    ]
  },
  {
    "code": "13-0858 TCX",
    "name": "Vibrant Yellow",
    "hex": "#fdd835",
    "family": "Yellow"
  },
  {
    "code": "14-0848 TCX",
    "name": "Mimosa",
    "hex": "#f0c05a",
    "family": "Yellow"
  },
  {
    "code": "15-0751 TCX",
    "name": "Spicy Mustard",
    "hex": "#d9a521",
    "family": "Yellow"
  },
  {
    "code": "16-0954 TCX",
    "name": "Golden Yellow",
    "hex": "#d6a01d",
    "family": "Yellow"
  },
  {
    "code": "12-0824 TCX",
    "name": "Pale Banana",
    "hex": "#fae199",
    "family": "Yellow",
    "aliases": [
      "butter",
      "butter yellow"
    ]
  },
  {
    "code": "PANTONE 109 C",
    "name": "Pantone 109",
    "hex": "#ffd100",
    "family": "Yellow"
  },
  {
    "code": "PANTONE 7548 C",
    "name": "Pantone 7548",
    "hex": "#ffc600",
    "family": "Yellow",
    "aliases": [
      "amber"
    ]
  },
  {
    "code": "14-0952 TCX",
    "name": "Ochre",
    "hex": "#cb9a3c",
    "family": "Yellow"
  },
  {
    "code": "13-0755 TCX",
    "name": "Aspen Gold",
    "hex": "#f6d155",
    "family": "Yellow"
  },
  {
    "code": "11-0617 TCX",
    "name": "Pastel Yellow",
    "hex": "#f3e7b1",
    "family": "Yellow"
  },
  {
    "code": "15-0343 TCX",
    "name": "Greenery",
    "hex": "#88b04b",
    "family": "Green"
  },
  {
    "code": "18-0228 TCX",
    "name": "Treetop",
    "hex": "#37503d",
    "family": "Green",
    "aliases": [
      "hunter green",
      "hunter"
    ]
  },
  {
    "code": "19-6311 TCX",
    "name": "Forest",
    "hex": "#27483f",
    "family": "Green",
    "aliases": [
      "evergreen"
    ]
  },
  {
    "code": "16-0123 TCX",
    "name": "Foliage",
    "hex": "#5e7045",
    "family": "Green"
  },
  {
    "code": "15-6442 TCX",
    "name": "Kelly Green",
    "hex": "#3c9a47",
    "family": "Green"
  },
  {
    "code": "17-0145 TCX",
    "name": "Vibrant Green",
    "hex": "#4a8c2a",
    "family": "Green"
  },
  {
    "code": "13-0220 TCX",
    "name": "Lime",
    "hex": "#bcd35f",
    "family": "Green"
  },
  {
    "code": "18-0135 TCX",
    "name": "Online Lime",
    "hex": "#5a8f29",
    "family": "Green"
  },
  {
    "code": "16-6340 TCX",
    "name": "Classic Green",
    "hex": "#008c45",
    "family": "Green"
  },
  {
    "code": "PANTONE 348 C",
    "name": "Pantone 348",
    "hex": "#00843d",
    "family": "Green"
  },
  {
    "code": "14-0156 TCX",
    "name": "Green Flash",
    "hex": "#79c143",
    "family": "Green"
  },
  {
    "code": "16-0228 TCX",
    "name": "Sap Green",
    "hex": "#7a9a3b",
    "family": "Green",
    "aliases": [
      "moss",
      "moss green"
    ]
  },
  {
    "code": "12-0225 TCX",
    "name": "Pistachio",
    "hex": "#b7cf9c",
    "family": "Green",
    "aliases": [
      "sage",
      "sage green"
    ]
  },
  {
    "code": "17-0119 TCX",
    "name": "Olive",
    "hex": "#6b703f",
    "family": "Green",
    "aliases": [
      "army green",
      "military green"
    ]
  },
  {
    "code": "15-5519 TCX",
    "name": "Turquoise",
    "hex": "#45b5aa",
    "family": "Teal"
  },
  {
    "code": "17-5025 TCX",
    "name": "Lake Blue",
    "hex": "#159a9c",
    "family": "Teal"
  },
  {
    "code": "18-4936 TCX",
    "name": "Deep Teal",
    "hex": "#15616d",
    "family": "Teal"
  },
  {
    "code": "16-5121 TCX",
    "name": "Spearmint",
    "hex": "#67bea3",
    "family": "Teal"
  },
  {
    "code": "13-5309 TCX",
    "name": "Aqua Glass",
    "hex": "#bfe3da",
    "family": "Teal"
  },
  {
    "code": "17-5126 TCX",
    "name": "Emerald",
    "hex": "#009473",
    "family": "Teal",
    "aliases": [
      "jade"
    ]
  },
  {
    "code": "18-5128 TCX",
    "name": "Viridian",
    "hex": "#107869",
    "family": "Teal",
    "aliases": [
      "pine",
      "pine green"
    ]
  },
  {
    "code": "16-5418 TCX",
    "name": "Biscay Green",
    "hex": "#56c5b6",
    "family": "Teal"
  },
  {
    "code": "PANTONE 3145 C",
    "name": "Pantone 3145",
    "hex": "#00778b",
    "family": "Teal"
  },
  {
    "code": "14-4807 TCX",
    "name": "Aqua Foam",
    "hex": "#9fd9cf",
    "family": "Teal",
    "aliases": [
      "seafoam",
      "sea foam"
    ]
  },
  {
    "code": "19-4052 TCX",
    "name": "Classic Blue",
    "hex": "#0f4c81",
    "family": "Blue"
  },
  {
    "code": "19-3938 TCX",
    "name": "Very Peri",
    "hex": "#6667ab",
    "family": "Blue",
    "aliases": [
      "periwinkle"
    ]
  },
  {
    "code": "19-4150 TCX",
    "name": "Directoire Blue",
    "hex": "#1f4e8c",
    "family": "Blue",
    "aliases": [
      "cobalt",
      "denim"
    ]
  },
  {
    "code": "18-4148 TCX",
    "name": "Brilliant Blue",
    "hex": "#1c75bc",
    "family": "Blue",
    "aliases": [
      "steel blue",
      "cerulean"
    ]
  },
  {
    "code": "16-4132 TCX",
    "name": "Little Boy Blue",
    "hex": "#6ba4d8",
    "family": "Blue"
  },
  {
    "code": "19-4023 TCX",
    "name": "Dress Blues",
    "hex": "#2a3244",
    "family": "Blue",
    "aliases": [
      "navy"
    ]
  },
  {
    "code": "19-4027 TCX",
    "name": "Estate Blue",
    "hex": "#22344a",
    "family": "Blue",
    "aliases": [
      "navy"
    ]
  },
  {
    "code": "14-4318 TCX",
    "name": "Sky Blue",
    "hex": "#8fc0db",
    "family": "Blue",
    "aliases": [
      "baby blue"
    ]
  },
  {
    "code": "18-4045 TCX",
    "name": "Strong Blue",
    "hex": "#1f6fb2",
    "family": "Blue"
  },
  {
    "code": "19-3955 TCX",
    "name": "Royal Blue",
    "hex": "#1e2a78",
    "family": "Blue"
  },
  {
    "code": "PANTONE 286 C",
    "name": "Pantone 286",
    "hex": "#0033a0",
    "family": "Blue",
    "aliases": [
      "cobalt"
    ]
  },
  {
    "code": "PANTONE 2925 C",
    "name": "Pantone 2925",
    "hex": "#009cde",
    "family": "Blue"
  },
  {
    "code": "19-4025 TCX",
    "name": "Navy Peony",
    "hex": "#26344b",
    "family": "Blue"
  },
  {
    "code": "12-4302 TCX",
    "name": "Powder Blue",
    "hex": "#cdd6dd",
    "family": "Blue"
  },
  {
    "code": "18-3838 TCX",
    "name": "Ultra Violet",
    "hex": "#5f4b8b",
    "family": "Purple"
  },
  {
    "code": "18-3224 TCX",
    "name": "Radiant Orchid",
    "hex": "#ad5e99",
    "family": "Purple"
  },
  {
    "code": "19-3536 TCX",
    "name": "Imperial Purple",
    "hex": "#553571",
    "family": "Purple"
  },
  {
    "code": "16-3520 TCX",
    "name": "Lavender",
    "hex": "#b08cc0",
    "family": "Purple",
    "aliases": [
      "mauve"
    ]
  },
  {
    "code": "17-3617 TCX",
    "name": "Amethyst",
    "hex": "#864d9f",
    "family": "Purple"
  },
  {
    "code": "19-3220 TCX",
    "name": "Grape Wine",
    "hex": "#5a2d45",
    "family": "Purple",
    "aliases": [
      "eggplant",
      "aubergine"
    ]
  },
  {
    "code": "18-3633 TCX",
    "name": "Deep Lavender",
    "hex": "#735ba1",
    "family": "Purple"
  },
  {
    "code": "14-3812 TCX",
    "name": "Lavender Frost",
    "hex": "#bdb0d0",
    "family": "Purple",
    "aliases": [
      "lilac"
    ]
  },
  {
    "code": "PANTONE 2685 C",
    "name": "Pantone 2685",
    "hex": "#330072",
    "family": "Purple",
    "aliases": [
      "indigo"
    ]
  },
  {
    "code": "19-2924 TCX",
    "name": "Boysenberry",
    "hex": "#7c3a52",
    "family": "Purple"
  },
  {
    "code": "18-3530 TCX",
    "name": "Bougainvillea",
    "hex": "#894b7d",
    "family": "Purple",
    "aliases": [
      "plum"
    ]
  },
  {
    "code": "12-2903 TCX",
    "name": "Pink Lavender",
    "hex": "#dbbdd2",
    "family": "Purple"
  },
  {
    "code": "19-1314 TCX",
    "name": "Chicory Coffee",
    "hex": "#3e2b23",
    "family": "Brown",
    "aliases": [
      "espresso",
      "coffee"
    ]
  },
  {
    "code": "18-1142 TCX",
    "name": "Bone Brown",
    "hex": "#8a6d51",
    "family": "Brown"
  },
  {
    "code": "17-1052 TCX",
    "name": "Cathay Spice",
    "hex": "#97572b",
    "family": "Brown",
    "aliases": [
      "copper"
    ]
  },
  {
    "code": "16-1432 TCX",
    "name": "Caramel",
    "hex": "#c47e5a",
    "family": "Brown"
  },
  {
    "code": "17-1340 TCX",
    "name": "Autumn Maple",
    "hex": "#c46d3b",
    "family": "Brown"
  },
  {
    "code": "18-1027 TCX",
    "name": "Toasted Coconut",
    "hex": "#806250",
    "family": "Brown"
  },
  {
    "code": "19-1218 TCX",
    "name": "Chocolate",
    "hex": "#5a3f36",
    "family": "Brown"
  },
  {
    "code": "16-1235 TCX",
    "name": "Tan",
    "hex": "#b08664",
    "family": "Brown",
    "aliases": [
      "camel"
    ]
  },
  {
    "code": "17-1230 TCX",
    "name": "Mocha",
    "hex": "#9a7058",
    "family": "Brown"
  },
  {
    "code": "18-0930 TCX",
    "name": "Cocoa Brown",
    "hex": "#6b4d3a",
    "family": "Brown"
  },
  {
    "code": "11-0601 TCX",
    "name": "Bright White",
    "hex": "#f4f4ee",
    "family": "Neutral",
    "aliases": [
      "ivory"
    ]
  },
  {
    "code": "11-4300 TCX",
    "name": "Cloud Dancer",
    "hex": "#f0eee9",
    "family": "Neutral",
    "aliases": [
      "off white",
      "off-white"
    ]
  },
  {
    "code": "12-0000 TCX",
    "name": "Whisper White",
    "hex": "#ede9e1",
    "family": "Neutral",
    "aliases": [
      "cream"
    ]
  },
  {
    "code": "13-0905 TCX",
    "name": "Cement",
    "hex": "#a89a8e",
    "family": "Neutral"
  },
  {
    "code": "14-1118 TCX",
    "name": "Sand",
    "hex": "#cbb393",
    "family": "Neutral"
  },
  {
    "code": "13-1008 TCX",
    "name": "Oatmeal",
    "hex": "#d8c8b1",
    "family": "Neutral",
    "aliases": [
      "ecru"
    ]
  },
  {
    "code": "15-1216 TCX",
    "name": "Warm Taupe",
    "hex": "#af9b87",
    "family": "Neutral"
  },
  {
    "code": "16-1324 TCX",
    "name": "Khaki",
    "hex": "#a18a64",
    "family": "Neutral"
  },
  {
    "code": "13-0607 TCX",
    "name": "Bone",
    "hex": "#dcd3c1",
    "family": "Neutral",
    "aliases": [
      "beige",
      "natural"
    ]
  },
  {
    "code": "16-1010 TCX",
    "name": "Stone Khaki",
    "hex": "#a99a85",
    "family": "Neutral"
  },
  {
    "code": "17-5104 TCX",
    "name": "Ultimate Gray",
    "hex": "#939597",
    "family": "Gray",
    "aliases": [
      "grey",
      "heather grey",
      "heather gray"
    ]
  },
  {
    "code": "18-0601 TCX",
    "name": "Pewter",
    "hex": "#69676c",
    "family": "Gray",
    "aliases": [
      "grey",
      "dark grey"
    ]
  },
  {
    "code": "19-3906 TCX",
    "name": "Charcoal Gray",
    "hex": "#41424a",
    "family": "Gray",
    "aliases": [
      "grey",
      "graphite"
    ]
  },
  {
    "code": "14-4102 TCX",
    "name": "Glacier Gray",
    "hex": "#c5c6c7",
    "family": "Gray",
    "aliases": [
      "grey",
      "silver",
      "light grey"
    ]
  },
  {
    "code": "16-3850 TCX",
    "name": "Sharkskin",
    "hex": "#838487",
    "family": "Gray",
    "aliases": [
      "grey",
      "slate",
      "slate grey"
    ]
  },
  {
    "code": "18-3905 TCX",
    "name": "Excalibur",
    "hex": "#6c6873",
    "family": "Gray",
    "aliases": [
      "grey"
    ]
  },
  {
    "code": "11-4001 TCX",
    "name": "Brilliant White",
    "hex": "#edf1fe",
    "family": "Gray"
  },
  {
    "code": "19-4007 TCX",
    "name": "Anthracite",
    "hex": "#28282d",
    "family": "Gray",
    "aliases": [
      "black",
      "off black"
    ]
  },
  {
    "code": "19-0303 TCX",
    "name": "Jet Black",
    "hex": "#16161a",
    "family": "Gray"
  },
  {
    "code": "19-4005 TCX",
    "name": "Stretch Limo",
    "hex": "#1f1f23",
    "family": "Gray",
    "aliases": [
      "black",
      "off black"
    ]
  }
];
var PANTONE_FAMILIES = ["Red", "Pink", "Orange", "Yellow", "Green", "Teal", "Blue", "Purple", "Brown", "Neutral", "Gray"];

// piobox-customizer/core/color.js
var clamp3 = (v, lo, hi) => Math.min(hi, Math.max(lo, v));
var lerp = (a, b, t) => a + (b - a) * t;
function normalizeHex(input) {
  const s = String(input ?? "").trim().replace(/^#/, "").toLowerCase();
  if (/^[0-9a-f]{3}$/.test(s)) return `#${s[0]}${s[0]}${s[1]}${s[1]}${s[2]}${s[2]}`;
  if (/^[0-9a-f]{6}$/.test(s)) return `#${s}`;
  return null;
}
function hexToRgb(hex) {
  const n = parseInt((normalizeHex(hex) ?? "#000000").slice(1), 16);
  return { r: n >> 16 & 255, g: n >> 8 & 255, b: n & 255 };
}
function rgbToHex({ r, g, b }) {
  const c = (v) => clamp3(Math.round(v), 0, 255).toString(16).padStart(2, "0");
  return `#${c(r)}${c(g)}${c(b)}`;
}
function srgbToLinear(v) {
  const c = v / 255;
  return c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
}
function linearToSrgb(v) {
  return 255 * (v <= 31308e-7 ? 12.92 * v : 1.055 * Math.pow(v, 1 / 2.4) - 0.055);
}
function mixLinear(a, b, t) {
  return {
    r: linearToSrgb(lerp(srgbToLinear(a.r), srgbToLinear(b.r), t)),
    g: linearToSrgb(lerp(srgbToLinear(a.g), srgbToLinear(b.g), t)),
    b: linearToSrgb(lerp(srgbToLinear(a.b), srgbToLinear(b.b), t))
  };
}
function relativeLuminance(hex) {
  const { r, g, b } = hexToRgb(hex);
  return 0.2126 * srgbToLinear(r) + 0.7152 * srgbToLinear(g) + 0.0722 * srgbToLinear(b);
}
function hexToLab(hex) {
  const { r, g, b } = hexToRgb(hex);
  const R = srgbToLinear(r), G = srgbToLinear(g), B = srgbToLinear(b);
  let x = (0.4124564 * R + 0.3575761 * G + 0.1804375 * B) / 0.95047;
  let y = 0.2126729 * R + 0.7151522 * G + 0.072175 * B;
  let z = (0.0193339 * R + 0.119192 * G + 0.9503041 * B) / 1.08883;
  const f = (t) => t > 8856e-6 ? Math.cbrt(t) : 7.787 * t + 16 / 116;
  x = f(x);
  y = f(y);
  z = f(z);
  return { L: 116 * y - 16, a: 500 * (x - y), b: 200 * (y - z) };
}
function deltaE2000(lab1, lab2) {
  const rad = Math.PI / 180, deg = 180 / Math.PI;
  const { L: L1, a: a1, b: b1 } = lab1;
  const { L: L2, a: a2, b: b2 } = lab2;
  const Lbar = (L1 + L2) / 2;
  const Cbar = (Math.hypot(a1, b1) + Math.hypot(a2, b2)) / 2;
  const G = 0.5 * (1 - Math.sqrt(Math.pow(Cbar, 7) / (Math.pow(Cbar, 7) + 6103515625)));
  const a1p = a1 * (1 + G), a2p = a2 * (1 + G);
  const C1p = Math.hypot(a1p, b1), C2p = Math.hypot(a2p, b2);
  const Cbarp = (C1p + C2p) / 2;
  const hue = (a, b) => {
    if (a === 0 && b === 0) return 0;
    let h2 = Math.atan2(b, a) * deg;
    if (h2 < 0) h2 += 360;
    return h2;
  };
  const h1p = hue(a1p, b1), h2p = hue(a2p, b2);
  const dCp = C2p - C1p;
  let dhp = 0;
  if (C1p * C2p !== 0) {
    dhp = h2p - h1p;
    if (dhp > 180) dhp -= 360;
    else if (dhp < -180) dhp += 360;
  }
  const dHp = 2 * Math.sqrt(C1p * C2p) * Math.sin(dhp / 2 * rad);
  let Hbarp = h1p + h2p;
  if (C1p * C2p !== 0) {
    if (Math.abs(h1p - h2p) > 180) Hbarp += h1p + h2p < 360 ? 360 : -360;
    Hbarp /= 2;
  }
  const SC = 1 + 0.045 * Cbarp;
  const T = 1 - 0.17 * Math.cos((Hbarp - 30) * rad) + 0.24 * Math.cos(2 * Hbarp * rad) + 0.32 * Math.cos((3 * Hbarp + 6) * rad) - 0.2 * Math.cos((4 * Hbarp - 63) * rad);
  const SH = 1 + 0.015 * Cbarp * T;
  const SL = 1 + 0.015 * Math.pow(Lbar - 50, 2) / Math.sqrt(20 + Math.pow(Lbar - 50, 2));
  const RT = -(2 * Math.sqrt(Math.pow(Cbarp, 7) / (Math.pow(Cbarp, 7) + 6103515625))) * Math.sin(2 * (30 * Math.exp(-Math.pow((Hbarp - 275) / 25, 2))) * rad);
  return Math.sqrt(Math.pow((L2 - L1) / SL, 2) + Math.pow(dCp / SC, 2) + Math.pow(dHp / SH, 2) + dCp / SC * RT * (dHp / SH));
}
function hexDeltaE(a, b) {
  return deltaE2000(hexToLab(a), hexToLab(b));
}
function nearestByDeltaE(hex, entries) {
  if (!entries.length) return null;
  const lab = hexToLab(hex);
  let best = entries[0];
  let bestD = deltaE2000(lab, hexToLab(best.hex));
  for (let i = 1; i < entries.length; i++) {
    const d = deltaE2000(lab, hexToLab(entries[i].hex));
    if (d < bestD) {
      bestD = d;
      best = entries[i];
    }
  }
  return { entry: best, deltaE: bestD };
}
function hexToHsv(hex) {
  const { r, g, b } = hexToRgb(hex);
  const R = r / 255, G = g / 255, B = b / 255;
  const max = Math.max(R, G, B), d = max - Math.min(R, G, B);
  let h2 = 0;
  if (d !== 0) {
    h2 = (max === R ? (G - B) / d % 6 : max === G ? (B - R) / d + 2 : (R - G) / d + 4) * 60;
    if (h2 < 0) h2 += 360;
  }
  return { h: Math.round(h2), s: Math.round(100 * (max === 0 ? 0 : d / max)), v: Math.round(100 * max) };
}
function hsvToHex({ h: h2, s, v }) {
  const H = (h2 % 360 + 360) % 360;
  const S = clamp3(s, 0, 100) / 100, V = clamp3(v, 0, 100) / 100;
  const c = V * S, x = c * (1 - Math.abs(H / 60 % 2 - 1)), m = V - c;
  let r = 0, g = 0, b = 0;
  if (H < 60) [r, g, b] = [c, x, 0];
  else if (H < 120) [r, g, b] = [x, c, 0];
  else if (H < 180) [r, g, b] = [0, c, x];
  else if (H < 240) [r, g, b] = [0, x, c];
  else if (H < 300) [r, g, b] = [x, 0, c];
  else [r, g, b] = [c, 0, x];
  return rgbToHex({ r: (r + m) * 255, g: (g + m) * 255, b: (b + m) * 255 });
}
var BLACK = { r: 0, g: 0, b: 0 };
var WHITE = { r: 255, g: 255, b: 255 };
var SHADE_STOPS = [
  { t: 0, mixWith: BLACK, amount: 0.48 },
  { t: 0.3, mixWith: BLACK, amount: 0.2 },
  { t: 0.5, mixWith: BLACK, amount: 0 },
  { t: 1, mixWith: WHITE, amount: 0.26 }
];
function buildRecolorLut(hex) {
  const target = hexToRgb(hex);
  const darkness = clamp3(1 - (0.2126 * srgbToLinear(target.r) + 0.7152 * srgbToLinear(target.g) + 0.0722 * srgbToLinear(target.b)) / 0.045, 0, 1);
  const r = new Uint8ClampedArray(256), g = new Uint8ClampedArray(256), b = new Uint8ClampedArray(256);
  const shade = (t) => {
    const base = darkness > 0 ? mixLinear(target, WHITE, 0.018 * darkness) : target;
    const amt = (stop) => stop.mixWith === WHITE ? stop.amount + 0.14 * darkness : stop.amount;
    if (t <= SHADE_STOPS[0].t) return mixLinear(base, SHADE_STOPS[0].mixWith, amt(SHADE_STOPS[0]));
    for (let i = 1; i < SHADE_STOPS.length; i++) {
      const prev = SHADE_STOPS[i - 1], next = SHADE_STOPS[i];
      if (t <= next.t) {
        const span = next.t - prev.t || 1;
        const k = (t - prev.t) / span;
        return mixLinear(mixLinear(base, prev.mixWith, amt(prev)), mixLinear(base, next.mixWith, amt(next)), k);
      }
    }
    const last = SHADE_STOPS[SHADE_STOPS.length - 1];
    return mixLinear(base, last.mixWith, amt(last));
  };
  for (let i = 0; i < 256; i++) {
    const c = shade(i / 255);
    r[i] = c.r;
    g[i] = c.g;
    b[i] = c.b;
  }
  return { r, g, b };
}

// piobox-customizer/core/pantone.js
function canonicalPantoneId(code) {
  const t = code.trim().toUpperCase();
  const tcx = t.match(/^(\d{2})-(\d{4})\s+(TCX|TPG|TPX)$/);
  if (tcx) return `pantone-reference:${tcx[3].toLowerCase()}:${tcx[1]}-${tcx[2]}`;
  const solid = t.match(/^PANTONE\s+(\d{2,4})\s+([CU])$/);
  if (solid) return `pantone-reference:${solid[2].toLowerCase()}:${solid[1]}`;
  return `pantone-reference:invalid:${t.toLowerCase().replace(/[^a-z0-9]+/g, "-")}`;
}
var PANTONE_COLORS = PANTONE_LIBRARY.map((c) => ({ ...c, id: canonicalPantoneId(c.code), aliases: c.aliases ?? [] }));
var PANTONE_DATASET = { id: "pantone-reference-library", version: "2026-07-12.1", displayValues: "srgb-screen-approximations" };
var normalizeCode = (s) => s.toLowerCase().replace(/pantone/g, "").replace(/[\s\-_]/g, "").replace(/(tcx|tpg|tpx|c|u)$/, "");
var INDEX = PANTONE_COLORS.map((c) => ({
  color: c,
  text: `${c.name} ${c.code} ${c.family} ${c.aliases.join(" ")}`.toLowerCase(),
  normalizedCode: normalizeCode(c.code)
}));
function searchPantone(query) {
  const q = query.trim().toLowerCase();
  if (!q) return [...PANTONE_COLORS];
  const nq = normalizeCode(q);
  return INDEX.filter(({ text, normalizedCode }) => text.includes(q) || nq.length > 0 && normalizedCode.includes(nq)).map((e) => e.color);
}
function findPantoneByCode(query) {
  const q = query.trim().toLowerCase();
  if (!q) return null;
  const exact = PANTONE_COLORS.find((c) => c.code.toLowerCase() === q || c.name.toLowerCase() === q);
  if (exact) return exact;
  const nq = normalizeCode(q);
  return nq ? PANTONE_COLORS.find((c) => normalizeCode(c.code) === nq) ?? null : null;
}
function parseCodeSpace(input) {
  const t = input.trim().toUpperCase().replace(/^PANTONE\s+/, "").replace(/\s+(TCX|TPG|TPX|C|U)$/, "").trim();
  const textile = t.match(/^(\d{2})[-\s]?(\d{4})$/);
  if (textile) return { space: "textile", value: 1e4 * Number(textile[1]) + Number(textile[2]) };
  const solid = t.match(/^(\d{3,4})$/);
  return solid ? { space: "solid", value: Number(solid[1]) } : null;
}
var NEAREST_CODE_DISTANCE = { solid: 50, textile: 200 };
function resolvePantoneQuery(input) {
  const q = input.trim();
  if (!q) return null;
  const exact = findPantoneByCode(q);
  if (exact) return { match: "exact", color: exact };
  const parsed = parseCodeSpace(q);
  if (parsed) {
    let best = null;
    for (const c of PANTONE_COLORS) {
      const p = parseCodeSpace(c.code);
      if (!p || p.space !== parsed.space) continue;
      const d = Math.abs(p.value - parsed.value);
      if (!best || d < best.distance) best = { color: c, distance: d };
    }
    return best && best.distance <= NEAREST_CODE_DISTANCE[parsed.space] ? { match: "nearest", color: best.color, by: "code" } : null;
  }
  const hex = q.startsWith("#") || /^[0-9a-f]{6}$/i.test(q) ? normalizeHex(q) : null;
  if (hex) {
    const n = nearestByDeltaE(hex, PANTONE_COLORS);
    if (n) return { match: "nearest", color: n.entry, by: "color" };
  }
  if (!/[a-z]/i.test(q)) return null;
  const first = searchPantone(q)[0];
  return first ? { match: "nearest", color: first, by: "name" } : null;
}
function popularPantone(limit = 12) {
  const seen = /* @__PURE__ */ new Set(), out = [];
  for (const c of PANTONE_COLORS) {
    if (seen.has(c.family)) continue;
    seen.add(c.family);
    out.push(c);
    if (out.length >= limit) break;
  }
  return out;
}
function stockedPicksForProduct(product) {
  return (product?.colors ?? []).map((c) => ({
    label: c.label,
    color: { id: c.id, name: c.label, hex: normalizeHex(c.swatchHex) ?? "#ffffff" }
  }));
}
function defaultStockedSelection(product) {
  const picks = stockedPicksForProduct(product);
  const white = picks.find((p) => /pfd|white/i.test(p.color.id) || /white|pfd/i.test(p.label));
  const pick = white ?? picks[0];
  return pick ? { kind: "stocked", color: pick.color } : { kind: "custom", hex: "#f4f4f5" };
}
function stockedKindForHex(hex) {
  const { r, g, b } = hexToRgbLocal(hex);
  if (Math.max(r, g, b) - Math.min(r, g, b) > 16) return null;
  const l = 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
  return l >= 0.9 ? "white" : l <= 0.1 ? "black" : null;
}
function hexToRgbLocal(hex) {
  const n = parseInt((normalizeHex(hex) ?? "#000000").slice(1), 16);
  return { r: n >> 16 & 255, g: n >> 8 & 255, b: n & 255 };
}
function lin(v) {
  const c = v / 255;
  return c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
}
var selectionHex = (sel) => sel.kind === "custom" ? sel.hex : sel.color.hex;
var selectionName = (sel) => sel.kind === "custom" ? "Custom mix" : sel.color.name;
function selectionSubtitle(sel) {
  if (sel.kind === "stocked") return `Stocked ${sel.color.id}`;
  if (sel.kind === "pantone") return sel.color.code;
  return `Custom ${sel.hex.toUpperCase()}`;
}
function selectionNotesLabel(sel) {
  if (sel.kind === "stocked") return `Stocked ${sel.color.name} (${sel.color.hex.toUpperCase()})`;
  if (sel.kind === "pantone") return `Pantone ${sel.color.code} \xB7 ${sel.color.name} (${sel.color.hex.toUpperCase()})`;
  const n = nearestByDeltaE(sel.hex, PANTONE_COLORS);
  return `Custom ${sel.hex.toUpperCase()}${n ? `, nearest Pantone ${n.entry.code} ${n.entry.name}` : ""}`;
}
function serializeSelection(sel) {
  if (sel.kind === "stocked") return { schemaVersion: 2, kind: "stocked", hex: sel.color.hex, canonicalId: `supplier:${sel.color.id}`, colorwayId: sel.color.id, name: sel.color.name };
  if (sel.kind === "pantone") return { schemaVersion: 2, kind: "pantone", hex: sel.color.hex, canonicalId: sel.color.id, name: sel.color.name, code: sel.color.code, datasetVersion: PANTONE_DATASET.version };
  return { schemaVersion: 2, kind: "custom", hex: sel.hex, canonicalId: `custom-hex:${sel.hex.toLowerCase()}`, name: `Custom HEX ${sel.hex.toUpperCase()}` };
}
function deserializeSelection(raw, product) {
  if (!raw || typeof raw !== "object") return null;
  const hex = typeof raw.hex === "string" ? normalizeHex(raw.hex) : null;
  if (!hex) return null;
  const picks = stockedPicksForProduct(product);
  if (raw.kind === "stocked") {
    const p = picks.find(({ color }) => color.id === raw.colorwayId || color.hex.toLowerCase() === hex);
    if (p) return { kind: "stocked", color: p.color };
  }
  if (raw.kind === "pantone") {
    if (typeof raw.canonicalId === "string") {
      const c2 = PANTONE_COLORS.find((x) => x.id === raw.canonicalId);
      if (c2 && c2.hex.toLowerCase() === hex) return { kind: "pantone", color: c2 };
    }
    const c = PANTONE_COLORS.find((x) => x.code === raw.code && x.hex.toLowerCase() === hex) ?? PANTONE_COLORS.find((x) => x.hex.toLowerCase() === hex);
    if (c) return { kind: "pantone", color: c };
  }
  const stocked = picks.find(({ color }) => color.hex.toLowerCase() === hex);
  if (stocked) return { kind: "stocked", color: stocked.color };
  return { kind: "custom", hex };
}
function resolveColorHint(hint, product) {
  const h2 = (hint ?? "").trim();
  if (!h2) return null;
  const picks = stockedPicksForProduct(product);
  const low = h2.toLowerCase();
  const stocked = picks.find(({ color, label }) => color.id.toLowerCase() === low || label.toLowerCase() === low);
  if (stocked) return { kind: "stocked", color: stocked.color };
  const q = resolvePantoneQuery(h2);
  if (q?.match === "exact") return { kind: "pantone", color: q.color };
  const hex = h2.startsWith("#") || /^[0-9a-f]{6}$/i.test(h2) ? normalizeHex(h2) : null;
  if (hex) {
    const kind = stockedKindForHex(hex);
    if (kind) {
      const pick = picks.find(({ color }) => stockedKindForHex(color.hex) === kind);
      if (pick) return { kind: "stocked", color: pick.color };
    }
    const exact = PANTONE_COLORS.find((c) => c.hex === hex);
    return exact ? { kind: "pantone", color: exact } : { kind: "custom", hex };
  }
  return q ? { kind: "pantone", color: q.color } : null;
}

// piobox-customizer/core/text.js
var TEXT_PLACEHOLDER = "Your text";
var TEXT_MAX_CHARS = 120;
var INK_COLORS = [
  { id: "white", label: "White", hex: "#FFFFFF" },
  { id: "black", label: "Black", hex: "#111111" },
  { id: "navy", label: "Navy", hex: "#1E2A45" },
  { id: "royal", label: "Royal blue", hex: "#1D4ED8" },
  { id: "red", label: "Red", hex: "#C8102E" },
  { id: "gold", label: "Gold", hex: "#F2A900" },
  { id: "forest", label: "Forest", hex: "#115740" },
  { id: "silver", label: "Silver", hex: "#A2AAAD" }
];
var HEX6 = /^#[0-9a-fA-F]{6}$/;
var clamp4 = (v, lo, hi) => Math.min(hi, Math.max(lo, v));
var isTextEmpty = (t) => t.content.trim().length === 0;
function sanitizeTextSpec(raw, registry) {
  if (!raw || typeof raw !== "object" || typeof raw.content !== "string") return null;
  return {
    content: raw.content.slice(0, TEXT_MAX_CHARS),
    fontId: registry.get(typeof raw.fontId === "string" ? raw.fontId : void 0).id,
    color: typeof raw.color === "string" && HEX6.test(raw.color) ? raw.color : "#111111",
    spacing: typeof raw.spacing === "number" && Number.isFinite(raw.spacing) ? clamp4(raw.spacing, 0, 0.4) : void 0,
    arc: typeof raw.arc === "number" && Number.isFinite(raw.arc) ? clamp4(raw.arc, -1, 1) : void 0,
    outline: typeof raw.outline === "string" && HEX6.test(raw.outline) ? raw.outline : void 0
  };
}
function measureChars(ctx, text, spacing) {
  const chars = [...text];
  const widths = chars.map((c) => ctx.measureText(c).width);
  return { chars, widths, width: widths.reduce((a, b) => a + b, 0) + spacing * Math.max(0, chars.length - 1) };
}
function paintText(ctx, text, x, y, style) {
  if (style.outline) {
    ctx.strokeStyle = style.outline;
    ctx.lineWidth = style.outlinePx;
    ctx.lineJoin = "round";
    ctx.strokeText(text, x, y);
  }
  ctx.fillStyle = style.fill;
  ctx.fillText(text, x, y);
}
function paintArched(ctx, measured, spacing, cx, baseline, arc, style) {
  const angle = Math.abs(arc) * Math.PI, radius = measured.width / angle, dir = arc > 0 ? 1 : -1;
  let cursor = -measured.width / 2;
  for (let i = 0; i < measured.chars.length; i++) {
    const ch = measured.chars[i], w = measured.widths[i];
    if (ch.trim() !== "") {
      const t = (cursor + w / 2) / radius;
      ctx.save();
      ctx.translate(cx + radius * Math.sin(t), baseline + dir * radius * (1 - Math.cos(t)));
      ctx.rotate(dir * t);
      paintText(ctx, ch, 0, 0, style);
      ctx.restore();
    }
    cursor += w + spacing;
  }
}
function contentBounds(canvas) {
  const ctx = canvas.getContext("2d"), { width: w, height: h2 } = canvas;
  const d = ctx.getImageData(0, 0, w, h2).data;
  let l = w, t = h2, r = -1, b = -1;
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    if (d[(y * w + x) * 4 + 3] <= 8) continue;
    if (x < l) l = x;
    if (x > r) r = x;
    if (y < t) t = y;
    if (y > b) b = y;
  }
  return r < 0 ? null : { x: l, y: t, w: r - l + 1, h: b - t + 1 };
}
async function renderTextLayer(spec, registry) {
  const font = registry.get(spec.fontId);
  const empty = isTextEmpty(spec);
  let lines = (empty ? TEXT_PLACEHOLDER : spec.content).replace(/\r\n?/g, "\n").split("\n").map((l) => l.trimEnd());
  while (lines.length > 0 && lines[0].trim() === "") lines.shift();
  while (lines.length > 0 && lines[lines.length - 1].trim() === "") lines.pop();
  if (lines.length === 0) lines.push(TEXT_PLACEHOLDER);
  const spacing = empty ? 0 : clamp4(spec.spacing ?? 0, 0, 0.4);
  const arc = empty ? 0 : clamp4(spec.arc ?? 0, -1, 1);
  const outline = empty ? void 0 : spec.outline;
  const family = registry.canvasFamily(spec.fontId);
  const fontString = (px2) => `${font.weight} ${px2}px ${family}`;
  try {
    await document.fonts.load(fontString(64), lines.join(" ") || TEXT_PLACEHOLDER);
  } catch {
  }
  const canvas = document.createElement("canvas");
  const ctx = canvas.getContext("2d");
  if (!ctx) throw new Error("Canvas 2D unavailable");
  const size = (px2) => {
    ctx.font = fontString(px2);
    const sp2 = spacing * px2;
    const widest = Math.max(1, ...lines.map((l) => l ? measureChars(ctx, l, sp2).width : 0));
    const angle = Math.abs(arc) * Math.PI;
    const sagitta = angle > 0.02 ? widest / angle * (1 - Math.cos(angle / 2)) : 0;
    return { width: Math.ceil(widest + 2 * px2), height: Math.ceil(2.5 * px2 + 1.18 * px2 * (lines.length - 1) + 2 * sagitta), sagitta };
  };
  let px = 400, dims = size(px);
  const over = Math.max(dims.width / 3200, dims.height / 3200);
  if (over > 1) {
    px = Math.max(24, Math.floor(px / over));
    dims = size(px);
  }
  canvas.width = dims.width;
  canvas.height = dims.height;
  ctx.font = fontString(px);
  ctx.textAlign = "center";
  ctx.textBaseline = "alphabetic";
  const style = { fill: empty ? "#9AA1AC" : spec.color, outline, outlinePx: Math.max(2, 0.08 * px) };
  const sp = spacing * px, arched = Math.abs(arc) > 0.02, firstBaseline = 1.25 * px + dims.sagitta;
  lines.forEach((line, i) => {
    if (line === "") return;
    const y2 = firstBaseline + 1.18 * px * i;
    if (arched) paintArched(ctx, measureChars(ctx, line, sp), sp, canvas.width / 2, y2, arc, style);
    else {
      const prev = ctx.letterSpacing;
      if (sp > 0 && "letterSpacing" in ctx) ctx.letterSpacing = `${sp}px`;
      paintText(ctx, line, canvas.width / 2, y2, style);
      if ("letterSpacing" in ctx) ctx.letterSpacing = prev;
    }
  });
  const bounds = contentBounds(canvas);
  if (!bounds) throw new Error("Failed to render text");
  const pad = Math.ceil(0.02 * Math.max(bounds.w, bounds.h)) + 2;
  const x = Math.max(0, bounds.x - pad), y = Math.max(0, bounds.y - pad);
  const w = Math.min(canvas.width - x, bounds.w + 2 * pad), h2 = Math.min(canvas.height - y, bounds.h + 2 * pad);
  const out = document.createElement("canvas");
  out.width = w;
  out.height = h2;
  out.getContext("2d").drawImage(canvas, x, y, w, h2, 0, 0, w, h2);
  const blob = await new Promise((res) => out.toBlob(res, "image/png"));
  if (!blob) throw new Error("Failed to render text");
  const slug = lines[0].toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 24) || "text";
  const file = new File([blob], `text-${slug}.png`, { type: "image/png" });
  return { file, url: URL.createObjectURL(file), naturalW: w, naturalH: h2, isVector: false };
}
var TEXT_STARTERS = [
  { id: "varsity-arch", label: "Varsity arch", fontId: "varsity", layers: [
    { text: { content: "YOUR CITY", fontId: "varsity", arc: 0.55, spacing: 0.08 }, cx: 0.5, cy: 0.4, width: 0.4 },
    { text: { content: "EST. 2026", fontId: "modern", spacing: 0.3 }, cx: 0.5, cy: 0.495, width: 0.17 }
  ] },
  { id: "badge", label: "Badge", fontId: "varsity", layers: [
    { text: { content: "YOUR BRAND", fontId: "varsity", arc: 0.62, spacing: 0.1 }, cx: 0.5, cy: 0.35, width: 0.34 },
    { text: { content: "2026", fontId: "impact", spacing: 0.06 }, cx: 0.5, cy: 0.435, width: 0.11 },
    { text: { content: "SINCE DAY ONE", fontId: "varsity", arc: -0.62, spacing: 0.1 }, cx: 0.5, cy: 0.5, width: 0.34 }
  ] },
  { id: "team-number", label: "Team number", fontId: "impact", layers: [
    { text: { content: "YOUR TEAM", fontId: "impact", spacing: 0.16 }, cx: 0.5, cy: 0.335, width: 0.24 },
    { text: { content: "01", fontId: "impact" }, cx: 0.5, cy: 0.468, width: 0.16 }
  ] },
  { id: "wordmark", label: "Wordmark", fontId: "modern", layers: [{ text: { content: "YOUR BRAND", fontId: "modern", spacing: 0.24 }, cx: 0.5, cy: 0.42, width: 0.34 }] },
  { id: "mono-tag", label: "Mono tag", fontId: "mono", layers: [
    { text: { content: "YOUR TEAM", fontId: "mono", spacing: 0.26 }, cx: 0.5, cy: 0.395, width: 0.3 },
    { text: { content: "SHIP FAST", fontId: "mono", spacing: 0.26 }, cx: 0.5, cy: 0.45, width: 0.2 }
  ] },
  { id: "coordinates", label: "Coordinates", fontId: "mono", layers: [{ text: { content: "37.7749\xB0 N\n122.4194\xB0 W", fontId: "mono", spacing: 0.18 }, cx: 0.5, cy: 0.42, width: 0.24 }] },
  { id: "heritage", label: "Heritage", fontId: "serif", layers: [
    { text: { content: "Your Brand", fontId: "serif", spacing: 0.02 }, cx: 0.5, cy: 0.4, width: 0.34 },
    { text: { content: "EST. 2026", fontId: "mono", spacing: 0.3 }, cx: 0.5, cy: 0.478, width: 0.17 }
  ] },
  { id: "monogram", label: "Monogram", fontId: "serif", layers: [{ text: { content: "YB", fontId: "serif", spacing: 0.06 }, cx: 0.5, cy: 0.42, width: 0.16 }] },
  { id: "script", label: "Script", fontId: "script", layers: [{ text: { content: "your brand", fontId: "script" }, cx: 0.5, cy: 0.42, width: 0.3 }] },
  { id: "statement", label: "Statement", fontId: "impact", layers: [{ text: { content: "BOLD\nMOVES", fontId: "impact", spacing: 0.02 }, cx: 0.5, cy: 0.43, width: 0.3 }] },
  { id: "repeat", label: "Repeat", fontId: "modern", layers: [{ text: { content: "YOUR BRAND\nYOUR BRAND\nYOUR BRAND", fontId: "modern", spacing: 0.12 }, cx: 0.5, cy: 0.42, width: 0.36 }] }
];

// piobox-customizer/core/fonts.js
var BUILTIN_FONTS = [
  { id: "impact", label: "Impact", name: "Anton", family: "Anton", weight: "400", file: "anton-latin.woff2", fallback: "Impact, 'Arial Narrow Bold', sans-serif" },
  { id: "modern", label: "Modern", name: "Geist", family: "Geist", weight: "600", file: "geist-latin.woff2", fallback: "ui-sans-serif, system-ui, sans-serif" },
  { id: "varsity", label: "Varsity", name: "Graduate", family: "Graduate", weight: "400", file: "graduate-latin.woff2", fallback: "'Times New Roman', serif" },
  { id: "serif", label: "Serif", name: "Playfair Display", family: "Playfair Display", weight: "600", file: "playfair-display-latin.woff2", fallback: "Georgia, 'Times New Roman', serif" },
  { id: "script", label: "Script", name: "Pacifico", family: "Pacifico", weight: "400", file: "pacifico-latin.woff2", fallback: "'Brush Script MT', cursive" },
  { id: "mono", label: "Mono", name: "Geist Mono", family: "Geist Mono", weight: "500", file: "geist-mono-latin.woff2", fallback: "ui-monospace, SFMono-Regular, Menlo, monospace" }
];
var DB_NAME = "piobox-customizer";
var STORE = "fonts";
var FONT_EXTS = [".ttf", ".otf", ".woff", ".woff2"];
var FONT_ACCEPT_ATTR = FONT_EXTS.join(",");
var MAX_FONT_BYTES = 6 * 1024 * 1024;
function openDb() {
  return new Promise((resolve, reject) => {
    if (typeof indexedDB === "undefined") return reject(new Error("IndexedDB unavailable"));
    const req = indexedDB.open(DB_NAME, 1);
    req.onupgradeneeded = () => {
      const db = req.result;
      if (!db.objectStoreNames.contains(STORE)) db.createObjectStore(STORE, { keyPath: "id" });
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}
async function idb(mode, fn) {
  const db = await openDb();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(STORE, mode), store = tx.objectStore(STORE);
    const req = fn(store);
    tx.oncomplete = () => resolve(req?.result);
    tx.onerror = () => reject(tx.error);
  });
}
var quote = (family) => /^[a-z0-9_-]+$/i.test(family) ? family : `'${family.replace(/'/g, "\\'")}'`;
var hash = (s) => {
  let h2 = 5381;
  for (let i = 0; i < s.length; i++) h2 = (h2 << 5) + h2 + s.charCodeAt(i) | 0;
  return (h2 >>> 0).toString(36);
};
var FontRegistry = class _FontRegistry {
  constructor({ fontBase = "", fonts = [], allowUpload = true } = {}) {
    this.fontBase = fontBase.replace(/\/$/, "");
    this.allowUpload = allowUpload;
    this.fonts = /* @__PURE__ */ new Map();
    this.listeners = /* @__PURE__ */ new Set();
    this.injected = /* @__PURE__ */ new Set();
    for (const f of BUILTIN_FONTS) this.fonts.set(f.id, { ...f, source: "builtin", cssFamily: `${quote(f.family)}, ${f.fallback}` });
    for (const f of fonts) this.addConfigured(f);
    this.injectBuiltinCss();
  }
  onChange(fn) {
    this.listeners.add(fn);
    return () => this.listeners.delete(fn);
  }
  emit() {
    for (const fn of this.listeners) fn(this.list());
  }
  list() {
    return [...this.fonts.values()];
  }
  get(id) {
    return this.fonts.get(id) ?? this.fonts.get(BUILTIN_FONTS[0].id);
  }
  defaultId() {
    return BUILTIN_FONTS[0].id;
  }
  cssFamily(id) {
    return this.get(id).cssFamily;
  }
  canvasFamily(id) {
    const f = this.get(id);
    return `${quote(f.family)}, ${f.fallback ?? "sans-serif"}`;
  }
  weight(id) {
    return String(this.get(id).weight ?? "400");
  }
  injectBuiltinCss() {
    if (typeof document === "undefined" || document.getElementById("pc-builtin-fonts")) return;
    const css = BUILTIN_FONTS.map((f) => {
      const range = f.name.startsWith("Geist") ? "100 900" : f.name === "Playfair Display" ? "400 900" : "400";
      return `@font-face{font-family:${quote(f.family)};font-style:normal;font-weight:${range};font-display:swap;src:url("${this.fontBase}/${f.file}") format("woff2")}`;
    }).join("\n");
    const style = document.createElement("style");
    style.id = "pc-builtin-fonts";
    style.textContent = css;
    document.head.appendChild(style);
  }
  // Merchant-configured font: { id, label, family, weight?, src?: url, google?: true, fallback? }
  addConfigured(f) {
    const id = f.id ?? `font-${hash(f.family)}`;
    const entry = { id, label: f.label ?? f.family, name: f.family, family: f.family, weight: String(f.weight ?? "400"), fallback: f.fallback ?? "sans-serif", source: "configured", cssFamily: `${quote(f.family)}, ${f.fallback ?? "sans-serif"}` };
    this.fonts.set(id, entry);
    if (f.google) this.injectGoogle(f.family, entry.weight);
    else if (f.src) this.injectFace(f.family, f.src, entry.weight);
    return entry;
  }
  injectGoogle(family, weight) {
    const key = `google:${family}:${weight}`;
    if (this.injected.has(key) || typeof document === "undefined") return;
    this.injected.add(key);
    const link = document.createElement("link");
    link.rel = "stylesheet";
    link.href = `https://fonts.googleapis.com/css2?family=${encodeURIComponent(family).replace(/%20/g, "+")}:wght@${weight}&display=swap`;
    document.head.appendChild(link);
  }
  injectFace(family, src, weight) {
    const key = `face:${family}:${src}`;
    if (this.injected.has(key) || typeof document === "undefined") return;
    this.injected.add(key);
    try {
      const face = new FontFace(family, `url("${src}")`, { weight });
      document.fonts.add(face);
      face.load().catch(() => {
      });
    } catch {
    }
  }
  async ensureLoaded(id, sample = "Ag") {
    const f = this.get(id);
    try {
      await document.fonts.load(`${f.weight} 64px ${quote(f.family)}`, sample);
    } catch {
    }
  }
  // ---- customer uploads ------------------------------------------------------------
  static isFontFile(file) {
    const n = file.name.toLowerCase();
    return FONT_EXTS.some((e) => n.endsWith(e));
  }
  async addFile(file, { persist = true } = {}) {
    if (!this.allowUpload) throw new Error("Font uploads are disabled.");
    if (!_FontRegistry.isFontFile(file)) throw new Error("Use a TTF, OTF, WOFF or WOFF2 font file.");
    if (file.size > MAX_FONT_BYTES) throw new Error(`Font file is too large (max ${Math.round(MAX_FONT_BYTES / 1048576)} MB).`);
    const bytes = await file.arrayBuffer();
    return this.registerUploaded({ id: `upload-${hash(file.name + file.size)}`, name: file.name, bytes, type: file.type }, persist);
  }
  async registerUploaded(rec, persist) {
    const stem = rec.name.replace(/\.[^.]+$/, "");
    const family = `PC Upload ${stem}`.replace(/[^\w \-]/g, "").slice(0, 60) || `PC Upload ${rec.id}`;
    const face = new FontFace(family, rec.bytes, { weight: "400" });
    await face.load();
    document.fonts.add(face);
    const entry = { id: rec.id, label: stem.slice(0, 24), name: stem, family, weight: "400", fallback: "sans-serif", source: "uploaded", cssFamily: `${quote(family)}, sans-serif`, fileName: rec.name, mime: rec.type || "font/ttf", bytes: rec.bytes };
    this.fonts.set(entry.id, entry);
    if (persist) {
      try {
        await idb("readwrite", (s) => s.put({ id: rec.id, name: rec.name, bytes: rec.bytes, type: rec.type }));
      } catch {
      }
    }
    this.emit();
    return entry;
  }
  async restoreUploaded() {
    if (!this.allowUpload) return [];
    let rows = [];
    try {
      rows = await idb("readonly", (s) => s.getAll()) ?? [];
    } catch {
      return [];
    }
    const out = [];
    for (const r of rows) {
      try {
        out.push(await this.registerUploaded(r, false));
      } catch {
      }
    }
    return out;
  }
  async remove(id) {
    const f = this.fonts.get(id);
    if (!f || f.source !== "uploaded") return false;
    this.fonts.delete(id);
    try {
      await idb("readwrite", (s) => s.delete(id));
    } catch {
    }
    this.emit();
    return true;
  }
  // Data URL of an uploaded font so it can travel with the order for production.
  exportUploaded(id) {
    const f = this.fonts.get(id);
    if (!f || f.source !== "uploaded" || !f.bytes) return null;
    let bin = "";
    const u8 = new Uint8Array(f.bytes);
    for (let i = 0; i < u8.length; i += 32768) bin += String.fromCharCode.apply(null, u8.subarray(i, i + 32768));
    return { name: f.fileName, type: f.mime, dataUrl: `data:${f.mime};base64,${btoa(bin)}` };
  }
};

// piobox-customizer/core/draft.js
var DESIGN_RAIL_MAX = 6;
var DRAFT_ARTWORK_MAX_BYTES = 2 * 1024 * 1024;
function dataUrlToBlob(dataUrl) {
  const comma = dataUrl.indexOf(",");
  if (!dataUrl.startsWith("data:") || comma < 0) return null;
  const meta = dataUrl.slice(5, comma), isB64 = meta.endsWith(";base64"), type = meta.replace(";base64", "") || "application/octet-stream";
  try {
    if (isB64) {
      const bin = atob(dataUrl.slice(comma + 1));
      const u8 = new Uint8Array(bin.length);
      for (let i = 0; i < bin.length; i++) u8[i] = bin.charCodeAt(i);
      return new Blob([u8], { type });
    }
    return new Blob([decodeURIComponent(dataUrl.slice(comma + 1))], { type });
  } catch {
    return null;
  }
}
function fileToDraftArtwork(file) {
  if (file.size > DRAFT_ARTWORK_MAX_BYTES) return Promise.resolve(null);
  return new Promise((resolve) => {
    const r = new FileReader();
    r.onload = () => resolve({ name: file.name, type: file.type, dataUrl: String(r.result), isVector: /\.svg$/i.test(file.name) });
    r.onerror = () => resolve(null);
    r.readAsDataURL(file);
  });
}
function readDraft(key) {
  try {
    const raw = localStorage.getItem(key);
    return raw ? JSON.parse(raw) : null;
  } catch {
    return null;
  }
}
function writeDraft(key, draft) {
  const json = JSON.stringify(draft);
  try {
    if (json.length <= 35e5) {
      localStorage.setItem(key, json);
      return "full";
    }
  } catch {
  }
  try {
    const slim = (sides) => sides ? { front: sides.front.map((l) => ({ ...l, artwork: null })), back: sides.back.map((l) => ({ ...l, artwork: null })) } : sides;
    localStorage.setItem(key, JSON.stringify({ ...draft, sides: slim(draft.sides), designs: draft.designs?.map((d) => ({ ...d, sides: slim(d.sides) })) }));
    return "slim";
  } catch {
    return "failed";
  }
}
function encodeShare(obj) {
  const json = JSON.stringify(obj);
  const b64 = btoa(unescape(encodeURIComponent(json)));
  return b64.replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function decodeShare(str) {
  try {
    const b64 = str.replace(/-/g, "+").replace(/_/g, "/") + "===".slice((str.length + 3) % 4);
    return JSON.parse(decodeURIComponent(escape(atob(b64))));
  } catch {
    return null;
  }
}

// piobox-customizer/ui/panels.js
var CATEGORY_LABELS = { Tees: "Tees", Fleece: "Hoodies", Bottoms: "Bottoms", Headwear: "Hats", Outerwear: "Outerwear", Accessories: "Accessories" };
var SIDE_LABEL = { front: "Front", back: "Back" };
var METHODS = [
  { id: "dtg", label: "DTG", note: "Low qty" },
  { id: "dtf", label: "DTF", note: "Low qty" },
  { id: "screen", label: "Screen print", note: "Setup \xB7 best ~25" },
  { id: "embroidery", label: "Embroidery", note: "Setup \xB7 best ~25" }
];
var PRICE_BREAKS = [1, 6, 16, 25, 50, 100, 200, 500];
var cn = (...c) => c.filter(Boolean).join(" ");
var layerCount = (sides) => sides ? sides.front.filter(isReal).length + sides.back.filter(isReal).length : 0;
var isReal = (l) => !l.text || l.text.content.trim() !== "";
var serializedCount = (s) => ["front", "back"].reduce((n, side) => n + (Array.isArray(s?.[side]) ? s[side].filter((l) => l && (!l.text || (l.text.content ?? "").trim() !== "")).length : 0), 0);
function renderDesignsRail(s, a) {
  return h(
    "div",
    { class: "pc-rail", role: "group", "aria-label": "Your designs" },
    h("span", { class: "pc-overline pc-rail__label" }, "Designs"),
    s.designs.map((d, i) => {
      const active = d.id === s.activeDesignId;
      const product = s.products.find((p) => p.id === (active ? s.productId : d.productId));
      const count = active ? s.layerTotal : d.pending ? serializedCount(d.pending.sides) : layerCount(d.stash);
      return h(
        "div",
        { key: d.id, class: cn("pc-rail__item", active && "is-active") },
        h(
          "button",
          { type: "button", class: "pc-rail__btn", "aria-pressed": active, "aria-label": `Edit design ${i + 1}${product ? ` on ${product.name}` : ""}`, onclick: () => a.switchDesign(d.id) },
          h("span", { class: "pc-rail__num" }, i + 1),
          h("span", { class: "pc-rail__text" }, h("span", { class: "pc-rail__name" }, product?.name ?? "New design"), h("span", { class: "pc-rail__meta" }, count === 0 ? "Empty" : count === 1 ? "1 layer" : `${count} layers`))
        ),
        s.designs.length > 1 ? h("button", { type: "button", class: "pc-rail__remove", "aria-label": `Remove design ${i + 1}`, title: "Remove design", onclick: () => a.removeDesign(d.id) }, svg(I.x)) : null
      );
    }),
    s.designs.length < DESIGN_RAIL_MAX ? [
      h("button", { type: "button", class: "pc-rail__add", title: "Start another design on this blank (keeps this one)", onclick: a.newDesign }, svg(I.plus), "New design"),
      s.layerTotal > 0 ? h("button", { type: "button", class: "pc-rail__add", title: "Duplicate this design to try a variation", onclick: a.duplicateDesign }, svg(I.copy), "Duplicate") : null
    ] : h("span", { class: "pc-rail__cap" }, `Up to ${DESIGN_RAIL_MAX} designs`),
    s.notice ? h("div", { class: "pc-notice", role: "status" }, h("span", null, s.notice), h("button", { type: "button", class: "pc-notice__x", "aria-label": "Dismiss", onclick: a.dismissNotice }, svg(I.x))) : null
  );
}
function renderBlanks(s, a) {
  const categories = ["Tees", "Fleece", "Bottoms", "Headwear", "Outerwear", "Accessories"].filter((c) => s.products.some((p) => p.category === c));
  const q = s.blanksQuery.trim().toLowerCase();
  const filtered = s.products.filter((p) => (s.blanksCategory === "all" || p.category === s.blanksCategory) && (!q || p.name.toLowerCase().includes(q) || p.styleNumber.toLowerCase().includes(q)));
  const filtering = s.blanksCategory !== "all" || q.length > 0;
  const expanded = filtering || s.blanksExpanded;
  let shown = filtered;
  if (!expanded) {
    shown = filtered.slice(0, 6);
    if (s.product && !shown.some((p) => p.id === s.product.id) && filtered.some((p) => p.id === s.product.id)) shown = [s.product, ...shown.slice(0, 5)];
  }
  const hidden = filtered.length - shown.length;
  return h(
    "section",
    { class: "pc-panel pc-panel--blanks", id: "pc-section-blank", "aria-labelledby": "pc-h-blank" },
    h("h2", { class: "pc-overline pc-panel__title", id: "pc-h-blank" }, s.labels.blanks, h("button", { type: "button", class: "pc-panel__hide", onclick: () => a.toggleBlanks(true), title: "Hide the blanks column to give the design more room" }, "Hide")),
    h(
      "div",
      { class: "pc-panel__scroll", "data-scroller": "blanks" },
      h(
        "div",
        { class: "pc-blanks__head" },
        h("div", { class: "pc-search" }, svg(I.search, "pc-search__icon"), h("input", { type: "search", value: s.blanksQuery, placeholder: "Search styles or codes", "aria-label": "Search blanks", class: "pc-input pc-input--search", oninput: (e) => a.setBlanksQuery(e.target.value) })),
        categories.length > 1 ? h(
          "div",
          { class: "pc-chips", role: "tablist", "aria-label": "Filter blanks by category" },
          [{ id: "all", label: "All" }, ...categories.map((c) => ({ id: c, label: CATEGORY_LABELS[c] ?? c }))].map((c) => h("button", { key: c.id, type: "button", role: "tab", class: cn("pc-chip", s.blanksCategory === c.id && "is-active"), "aria-selected": s.blanksCategory === c.id, onclick: () => a.setBlanksCategory(c.id) }, c.label))
        ) : null,
        h("p", { class: "pc-mono-meta" }, filtering ? `${filtered.length} of ${s.products.length} styles` : `${s.products.length} styles`)
      ),
      h(
        "div",
        { class: "pc-blanks__grid" },
        shown.length === 0 ? h("div", { class: "pc-empty-box" }, h("span", null, "No styles match that search."), h("button", { type: "button", class: "pc-link", onclick: () => {
          a.setBlanksQuery("");
          a.setBlanksCategory("all");
        } }, "Clear filters")) : null,
        shown.map((p) => {
          const active = p.id === s.productId, weight = p.fabricWeight?.split("/")[0]?.trim();
          return h(
            "button",
            { key: p.id, type: "button", class: cn("pc-blank", active && "is-active"), "aria-pressed": active, "aria-label": `${p.name}, ${p.styleNumber}${weight ? `, ${weight}` : ""}`, onclick: () => a.selectProduct(p.id) },
            h("span", { class: "pc-blank__stage" }, h("img", { src: a.thumbSrc(p), alt: p.name, loading: "lazy", draggable: false })),
            h("span", { class: "pc-blank__text" }, h("span", { class: "pc-blank__name" }, p.name), h("span", { class: "pc-blank__meta" }, p.styleNumber, weight ? ` \xB7 ${weight}` : ""))
          );
        })
      ),
      !expanded && hidden > 0 ? h("button", { type: "button", class: "pc-btn pc-btn--dark pc-btn--block", onclick: a.expandBlanks }, `Browse all ${s.products.length} styles`, svg(I.arrowRight)) : null
    )
  );
}
function renderRightPanel(s, a) {
  const hex = selectionHex(s.colorSel);
  const tabs = [
    { id: "color", label: s.labels.colorTab, meta: h("span", { class: "pc-tab__meta" }, h("span", { class: "pc-dot", style: { backgroundColor: hex } }), selectionName(s.colorSel)) },
    { id: "design", label: s.labels.designTab, meta: h("span", { class: "pc-tab__meta" }, s.layerTotal === 0 ? "Nothing yet" : `${s.layerTotal} ${s.layerTotal === 1 ? "layer" : "layers"}`) },
    {
      id: "quote",
      label: s.mode === "cart" ? s.labels.orderTab : s.labels.quoteTab,
      meta: h("span", { class: "pc-tab__meta" }, s.mode === "cart" ? s.orderQty > 0 ? `${s.orderQty} pcs \xB7 ${s.money(s.price.subtotal)}` : "Pick sizes" : s.quantityNumber ? `${s.quantityNumber} pcs` : "")
    }
  ];
  return h(
    "section",
    { class: "pc-panel pc-panel--right", "aria-label": "Design options" },
    h("div", { class: "pc-tabs", role: "tablist" }, tabs.map((t) => h("button", { key: t.id, type: "button", role: "tab", id: `pc-tab-${t.id}`, "aria-selected": s.tab === t.id, "aria-controls": `pc-tabpanel-${t.id}`, class: cn("pc-tab", s.tab === t.id && "is-active"), onclick: () => a.setTab(t.id) }, h("span", { class: "pc-tab__label" }, t.label), t.meta))),
    h(
      "div",
      { class: "pc-panel__scroll", "data-scroller": "right", id: `pc-tabpanel-${s.tab}`, role: "tabpanel", "aria-labelledby": `pc-tab-${s.tab}` },
      s.tab === "color" ? renderColorTab(s, a) : s.tab === "design" ? renderDesignTab(s, a) : s.mode === "cart" ? renderOrderTab(s, a) : renderQuoteTab(s, a)
    )
  );
}
function renderColorTab(s, a) {
  const sel = s.colorSel, ui = s.colorUi, hex = selectionHex(sel);
  const picks = stockedPicksForProduct(s.product);
  const activeCode = sel.kind === "pantone" ? sel.color.code : null;
  const swatchGrid = (colors) => h("div", { class: "pc-swatches", role: "group" }, colors.map((c) => {
    const on = activeCode === c.code && hex === c.hex;
    return h("button", { key: c.code + c.hex, type: "button", class: cn("pc-swatch", on && "is-active"), "aria-pressed": on, "aria-label": `${c.name}, ${c.code}`, title: `${c.name} \xB7 ${c.code}`, style: { backgroundColor: c.hex }, onclick: () => a.setColor({ kind: "pantone", color: c }) }, on ? svg(I.check, "pc-swatch__check") : null);
  }));
  const results = searchPantone(ui.query);
  const visible = ui.family === "all" ? results : results.filter((c) => c.family === ui.family);
  const grouped = ui.query.trim() === "" && ui.family === "all";
  const hsv = ui.hsv ?? hexToHsv(hex);
  const nearest = nearestByDeltaE(hex, PANTONE_COLORS);
  const codeMatch = findPantoneByCode(ui.code), codeNearest = codeMatch ? null : resolvePantoneQuery(ui.code)?.match === "nearest" ? resolvePantoneQuery(ui.code).color : null;
  const supportsEyeDropper = typeof window !== "undefined" && "EyeDropper" in window;
  return h(
    "div",
    { class: "pc-tabbody" },
    h(
      "div",
      { class: "pc-row pc-row--between" },
      h("p", { class: "pc-strong" }, "Garment color"),
      h("div", { class: "pc-segment" }, [["library", "Pantone refs"], ["custom", "Custom HEX"]].map(([id, label]) => h("button", { key: id, type: "button", class: cn("pc-segment__btn", ui.mode === id && "is-active"), "aria-pressed": ui.mode === id, onclick: () => a.setColorMode(id) }, label)))
    ),
    picks.length ? h(
      "div",
      null,
      h("p", { class: "pc-overline-xs" }, "Core colors"),
      h("div", { class: "pc-row pc-row--wrap" }, picks.map((p) => {
        const on = sel.kind === "stocked" && sel.color.id === p.color.id;
        return h("button", { key: p.color.id, type: "button", class: cn("pc-pill", on && "is-active"), "aria-pressed": on, onclick: () => a.setColor({ kind: "stocked", color: p.color }) }, h("span", { class: "pc-dot pc-dot--lg", style: { backgroundColor: p.color.hex } }), p.label);
      }))
    ) : null,
    ui.mode === "library" ? [
      h("div", { class: "pc-current" }, h("span", { class: "pc-current__chip", style: { backgroundColor: hex } }), h("div", { class: "pc-current__text" }, h("p", { class: "pc-strong pc-truncate" }, selectionName(sel)), h("p", { class: "pc-mono-meta pc-truncate" }, selectionSubtitle(sel)))),
      h("div", null, h("p", { class: "pc-overline-xs" }, "Popular"), swatchGrid(popularPantone())),
      h("div", { class: "pc-divider" }),
      h("div", { class: "pc-search" }, svg(I.search, "pc-search__icon"), h("input", { type: "search", class: "pc-input pc-input--search", value: ui.query, placeholder: "Search name or code (e.g. 286, navy)", "aria-label": "Search Pantone colors", oninput: (e) => a.setColorUi({ query: e.target.value }) })),
      h("div", { class: "pc-chips", role: "tablist", "aria-label": "Color groups" }, ["all", ...PANTONE_FAMILIES].map((f) => h("button", { key: f, type: "button", role: "tab", class: cn("pc-chip", ui.family === f && "is-active"), "aria-selected": ui.family === f, onclick: () => a.setColorUi({ family: f, query: "" }) }, f === "all" ? "All" : f))),
      h(
        "div",
        { class: "pc-swatch-panel" },
        visible.length === 0 ? h("p", { class: "pc-muted pc-text-center" }, "No colors match that search.") : grouped ? PANTONE_FAMILIES.map((f) => {
          const list = visible.filter((c) => c.family === f);
          return list.length ? h("div", { key: f, class: "pc-swatch-group" }, h("button", { type: "button", class: "pc-overline pc-swatch-group__title", onclick: () => a.setColorUi({ family: f, query: "" }) }, f), swatchGrid(list)) : null;
        }) : swatchGrid(visible)
      )
    ] : [
      h(
        "div",
        {
          class: "pc-sv",
          role: "application",
          tabindex: 0,
          "aria-label": `Saturation and brightness. Saturation ${hsv.s}%, brightness ${hsv.v}%. Use arrow keys to adjust.`,
          style: { backgroundColor: hsvToHex({ h: hsv.h, s: 100, v: 100 }) },
          onpointerdown: (e) => {
            e.currentTarget.setPointerCapture(e.pointerId);
            e.currentTarget.dataset.drag = "1";
            a.pickSV(e);
          },
          onpointermove: (e) => {
            if (e.currentTarget.dataset.drag) a.pickSV(e);
          },
          onpointerup: (e) => {
            delete e.currentTarget.dataset.drag;
            a.commitCustomHex();
          },
          onkeydown: (e) => {
            const st = e.shiftKey ? 10 : 1;
            let { s: sat, v } = hsv;
            if (e.key === "ArrowLeft") sat -= st;
            else if (e.key === "ArrowRight") sat += st;
            else if (e.key === "ArrowUp") v += st;
            else if (e.key === "ArrowDown") v -= st;
            else return;
            e.preventDefault();
            a.setHsv({ ...hsv, s: Math.min(100, Math.max(0, sat)), v: Math.min(100, Math.max(0, v)) });
          }
        },
        h("div", { class: "pc-sv__white" }),
        h("div", { class: "pc-sv__black" }),
        h("span", { class: "pc-sv__thumb", style: { left: `${hsv.s}%`, top: `${100 - hsv.v}%`, backgroundColor: hex } })
      ),
      h("input", { type: "range", min: 0, max: 360, value: hsv.h, class: "pc-hue", "aria-label": "Hue", oninput: (e) => a.setHsv({ ...hsv, h: Number(e.target.value) }), onpointerup: a.commitCustomHex }),
      h(
        "div",
        null,
        h("label", { class: "pc-overline-xs", for: "pc-pt-code" }, "Pantone code"),
        h("input", { id: "pc-pt-code", class: "pc-input", value: ui.code, spellcheck: false, autocomplete: "off", placeholder: "e.g. 286 C \xB7 18-1664 \xB7 Classic Blue", oninput: (e) => a.setPantoneCode(e.target.value) }),
        ui.code.trim() ? codeMatch ? h("p", { class: "pc-hint" }, h("span", { class: "pc-dot", style: { backgroundColor: codeMatch.hex } }), "Matched ", h("b", null, codeMatch.name), ` \xB7 ${codeMatch.code}`) : codeNearest ? h("p", { class: "pc-hint" }, h("span", { class: "pc-dot", style: { backgroundColor: codeNearest.hex } }), `No exact match for ${ui.code.trim()}. Closest in our library is `, h("b", null, codeNearest.name), ` \xB7 ${codeNearest.code}. `, h("button", { type: "button", class: "pc-link", onclick: () => a.setColor({ kind: "pantone", color: codeNearest }) }, "Use it")) : h("p", { class: "pc-hint" }, "No match in our library. Use the picker above, or the Pantone refs tab to browse.") : null
      ),
      h(
        "div",
        { class: "pc-row" },
        h("span", { class: "pc-current__chip", style: { backgroundColor: hex } }),
        h("div", { class: "pc-hexfield" }, h("span", { class: "pc-hexfield__hash" }, "#"), h("input", { class: "pc-input pc-input--hex", value: (ui.hexDraft ?? hex).replace(/^#/, "").toUpperCase(), maxlength: 6, spellcheck: false, inputmode: "text", placeholder: "0F4C81", "aria-label": "Hex color code", oninput: (e) => a.setHexDraft(e.target.value), onblur: a.commitCustomHex })),
        supportsEyeDropper ? h("button", { type: "button", class: "pc-iconbtn", title: "Pick a color from anywhere on your screen", "aria-label": "Pick a color from anywhere on your screen", onclick: a.eyeDrop }, svg(I.pipette)) : h("label", { class: "pc-iconbtn", title: "Open the system color picker" }, svg(I.pipette), h("input", { type: "color", value: hex, class: "pc-sr-only", "aria-label": "Open the system color picker", oninput: (e) => a.setCustomHex(e.target.value, true) }))
      ),
      ui.recent.length ? h("div", null, h("p", { class: "pc-overline-xs" }, "Recent"), h("div", { class: "pc-row pc-row--wrap" }, ui.recent.map((r) => h("button", { key: r, type: "button", class: cn("pc-recent", sel.kind === "custom" && sel.hex.toLowerCase() === r.toLowerCase() && "is-active"), style: { backgroundColor: r }, title: r.toUpperCase(), "aria-label": `Reuse ${r.toUpperCase()}`, onclick: () => a.setCustomHex(r, false) })))) : null,
      nearest ? h(
        "div",
        { class: "pc-nearest" },
        h("span", { class: "pc-current__chip", style: { backgroundColor: nearest.entry.hex } }),
        h("div", { class: "pc-current__text" }, h("p", { class: "pc-overline-xs" }, nearest.deltaE <= 1 ? "Exact Pantone" : `Nearest Pantone \xB7 \u0394E ${nearest.deltaE.toFixed(1)}`), h("p", { class: "pc-strong pc-truncate" }, nearest.entry.name), h("p", { class: "pc-mono-meta" }, nearest.entry.code)),
        nearest.deltaE <= 1 ? h("span", { class: "pc-pill is-active" }, svg(I.check), "Match") : h("button", { type: "button", class: "pc-pill", title: `Use ${nearest.entry.name} (${nearest.entry.code})`, onclick: () => a.setColor({ kind: "pantone", color: nearest.entry }) }, "Snap")
      ) : null
    ],
    h("p", { class: "pc-disclaimer" }, s.labels.colorDisclaimer)
  );
}
function renderDesignTab(s, a) {
  const layer = s.activeLayer, layers = s.sides[s.side];
  const hex = selectionHex(s.colorSel);
  const showStarters = layers.length === 0 && isApparelTopCategory(s.product.category) && s.side === "front";
  const addRow = h(
    "div",
    { class: "pc-addrow" },
    h("button", { type: "button", class: "pc-btn pc-btn--outline pc-btn--block", onclick: a.openFilePicker }, svg(I.upload), "Upload artwork"),
    h("button", { type: "button", class: "pc-btn pc-btn--outline pc-btn--block", onclick: a.addText }, svg(I.type), "Add a text layer")
  );
  if (!layer) {
    return h(
      "div",
      { class: "pc-tabbody" },
      addRow,
      h("p", { class: "pc-muted" }, layers.length > 0 ? "Pick a layer from the Layers rail to place, size, and restyle it." : `You can also drag a file straight onto the ${SIDE_LABEL[s.side].toLowerCase()} of the garment.`),
      layers.length === 0 ? h(
        "div",
        { class: "pc-divider-top" },
        h("p", { class: "pc-overline" }, "Start from"),
        h(
          "div",
          { class: cn("pc-starters", showStarters && "pc-starters--grid") },
          s.sampleLogo ? h("button", { type: "button", class: "pc-starter pc-starter--logo", "aria-label": "Use the sample logo to try the tools", title: "Drop a sample logo on to try the tools", onclick: a.addSampleLogo }, h("img", { src: s.sampleLogo, alt: "" })) : null,
          showStarters ? TEXT_STARTERS.map((st) => h("button", { key: st.id, type: "button", class: "pc-starter", style: { fontFamily: s.fonts.cssFamily(st.fontId) }, onclick: () => a.applyStarter(st) }, st.label)) : null
        )
      ) : null,
      renderFontLibrary(s, a)
    );
  }
  const spots = s.layout.spots, p = layer.placement;
  const isSpot = (sp) => p.area === sp.areaId && Math.abs(p.cx - sp.cx) < 0.02 && Math.abs(p.cy - sp.cy) < 0.02 && Math.abs(p.width - sp.width) < 0.02;
  const idx = layers.findIndex((l) => l.id === layer.id), multi = layers.length > 1;
  const tool = (icon, label, onclick, opts = {}) => h("button", { type: "button", class: cn("pc-tool", opts.destructive && "pc-tool--danger"), disabled: opts.disabled, title: opts.title ?? label, "aria-label": label, onclick }, svg(icon), opts.iconOnly ? null : h("span", null, label));
  return h(
    "div",
    { class: "pc-tabbody" },
    h("button", { type: "button", class: "pc-back", onclick: a.deselect }, svg(I.chevronLeft), layer.text ? "Text layer" : "Artwork layer", h("span", { class: "pc-back__meta" }, multi ? `${idx + 1} of ${layers.length}` : "")),
    h("p", { class: "pc-muted" }, "Drag on the garment to move; drag a corner to resize. Scroll to scale."),
    layer.text ? renderTextControls(s, a, layer) : null,
    h(
      "div",
      { class: "pc-group" },
      h("p", { class: "pc-overline" }, "Layer & placement"),
      h(
        "div",
        { class: "pc-tools" },
        tool(I.copy, "Duplicate", () => a.duplicateLayer(layer.id), { title: "Duplicate this layer (\u2318/Ctrl+D)" }),
        tool(I.flip, "Flip", () => a.updateActivePlacement((pl) => ({ ...pl, flipX: !pl.flipX }))),
        tool(I.reset, "Reset", a.resetPlacement),
        multi ? tool(I.sendBack, "Send back", () => a.reorderLayer(-1), { iconOnly: true, title: "Send layer back (\u2318/Ctrl+[)", disabled: idx <= 0 }) : null,
        multi ? tool(I.bringFront, "Bring forward", () => a.reorderLayer(1), { iconOnly: true, title: "Bring layer forward (\u2318/Ctrl+])", disabled: idx >= layers.length - 1 }) : null,
        tool(I.trash, "Remove", () => a.removeLayer(layer.id), { destructive: true, title: "Remove this layer" })
      ),
      h("p", { class: "pc-overline-xs" }, "Placement"),
      h("div", { class: "pc-spots" }, spots.map((sp) => h("button", { key: sp.id, type: "button", class: cn("pc-spot", isSpot(sp) && "is-active"), "aria-pressed": isSpot(sp), onclick: () => a.applySpot(sp) }, sp.label))),
      !layer.text ? renderSlider("Size", `${Math.round(100 * p.width)}%`, { min: 4, max: 92, step: 1, value: Math.round(100 * p.width), oninput: (e) => a.updateActivePlacement((pl) => ({ ...pl, width: Number(e.target.value) / 100 })) }) : null,
      renderSlider("Rotation", `${p.rotation}\xB0`, { min: -180, max: 180, step: 1, value: p.rotation, oninput: (e) => a.updateActivePlacement((pl) => ({ ...pl, rotation: Number(e.target.value) })), ondblclick: () => a.updateActivePlacement((pl) => ({ ...pl, rotation: 0 })) })
    ),
    !layer.text ? renderPreflight(s, a) : null,
    !layer.text && s.quality?.quality === "low" && !s.preflightReport ? h("p", { class: "pc-warn" }, "Heads up: the selected layer is low resolution at this print size. You can still submit (we'll flag it on your proof), or upload a larger file / shrink the print.") : null
  );
}
function renderSlider(label, value, props) {
  return h("div", { class: "pc-slider" }, h("label", { class: "pc-slider__label" }, label, h("span", { class: "pc-slider__value" }, value)), h("input", { type: "range", class: "pc-range", ...props }));
}
function renderTextControls(s, a, layer) {
  const t = layer.text, p = layer.placement;
  const swatchRow = (label, value, onPick, extra) => h(
    "div",
    { class: "pc-inkrow" },
    h("span", { class: "pc-inkrow__label" }, label),
    h("div", { class: "pc-inkrow__swatches", role: "radiogroup", "aria-label": `${label} color` }, extra?.leading, INK_COLORS.map((c) => {
      const on = value?.toLowerCase() === c.hex.toLowerCase();
      return h("button", { key: c.id, type: "button", role: "radio", "aria-checked": on, "aria-label": `${label} ${c.label}`, title: c.label, class: cn("pc-ink", on && "is-active"), onclick: () => onPick(c.hex) }, h("span", { class: "pc-ink__dot", style: { backgroundColor: c.hex } }));
    }), extra?.trailing)
  );
  return h(
    "div",
    { class: "pc-group" },
    h("p", { class: "pc-overline" }, "Text"),
    h("textarea", { class: "pc-input pc-textarea", rows: 2, maxlength: 120, placeholder: "Your text", value: t.content, oninput: (e) => a.setText(layer.id, { content: e.target.value }) }),
    h("p", { class: "pc-hint" }, "Tip: double-click the text on the garment to retype it."),
    h(
      "div",
      { class: "pc-row pc-row--between" },
      h("span", { class: "pc-strong-xs" }, "Text size"),
      h(
        "div",
        { class: "pc-stepper" },
        h("button", { type: "button", class: "pc-stepper__btn", "aria-label": "Smaller text", onclick: () => a.updateActivePlacement((pl) => ({ ...pl, width: Math.min(0.92, Math.max(0.04, pl.width / 1.12)) })) }, h("span", { style: { fontSize: ".7rem", fontWeight: 600 } }, "A")),
        h("input", { type: "number", class: "pc-stepper__input", "aria-label": "Text size percent", min: 4, max: 92, step: 1, value: Math.round(100 * p.width), onchange: (e) => {
          const v = Number(e.target.value);
          if (Number.isFinite(v)) a.updateActivePlacement((pl) => ({ ...pl, width: Math.min(0.92, Math.max(0.04, Math.round(v) / 100)) }));
        } }),
        h("span", { class: "pc-stepper__pct" }, "%"),
        h("button", { type: "button", class: "pc-stepper__btn", "aria-label": "Larger text", onclick: () => a.updateActivePlacement((pl) => ({ ...pl, width: Math.min(0.92, Math.max(0.04, pl.width * 1.12)) })) }, h("span", { style: { fontSize: "1rem", fontWeight: 600 } }, "A"))
      )
    ),
    h(
      "div",
      { class: "pc-fonts", role: "radiogroup", "aria-label": "Typeface" },
      s.fonts.list().map((f) => h("button", { key: f.id, type: "button", role: "radio", "aria-checked": t.fontId === f.id, class: cn("pc-font", t.fontId === f.id && "is-active", f.source === "uploaded" && "pc-font--uploaded"), title: f.name, style: { fontFamily: f.cssFamily, fontWeight: Number(f.weight) }, onclick: () => a.setText(layer.id, { fontId: f.id }) }, f.label)),
      s.allowFontUpload ? h("label", { class: "pc-font pc-font--add", title: "Upload a TTF, OTF, WOFF or WOFF2 font" }, svg(I.font), "Upload font", h("input", { type: "file", accept: FONT_ACCEPT_ATTR, class: "pc-sr-only", onchange: (e) => {
        const f = e.target.files?.[0];
        if (f) a.uploadFont(f, layer.id);
        e.target.value = "";
      } })) : null
    ),
    s.fonts.list().some((f) => f.source === "uploaded") ? h("p", { class: "pc-hint" }, "Uploaded fonts stay in this browser and travel with your request. ", h("button", { type: "button", class: "pc-link", onclick: () => a.setFontManager(!s.fontManager) }, s.fontManager ? "Hide" : "Manage")) : null,
    s.fontManager ? renderFontLibrary(s, a, true) : null,
    swatchRow("Ink", t.color, (hex) => a.setText(layer.id, { color: hex }), { trailing: h("label", { class: "pc-ink pc-ink--custom", title: "Custom ink color" }, h("span", { class: "pc-ink__dot pc-ink__dot--rainbow" }), h("input", { type: "color", value: t.color, class: "pc-sr-only", "aria-label": "Custom ink color", oninput: (e) => a.setText(layer.id, { color: e.target.value }) })) }),
    swatchRow("Outline", t.outline, (hex) => a.setText(layer.id, { outline: hex }), { leading: h("button", { type: "button", role: "radio", "aria-checked": !t.outline, "aria-label": "No outline", title: "No outline", class: cn("pc-ink", !t.outline && "is-active"), onclick: () => a.setText(layer.id, { outline: void 0 }) }, h("span", { class: "pc-ink__dot pc-ink__dot--none" })) }),
    h(
      "div",
      { class: "pc-two" },
      renderSlider("Curve", `${Math.round((t.arc ?? 0) * 100)}%`, { min: -100, max: 100, step: 5, value: Math.round((t.arc ?? 0) * 100), title: "Drag to arch the text; double-click to reset", oninput: (e) => a.setText(layer.id, { arc: Number(e.target.value) / 100 }), ondblclick: () => a.setText(layer.id, { arc: 0 }) }),
      renderSlider("Spacing", `${Math.round((t.spacing ?? 0) * 100)}%`, { min: 0, max: 40, step: 2, value: Math.round((t.spacing ?? 0) * 100), oninput: (e) => a.setText(layer.id, { spacing: Number(e.target.value) / 100 }) })
    )
  );
}
function renderFontLibrary(s, a, compact = false) {
  const uploaded = s.fonts.list().filter((f) => f.source === "uploaded");
  if (!s.allowFontUpload && uploaded.length === 0) return null;
  return h(
    "div",
    { class: cn("pc-fontlib", compact && "pc-fontlib--compact") },
    compact ? null : h("p", { class: "pc-overline" }, "Fonts"),
    compact ? null : h("p", { class: "pc-muted" }, `${s.fonts.list().length - uploaded.length} built-in typefaces. Upload your own TTF/OTF/WOFF to use it in text layers.`),
    uploaded.map((f) => h("div", { key: f.id, class: "pc-fontlib__row" }, h("span", { class: "pc-fontlib__sample", style: { fontFamily: f.cssFamily } }, f.name), h("button", { type: "button", class: "pc-iconbtn pc-iconbtn--sm", "aria-label": `Remove font ${f.name}`, title: "Remove", onclick: () => a.removeFont(f.id) }, svg(I.x)))),
    s.allowFontUpload && !compact ? h("label", { class: "pc-btn pc-btn--outline pc-btn--block" }, svg(I.font), "Upload a font", h("input", { type: "file", accept: FONT_ACCEPT_ATTR, class: "pc-sr-only", onchange: (e) => {
      const f = e.target.files?.[0];
      if (f) a.uploadFont(f);
      e.target.value = "";
    } })) : null
  );
}
var STATUS_CLASS = { ready: "is-ready", review: "is-review", fix: "is-fix" };
var SEV_CLASS = { pass: "is-pass", info: "is-info", warn: "is-warn", fail: "is-fail" };
function renderPreflight(s, a) {
  const r = s.preflightReport;
  return h(
    "div",
    { class: "pc-divider-top" },
    h(
      "div",
      { class: "pc-preflight", role: "status", "aria-live": "polite" },
      !r ? h("p", { class: "pc-preflight__pending" }, h("span", { class: "pc-preflight__dot is-pending" }), "Checking your artwork...") : [
        h("button", { type: "button", class: "pc-preflight__head", "aria-expanded": s.preflightOpen, onclick: () => a.setPreflightOpen(!s.preflightOpen) }, h("span", { class: cn("pc-preflight__dot", STATUS_CLASS[r.status]) }), h("span", { class: "pc-preflight__headline" }, r.headline), svg(I.chevronDown, s.preflightOpen ? "is-open" : "")),
        h("p", { class: "pc-preflight__facts" }, r.facts.join(" \xB7 ")),
        s.preflightOpen ? h(
          "div",
          { class: "pc-preflight__body" },
          h("ul", { class: "pc-preflight__list" }, r.findings.map((f) => h("li", { key: f.id, class: "pc-preflight__item" }, h("span", { class: cn("pc-preflight__dot", SEV_CLASS[f.severity]) }), h("div", null, h("p", { class: "pc-preflight__title" }, f.title), f.detail ? h("p", { class: "pc-preflight__detail" }, f.detail) : null)))),
          h("p", { class: "pc-overline-xs" }, "Method fit"),
          h("div", { class: "pc-row pc-row--wrap" }, r.methodFits.map((m) => h("span", { key: m.method, class: cn("pc-fit", `pc-fit--${m.status}`), title: m.note ?? "" }, m.label))),
          h("p", { class: "pc-hint" }, "Automatic check. We review every file by hand and confirm details on your proof.")
        ) : null
      ]
    ),
    s.contrastSuggestion ? h("button", { type: "button", class: "pc-suggest", onclick: () => a.setColor({ kind: "stocked", color: s.contrastSuggestion.color }) }, h("span", { class: "pc-dot pc-dot--lg", style: { backgroundColor: s.contrastSuggestion.hex } }), h("b", null, `Switch garment to ${s.contrastSuggestion.label}`), h("span", { class: "pc-muted" }, "so the art pops")) : null
  );
}
function renderQuoteTab(s, a) {
  const qty = s.quantityNumber;
  return h(
    "form",
    { class: "pc-tabbody", id: "pc-request-form", novalidate: true, onsubmit: (e) => {
      e.preventDefault();
      a.submit();
    } },
    h(
      "div",
      null,
      h("p", { class: "pc-overline" }, "Decoration"),
      h("div", { class: "pc-methods", role: "radiogroup", "aria-label": "Decoration method" }, METHODS.map((m) => h("button", { key: m.id, type: "button", role: "radio", "aria-checked": s.method === m.id, class: cn("pc-method", s.method === m.id && "is-active"), onclick: () => a.setMethod(m.id) }, h("span", { class: "pc-method__label" }, m.label), h("span", { class: "pc-method__note" }, m.note))))
    ),
    h(
      "div",
      null,
      h("label", { class: "pc-strong", for: "pc-qty" }, s.labels.quantityLabel),
      h("div", { class: "pc-qtys", role: "group", "aria-label": "Quantity shortcuts" }, PRICE_BREAKS.map((n) => h("button", { key: n, type: "button", class: cn("pc-qty", s.quantity === String(n) && "is-active"), "aria-pressed": s.quantity === String(n), onclick: () => a.setQuantity(String(n)) }, n))),
      h("p", { class: "pc-hint" }, s.labels.quantityHint),
      h("input", { id: "pc-qty", class: "pc-input", inputmode: "numeric", maxlength: 8, value: s.quantity, placeholder: "e.g. 48", oninput: (e) => a.setQuantity(e.target.value) })
    ),
    h(
      "div",
      null,
      h("label", { class: "pc-strong", for: "pc-notes" }, "Notes (optional)"),
      h("textarea", { id: "pc-notes", class: "pc-input pc-textarea", rows: 3, maxlength: 2e3, value: s.notes, placeholder: "Sizes, garment Pantone color, ink colors, deadline, placement...", oninput: (e) => a.setNotes(e.target.value) }),
      h("p", { class: "pc-hint" }, "Want a specific garment color? Add the Pantone code here and we'll match it on your proof.")
    ),
    s.labels.emailLabel ? h(
      "div",
      null,
      h("label", { class: "pc-strong", for: "pc-email" }, s.labels.emailLabel),
      h("input", { id: "pc-email", class: "pc-input", type: "email", inputmode: "email", autocomplete: "email", maxlength: 254, value: s.email, placeholder: "you@company.com", oninput: (e) => a.setEmail(e.target.value) }),
      h("p", { class: "pc-hint" }, s.labels.emailHint)
    ) : null,
    s.layerTotal > 0 ? h("p", { class: "pc-hint" }, `${s.layerTotal} ${s.layerTotal === 1 ? "placement" : "placements"} on this design${s.sides.back.length > 0 ? " (front + back)" : ""}.`) : h("p", { class: "pc-hint" }, s.labels.noArtworkHint),
    qty != null && qty > 0 && qty < 25 && (s.method === "screen" || s.method === "embroidery") ? h("p", { class: "pc-hint" }, "Setup is heavy at this qty \u2014 around 25 pieces the unit price starts making sense.") : null,
    s.designs.length > 1 ? h("p", { class: "pc-hint" }, `Sends the design on the stage (design ${s.activeDesignIndex + 1} of ${s.designs.length}) with its artwork. Your other designs stay saved here and are listed in the request notes so we can quote everything together.`) : null,
    h("button", { type: "submit", class: "pc-btn pc-btn--primary pc-btn--block pc-btn--lg pc-only-mobile", disabled: s.submitting, "aria-busy": s.submitting }, s.submitting ? [svg(I.loader, "pc-spin"), "Opening your request..."] : a.submitLabel()),
    h("p", { class: "pc-hint pc-divider-top" }, s.labels.submitFootnote)
  );
}
function renderMethods(s, a) {
  return h(
    "div",
    null,
    h("p", { class: "pc-overline" }, "Decoration"),
    h("div", { class: "pc-methods", role: "radiogroup", "aria-label": "Decoration method" }, METHODS.map((m) => h(
      "button",
      { key: m.id, type: "button", role: "radio", "aria-checked": s.method === m.id, class: cn("pc-method", s.method === m.id && "is-active"), onclick: () => a.setMethod(m.id) },
      h("span", { class: "pc-method__label" }, m.label),
      h("span", { class: "pc-method__note" }, m.note)
    )))
  );
}
function renderSizeRun(s, a) {
  if (!s.sizeRows.length) return null;
  return h(
    "div",
    null,
    h(
      "div",
      { class: "pc-row pc-row--between" },
      h("label", { class: "pc-strong" }, s.labels.sizeLabel),
      s.sizeTotal > 0 ? h("button", { type: "button", class: "pc-link", onclick: a.clearSizes }, "Clear") : null
    ),
    h("div", { class: "pc-sizes" }, s.sizeRows.map((r) => h(
      "div",
      { key: r.size, class: cn("pc-size", r.quantity > 0 && "is-filled", !r.available && "is-oos") },
      h("span", { class: "pc-size__label" }, r.size),
      h(
        "div",
        { class: "pc-size__ctrl" },
        h("button", { type: "button", class: "pc-size__btn", "aria-label": `One fewer ${r.size}`, disabled: r.quantity === 0, onclick: () => a.bumpSize(r.size, -1) }, "\u2212"),
        h("input", { type: "number", class: "pc-size__input", min: 0, max: 9999, inputmode: "numeric", "aria-label": `Quantity for size ${r.size}`, value: String(r.quantity), oninput: (e) => a.setSize(r.size, e.target.value) }),
        h("button", { type: "button", class: "pc-size__btn", "aria-label": `One more ${r.size}`, disabled: !r.available, onclick: () => a.bumpSize(r.size, 1) }, "+")
      ),
      h("span", { class: "pc-size__price" }, r.available ? r.unit > 0 ? s.money(r.unit) : "\u2014" : "Sold out")
    ))),
    h(
      "div",
      { class: "pc-row pc-row--wrap pc-quickfill" },
      h("span", { class: "pc-hint" }, "Quick fill:"),
      PRICE_BREAKS.filter((n) => n >= (s.price.quantity > 0 ? 0 : 0)).map((n) => h("button", { key: n, type: "button", class: "pc-qty pc-qty--sm", onclick: () => a.spreadSizes(n) }, n))
    ),
    h("p", { class: "pc-hint" }, s.labels.sizeHint)
  );
}
function renderPriceBreakdown(s, a) {
  const p = s.price;
  if (p.quantity === 0) return h("p", { class: "pc-muted pc-divider-top" }, "Add quantities above to see pricing.");
  const group = (kind) => p.lines.filter((l) => l.kind === kind && !(kind === "garment" && !(l.unit > 0)));
  const row = (l) => h(
    "div",
    { key: l.id, class: "pc-price__row" },
    h("span", { class: "pc-price__label" }, l.label, l.meta ? h("span", { class: "pc-price__meta" }, l.meta) : null),
    h("span", { class: "pc-price__unit" }, l.oneOff ? "one-off" : `${s.money(l.unit)} \xD7 ${l.qty}`),
    h("span", { class: "pc-price__total" }, s.money(l.total))
  );
  return h(
    "div",
    { class: "pc-price" },
    h("p", { class: "pc-overline" }, "Price"),
    [...group("garment"), ...group("decoration"), ...group("surcharge"), ...group("setup")].map(row),
    h(
      "div",
      { class: "pc-price__sum" },
      h("span", null, "Subtotal"),
      h("span", { class: "pc-price__big" }, s.money(p.subtotal))
    ),
    h("p", { class: "pc-price__unitline" }, `${s.money(p.unitPrice)} per piece \xB7 ${p.quantity} ${p.quantity === 1 ? "piece" : "pieces"}`),
    p.nextBreak ? h("p", { class: "pc-price__break" }, `Order ${p.nextBreak.qty}+ and each piece drops to ${s.money(p.nextBreak.unitPrice)} \u2014 saving ${s.money(p.nextBreak.saving)} each.`) : null,
    p.notes.map((n, i) => h("p", { key: `n${i}`, class: cn("pc-hint", n.level === "warn" && "pc-hint--warn") }, n.text)),
    p.blockers.map((b) => h(
      "p",
      { key: b.id, class: "pc-warn" },
      b.text,
      b.quoteInstead ? [" ", h("button", { type: "button", class: "pc-link", onclick: a.requestQuoteInstead }, "Request a quote instead")] : null
    ))
  );
}
function renderOrderTab(s, a) {
  return h(
    "form",
    { class: "pc-tabbody", id: "pc-request-form", novalidate: true, onsubmit: (e) => {
      e.preventDefault();
      a.submit();
    } },
    renderMethods(s, a),
    renderSizeRun(s, a),
    renderPriceBreakdown(s, a),
    h(
      "div",
      null,
      h("label", { class: "pc-strong", for: "pc-notes" }, "Notes for production (optional)"),
      h("textarea", { id: "pc-notes", class: "pc-input pc-textarea", rows: 2, maxlength: 2e3, value: s.notes, placeholder: "Deadline, thread colours, packing\u2026", oninput: (e) => a.setNotes(e.target.value) })
    ),
    h(
      "button",
      { type: "submit", class: "pc-btn pc-btn--primary pc-btn--block pc-btn--lg pc-only-mobile", disabled: s.submitting || s.price.quantity === 0 || s.price.blockers.length > 0, "aria-busy": s.submitting },
      s.submitting ? [svg(I.loader, "pc-spin"), s.labels.addingToCart] : [svg(I.cart), `${s.labels.addToCart} \xB7 ${s.money(s.price.subtotal)}`]
    ),
    h("p", { class: "pc-hint pc-divider-top" }, s.labels.cartFootnote ?? "Artwork is checked by hand before production. We email a proof before anything is printed.")
  );
}
function renderBottomBar(s, a) {
  const hex = selectionHex(s.colorSel);
  return h(
    "div",
    { class: "pc-bar" },
    h(
      "button",
      { type: "button", class: "pc-bar__chip", onclick: () => a.jumpTo("blank") },
      h("span", { class: "pc-bar__thumb" }, h("img", { src: s.mockup.front, alt: "", draggable: false })),
      h("span", { class: "pc-bar__text" }, h("span", { class: "pc-bar__title" }, s.product.name), h("span", { class: "pc-bar__meta" }, `${s.product.styleNumber} \xB7 Change blank`))
    ),
    h("span", { class: "pc-bar__sep" }),
    h(
      "button",
      { type: "button", class: "pc-bar__chip", onclick: () => a.jumpTo("color") },
      h("span", { class: "pc-dot pc-dot--xl", style: { backgroundColor: hex } }),
      h("span", { class: "pc-bar__text" }, h("span", { class: "pc-bar__title" }, selectionName(s.colorSel)), h("span", { class: "pc-bar__meta" }, "Change color"))
    ),
    s.mode === "cart" && s.price.quantity > 0 ? h(
      "button",
      { type: "button", class: "pc-bar__chip pc-bar__chip--price", onclick: () => a.jumpTo("quote") },
      h(
        "span",
        { class: "pc-bar__text" },
        h("span", { class: "pc-bar__title" }, `${s.money(s.price.subtotal)}`),
        h("span", { class: "pc-bar__meta" }, `${s.price.quantity} pcs \xB7 ${s.money(s.price.unitPrice)} each`)
      )
    ) : null,
    h(
      "div",
      { class: "pc-bar__actions" },
      h(
        "button",
        { type: "button", class: "pc-btn pc-btn--outline", disabled: s.share === "working" || s.layerTotal === 0, "aria-busy": s.share === "working", "aria-label": "Share a link to this design", title: s.layerTotal === 0 ? "Add artwork or text first, then share the design" : "Copy a link to this design", onclick: a.share },
        svg(s.share === "working" ? I.loader : I.link, s.share === "working" ? "pc-spin" : ""),
        h("span", { class: "pc-hide-sm" }, s.share === "copied" ? "Link copied" : s.share === "failed" ? "Try again" : s.share === "working" ? "Sharing..." : "Share")
      ),
      h(
        "button",
        { type: "button", class: "pc-btn pc-btn--outline", disabled: s.saving || s.layerTotal === 0, "aria-busy": s.saving, "aria-label": "Download mockup", title: s.layerTotal === 0 ? "Add artwork or text first, then download the mockup" : "Download a PNG mockup of this design (every side)", onclick: () => a.download("studio") },
        svg(s.saving ? I.loader : I.download, s.saving ? "pc-spin" : ""),
        h("span", { class: "pc-hide-sm" }, s.saving ? "Saving..." : "Download")
      ),
      h("button", { type: "button", class: "pc-btn pc-btn--outline", onclick: a.openPreview }, svg(I.eye), "Preview"),
      h(
        "button",
        { type: "submit", form: "pc-request-form", class: "pc-btn pc-btn--primary pc-btn--submit", disabled: s.submitting || s.mode === "cart" && (s.price.quantity === 0 || s.price.blockers.length > 0), "aria-busy": s.submitting, onclick: (e) => {
          e.preventDefault();
          a.submit();
        } },
        s.submitting ? [svg(I.loader, "pc-spin"), s.mode === "cart" ? s.labels.addingToCart : "Opening your request..."] : s.mode === "cart" ? [svg(I.cart), a.submitLabel(), s.price.quantity > 0 ? h("span", { class: "pc-btn__price" }, s.money(s.price.subtotal)) : null] : a.submitLabel()
      )
    ),
    s.shareExpires && s.share !== "working" ? h(
      "p",
      { class: "pc-bar__status", role: "status" },
      s.shareLink ? ["Copy your share link: ", h("input", { class: "pc-input pc-input--share", readonly: true, value: s.shareLink, "aria-label": "Share link", onclick: (e) => e.target.select() })] : "Link copied. Anyone with this link can view your design",
      s.shareLink ? null : s.shareExpires === true ? "." : [" until ", h("b", null, s.shareExpires), ", then it stops working."]
    ) : null
  );
}
function renderPreviewModal(s, a) {
  if (!s.preview.open) return null;
  const p = s.preview;
  return h(
    "div",
    { class: "pc-modal", role: "dialog", "aria-modal": "true", "aria-labelledby": "pc-preview-title", onclick: a.closePreview },
    h(
      "div",
      { class: "pc-modal__card", tabindex: -1, onclick: (e) => e.stopPropagation() },
      h(
        "div",
        { class: "pc-row pc-row--between pc-row--top" },
        h("div", null, h("h2", { class: "pc-modal__title", id: "pc-preview-title" }, "Final preview"), h("p", { class: "pc-muted" }, `${s.product.name} \xB7 ${selectionName(s.colorSel)}. We confirm exact placement and colors on your proof.`)),
        h("button", { type: "button", class: "pc-iconbtn pc-iconbtn--ghost", "aria-label": "Close preview", onclick: a.closePreview }, svg(I.x))
      ),
      h(
        "div",
        { class: "pc-modal__body", "aria-live": "polite" },
        p.busy ? h("p", { class: "pc-muted pc-text-center pc-modal__status", role: "status" }, svg(I.loader, "pc-spin"), "Rendering your mockup...") : p.failed || p.images.length === 0 && s.layerTotal > 0 ? h("div", { class: "pc-text-center pc-modal__status", role: "alert" }, h("p", { class: "pc-strong" }, "Couldn't render the preview."), h("p", { class: "pc-muted" }, "Your design is safe on the stage. This is usually a one-off, so try again."), h("button", { type: "button", class: "pc-btn pc-btn--outline", onclick: a.openPreview }, svg(I.reset), "Try again")) : p.images.length === 0 ? h("p", { class: "pc-muted pc-text-center pc-modal__status" }, "Add at least one image to preview your design.") : h("div", { class: cn("pc-modal__grid", p.images.length > 1 && "pc-modal__grid--2") }, p.images.map((img) => h("figure", { key: img.label, class: "pc-modal__fig" }, h("div", { class: "pc-modal__imgbox" }, h("img", { src: img.url, alt: `${img.label} mockup` })), h("figcaption", { class: "pc-modal__cap" }, img.label))))
      ),
      h(
        "div",
        { class: "pc-modal__foot" },
        h("button", { type: "button", class: "pc-btn pc-btn--outline", disabled: p.images.length === 0 || s.saving, onclick: () => a.download("preview") }, svg(s.saving ? I.loader : I.download, s.saving ? "pc-spin" : ""), s.saving ? "Preparing..." : "Download mockup"),
        h("button", { type: "button", class: "pc-btn pc-btn--primary", onclick: a.closePreview }, "Looks good")
      )
    )
  );
}
function renderResultModal(s, a) {
  if (!s.result) return null;
  const r = s.result;
  return h(
    "div",
    { class: "pc-modal", role: "dialog", "aria-modal": "true", onclick: a.closeResult },
    h(
      "div",
      { class: "pc-modal__card pc-modal__card--sm", onclick: (e) => e.stopPropagation() },
      h("div", { class: "pc-row pc-row--between pc-row--top" }, h("h2", { class: "pc-modal__title" }, r.title), h("button", { type: "button", class: "pc-iconbtn pc-iconbtn--ghost", "aria-label": "Close", onclick: a.closeResult }, svg(I.x))),
      h("p", { class: "pc-muted" }, r.message),
      r.previewUrl ? h("div", { class: "pc-modal__imgbox pc-modal__imgbox--mt" }, h("img", { src: r.previewUrl, alt: "Design mockup" })) : null,
      r.details ? h("pre", { class: "pc-code" }, r.details) : null,
      h("div", { class: "pc-modal__foot" }, h("button", { type: "button", class: "pc-btn pc-btn--primary", onclick: a.closeResult }, "Done"))
    )
  );
}
function renderToasts(s, a) {
  return h(
    "div",
    { class: "pc-toasts", role: "status", "aria-live": "polite", "aria-atomic": "true" },
    s.undo.design ? h("div", { class: "pc-toast" }, h("span", null, "Design removed"), h("button", { type: "button", class: "pc-toast__btn", onclick: a.undoRemoveDesign }, svg(I.reset), "Undo")) : null,
    s.undo.layer ? h("div", { class: "pc-toast" }, h("span", null, "Layer removed"), h("button", { type: "button", class: "pc-toast__btn", onclick: a.undoRemoveLayer }, svg(I.reset), "Undo")) : null
  );
}
function renderAlerts(s, a) {
  if (!s.error && !s.largeArtNotice) return null;
  return h(
    "div",
    { class: "pc-alerts" },
    s.error ? h("p", { class: "pc-alert", role: "alert" }, h("span", null, s.error), h("button", { type: "button", class: "pc-notice__x", "aria-label": "Dismiss", onclick: a.dismissError }, svg(I.x))) : null,
    s.largeArtNotice ? h("div", { class: "pc-notice", role: "status" }, h("span", null, "Heads up: large artwork stays in this session but is not saved to your draft, so it clears if you refresh. Submit your request to keep it safe."), h("button", { type: "button", class: "pc-notice__x", "aria-label": "Dismiss", onclick: a.dismissLargeArt }, svg(I.x))) : null
  );
}
function renderSideTabs(s, a) {
  if (s.sideList.length < 2) return null;
  return h("div", { class: "pc-sides", role: "tablist", "aria-label": "Garment side" }, s.sideList.map((side) => {
    const active = side === s.side, n = s.sides[side].length;
    return h(
      "button",
      {
        key: side,
        type: "button",
        role: "tab",
        id: `pc-side-${side}`,
        "aria-selected": active,
        tabindex: active ? 0 : -1,
        class: cn("pc-side", active && "is-active"),
        onclick: () => a.setSide(side),
        onkeydown: (e) => {
          if (!["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown"].includes(e.key)) return;
          e.preventDefault();
          const i = s.sideList.indexOf(side);
          const next = s.sideList[(i + (e.key === "ArrowLeft" || e.key === "ArrowUp" ? -1 : 1) + s.sideList.length) % s.sideList.length];
          a.setSide(next);
          document.getElementById(`pc-side-${next}`)?.focus();
        }
      },
      h(
        "span",
        { class: "pc-side__stage" },
        h("img", { src: s.mockup[side] ?? s.mockup.front, alt: "", draggable: false, class: cn(s.mirrored[side] && "is-mirrored") }),
        s.sides[side].map((l) => {
          const aspect = l.artwork.naturalW > 0 ? l.artwork.naturalH / l.artwork.naturalW : 1, hf = l.placement.width * aspect;
          return h("img", { key: l.id, src: l.artwork.url, alt: "", draggable: false, class: "pc-side__layer", style: { left: `${(l.placement.cx - l.placement.width / 2) * 100}%`, top: `${(l.placement.cy - hf / 2) * 100}%`, width: `${100 * l.placement.width}%`, height: `${100 * hf}%`, transform: `rotate(${l.placement.rotation}deg)${l.placement.flipX ? " scaleX(-1)" : ""}` } });
        })
      ),
      h("span", { class: "pc-side__label" }, SIDE_LABEL[side]),
      n > 0 ? h("span", { class: "pc-side__count" }, n) : null
    );
  }));
}
function renderLayersRail(s, a) {
  const layers = s.sides[s.side];
  return h(
    "div",
    { class: "pc-layers" },
    h("p", { class: "pc-overline pc-layers__label" }, "Layers"),
    h(
      "div",
      { class: "pc-layers__list" },
      layers.map((l, i) => {
        const active = l.id === s.activeLayerId;
        return h(
          "div",
          { key: l.id, class: "pc-layerchip-wrap" },
          h("button", { type: "button", class: cn("pc-layerchip", active && "is-active"), "aria-pressed": active, "aria-label": `Select layer ${i + 1}${l.text ? " (text)" : ""}`, style: l.text && relativeLuminance(l.text.color) > 0.6 ? { backgroundColor: "#3f3f46" } : null, onclick: () => a.selectLayer(l.id) }, h("img", { src: l.artwork.url, alt: "", draggable: false })),
          h("button", { type: "button", class: "pc-layerchip__x", "aria-label": `Remove layer ${i + 1}`, onclick: () => a.removeLayer(l.id) }, svg(I.x))
        );
      }),
      h("button", { type: "button", class: "pc-layerchip pc-layerchip--add", "aria-label": "Add artwork files", onclick: a.openFilePicker }, svg(I.imagePlus)),
      h("button", { type: "button", class: "pc-layerchip pc-layerchip--add", "aria-label": "Add a text layer", onclick: a.addText }, svg(I.type))
    )
  );
}

// piobox-customizer/core/recolor-core.js
var RECOLOR_ALPHA_OUTPUT_CUTOFF = 24;
var RECOLOR_MAX_EDGE = 1600;
function cornerAverage(data, w, h2) {
  const px = (x, y) => {
    const i = (y * w + x) * 4;
    return [data[i], data[i + 1], data[i + 2]];
  };
  const c = [px(0, 0), px(w - 1, 0), px(0, h2 - 1), px(w - 1, h2 - 1)];
  return [0, 1, 2].map((k) => c.reduce((s, v) => s + v[k], 0) / 4);
}
function distToRef(data, i, ref) {
  const o = 4 * i;
  const dr = data[o] - ref[0], dg = data[o + 1] - ref[1], db = data[o + 2] - ref[2];
  return Math.sqrt(dr * dr + dg * dg + db * db);
}
function bbox(mask, w, h2) {
  let left = w, top = h2, right = -1, bottom = -1, count = 0;
  for (let i = 0; i < mask.length; i++) {
    if (mask[i] === 0) continue;
    const x = i % w, y = i / w | 0;
    if (x < left) left = x;
    if (x > right) right = x;
    if (y < top) top = y;
    if (y > bottom) bottom = y;
    count++;
  }
  return count > 0 ? { left, top, right, bottom, count } : null;
}
function majority3x3(mask, w, h2) {
  const out = new Uint8ClampedArray(mask.length);
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    let on = 0, n = 0;
    for (let dy = -1; dy <= 1; dy++) for (let dx = -1; dx <= 1; dx++) {
      const xx = x + dx, yy = y + dy;
      if (xx < 0 || yy < 0 || xx >= w || yy >= h2) continue;
      n++;
      if (mask[yy * w + xx] > 0) on++;
    }
    out[y * w + x] = 255 * (2 * on > n);
  }
  return out;
}
function fillHoles(mask, w, h2) {
  const total = w * h2, seen = new Uint8Array(total), stack = [];
  const push = (x, y) => {
    if (x < 0 || y < 0 || x >= w || y >= h2) return;
    const i = y * w + x;
    if (seen[i] || mask[i] !== 0) return;
    seen[i] = 1;
    stack.push(i);
  };
  for (let x = 0; x < w; x++) {
    push(x, 0);
    push(x, h2 - 1);
  }
  for (let y = 0; y < h2; y++) {
    push(0, y);
    push(w - 1, y);
  }
  while (stack.length) {
    const i = stack.pop(), x = i % w, y = i / w | 0;
    push(x + 1, y);
    push(x - 1, y);
    push(x, y + 1);
    push(x, y - 1);
  }
  const out = Uint8ClampedArray.from(mask);
  for (let i = 0; i < total; i++) if (mask[i] === 0 && !seen[i]) out[i] = 255;
  return out;
}
function distanceTransform(bin, w, h2, borderIsZero) {
  const n = w * h2, d = new Int32Array(n);
  for (let i = 0; i < n; i++) d[i] = 1073741823 * (bin[i] ? 0 : 1);
  if (borderIsZero) {
    for (let x = 0; x < w; x++) {
      const b = (h2 - 1) * w + x;
      if (d[x] > 3) d[x] = 3;
      if (d[b] > 3) d[b] = 3;
    }
    for (let y = 0; y < h2; y++) {
      const l = y * w, r = l + w - 1;
      if (d[l] > 3) d[l] = 3;
      if (d[r] > 3) d[r] = 3;
    }
  }
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    const i = y * w + x;
    let v = d[i];
    if (x > 0 && d[i - 1] + 3 < v) v = d[i - 1] + 3;
    if (y > 0) {
      if (d[i - w] + 3 < v) v = d[i - w] + 3;
      if (x > 0 && d[i - w - 1] + 4 < v) v = d[i - w - 1] + 4;
      if (x < w - 1 && d[i - w + 1] + 4 < v) v = d[i - w + 1] + 4;
    }
    d[i] = v;
  }
  for (let y = h2 - 1; y >= 0; y--) for (let x = w - 1; x >= 0; x--) {
    const i = y * w + x;
    let v = d[i];
    if (x < w - 1 && d[i + 1] + 3 < v) v = d[i + 1] + 3;
    if (y < h2 - 1) {
      if (d[i + w] + 3 < v) v = d[i + w] + 3;
      if (x < w - 1 && d[i + w + 1] + 4 < v) v = d[i + w + 1] + 4;
      if (x > 0 && d[i + w - 1] + 4 < v) v = d[i + w - 1] + 4;
    }
    d[i] = v;
  }
  return d;
}
function erode(mask, w, h2, iterations) {
  let cur = mask;
  for (let it = 0; it < iterations; it++) {
    const next = new Uint8ClampedArray(cur.length);
    for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
      let on = cur[y * w + x] > 0;
      for (let dy = -1; dy <= 1 && on; dy++) for (let dx = -1; dx <= 1; dx++) {
        const xx = x + dx, yy = y + dy;
        if (xx < 0 || yy < 0 || xx >= w || yy >= h2 || cur[yy * w + xx] === 0) {
          on = false;
          break;
        }
      }
      next[y * w + x] = on ? 255 : 0;
    }
    cur = next;
  }
  return cur;
}
function dilate(mask, w, h2, iterations) {
  let cur = mask;
  for (let it = 0; it < iterations; it++) {
    const next = new Uint8ClampedArray(cur.length);
    for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
      let on = cur[y * w + x] > 0;
      for (let dy = -1; dy <= 1 && !on; dy++) for (let dx = -1; dx <= 1; dx++) {
        const xx = x + dx, yy = y + dy;
        if (xx >= 0 && yy >= 0 && xx < w && yy < h2 && cur[yy * w + xx] > 0) {
          on = true;
          break;
        }
      }
      next[y * w + x] = on ? 255 : 0;
    }
    cur = next;
  }
  return cur;
}
function boxBlur(src, w, h2, radius) {
  const size = 2 * radius + 1;
  const tmp = new Float32Array(src.length), out = new Float32Array(src.length);
  const cx = (x) => x < 0 ? 0 : x >= w ? w - 1 : x, cy = (y) => y < 0 ? 0 : y >= h2 ? h2 - 1 : y;
  for (let y = 0; y < h2; y++) {
    const row = y * w;
    let acc = 0;
    for (let k = -radius; k <= radius; k++) acc += src[row + cx(k)];
    for (let x = 0; x < w; x++) {
      tmp[row + x] = acc / size;
      acc += src[row + cx(x + radius + 1)] - src[row + cx(x - radius)];
    }
  }
  for (let x = 0; x < w; x++) {
    let acc = 0;
    for (let k = -radius; k <= radius; k++) acc += tmp[cy(k) * w + x];
    for (let y = 0; y < h2; y++) {
      out[y * w + x] = acc / size;
      acc += tmp[cy(y + radius + 1) * w + x] - tmp[cy(y - radius) * w + x];
    }
  }
  return out;
}
function softKnee(v, knee, limit) {
  const a = Math.abs(v);
  return limit * Math.tanh((a <= knee ? 0 : Math.sign(v) * (a - knee)) / limit);
}
function inpaintOutward(values, mask, w, h2, iterations) {
  const n = w * h2, acc = Float32Array.from(values, (v) => v), filled = new Uint8Array(n);
  for (let i = 0; i < n; i++) filled[i] = mask[i] > 0 ? 1 : 0;
  for (let it = 0; it < iterations; it++) {
    const snapshot = filled.slice();
    for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
      const i = y * w + x;
      if (snapshot[i]) continue;
      let sum = 0, cnt = 0;
      for (let dy = -1; dy <= 1; dy++) {
        const yy = y + dy;
        if (yy < 0 || yy >= h2) continue;
        for (let dx = -1; dx <= 1; dx++) {
          const xx = x + dx;
          if (xx < 0 || xx >= w) continue;
          const j = yy * w + xx;
          if (snapshot[j]) {
            sum += acc[j];
            cnt++;
          }
        }
      }
      if (cnt > 0) {
        acc[i] = sum / cnt;
        filled[i] = 1;
      }
    }
  }
  const out = new Uint8ClampedArray(n);
  for (let i = 0; i < n; i++) out[i] = Math.round(acc[i]);
  return out;
}
function blur3(values, w, h2, horizontal) {
  const out = new Float32Array(values.length);
  if (horizontal) {
    for (let y = 0; y < h2; y++) {
      const row = y * w;
      for (let x = 0; x < w; x++) {
        const a = x > 0 ? values[row + x - 1] : values[row + x], b = values[row + x], c = x < w - 1 ? values[row + x + 1] : values[row + x];
        out[row + x] = (a + b + c) / 3;
      }
    }
  } else {
    for (let x = 0; x < w; x++) for (let y = 0; y < h2; y++) {
      const a = y > 0 ? values[(y - 1) * w + x] : values[y * w + x], b = values[y * w + x], c = y < h2 - 1 ? values[(y + 1) * w + x] : values[y * w + x];
      out[y * w + x] = (a + b + c) / 3;
    }
  }
  return out;
}
function largestComponent(mask, w, h2) {
  const seen = new Uint8Array(mask.length), stack = [];
  let best = [];
  for (let s = 0; s < mask.length; s++) {
    if (seen[s] || mask[s] === 0) continue;
    const comp = [];
    stack.push(s);
    seen[s] = 1;
    while (stack.length) {
      const i = stack.pop();
      comp.push(i);
      const x = i % w, y = i / w | 0;
      for (let dy = -1; dy <= 1; dy++) {
        const yy = y + dy;
        if (yy < 0 || yy >= h2) continue;
        for (let dx = -1; dx <= 1; dx++) {
          const xx = x + dx;
          if (xx < 0 || xx >= w) continue;
          const j = yy * w + xx;
          if (!seen[j] && mask[j] > 0) {
            seen[j] = 1;
            stack.push(j);
          }
        }
      }
    }
    if (comp.length > best.length) best = comp;
  }
  const out = new Uint8ClampedArray(mask.length);
  for (const i of best) out[i] = 255;
  return out;
}
function dropSmallComponents(mask, w, h2) {
  const n = w * h2, minSize = Math.max(24, Math.round(4e-4 * n));
  const seen = new Uint8Array(n), stack = [], comp = [], out = Uint8ClampedArray.from(mask);
  for (let s = 0; s < n; s++) {
    if (seen[s] || mask[s] === 0) continue;
    comp.length = 0;
    stack.length = 0;
    stack.push(s);
    seen[s] = 1;
    while (stack.length) {
      const i = stack.pop();
      comp.push(i);
      const x = i % w, y = i / w | 0;
      for (let dy = -1; dy <= 1; dy++) {
        const yy = y + dy;
        if (yy < 0 || yy >= h2) continue;
        for (let dx = -1; dx <= 1; dx++) {
          const xx = x + dx;
          if (xx < 0 || xx >= w) continue;
          const j = yy * w + xx;
          if (!seen[j] && mask[j] > 0) {
            seen[j] = 1;
            stack.push(j);
          }
        }
      }
    }
    if (comp.length <= minSize) for (const i of comp) out[i] = 0;
  }
  return out;
}
function carveBackgroundPockets(mask, data, w, h2) {
  const n = w * h2, ref = cornerAverage(data, w, h2);
  const isBg = (i) => {
    const o = 4 * i;
    const dr = data[o] - ref[0], dg = data[o + 1] - ref[1], db = data[o + 2] - ref[2];
    return Math.sqrt(dr * dr + dg * dg + db * db) < 5;
  };
  const minPixels = Math.max(64, Math.round(6e-4 * n)), minDepth = Math.max(6, Math.round(0.017 * Math.max(w, h2)));
  const out = Uint8ClampedArray.from(mask), seen = new Uint8Array(n), inComp = new Uint8Array(n), stack = [], comp = [];
  const depthOf = () => {
    const dist = new Int32Array(comp.length).fill(-1), idx = /* @__PURE__ */ new Map();
    for (let k = 0; k < comp.length; k++) idx.set(comp[k], k);
    const queue = [];
    for (let k = 0; k < comp.length; k++) {
      const i = comp[k], x = i % w, y = i / w | 0;
      const interior = x !== 0 && y !== 0 && x !== w - 1 && y !== h2 - 1 && inComp[i - 1] && inComp[i + 1] && inComp[i - w] && inComp[i + w];
      if (!interior) {
        dist[k] = 1;
        queue.push(k);
      }
    }
    let maxD = 1;
    for (let q = 0; q < queue.length; q++) {
      const k = queue[q], i = comp[k], dcur = dist[k];
      if (dcur > maxD) maxD = dcur;
      if (maxD >= minDepth) break;
      const x = i % w, y = i / w | 0;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1]) {
        if (j < 0 || !inComp[j]) continue;
        const kk = idx.get(j);
        if (dist[kk] === -1) {
          dist[kk] = dcur + 1;
          queue.push(kk);
        }
      }
    }
    return maxD;
  };
  for (let s = 0; s < n; s++) {
    if (seen[s] || out[s] === 0 || !isBg(s)) continue;
    comp.length = 0;
    stack.length = 0;
    stack.push(s);
    seen[s] = 1;
    while (stack.length) {
      const i = stack.pop();
      comp.push(i);
      inComp[i] = 1;
      const x = i % w, y = i / w | 0;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1])
        if (j >= 0 && !seen[j] && out[j] !== 0 && isBg(j)) {
          seen[j] = 1;
          stack.push(j);
        }
    }
    if (comp.length >= minPixels && depthOf() >= minDepth) for (const i of comp) out[i] = 0;
    for (const i of comp) inComp[i] = 0;
  }
  return out;
}
function reclaimInnerPockets(mask, data, w, h2) {
  const n = w * h2, threshold = 3 * Math.max(10, Math.round(0.019 * Math.max(w, h2)));
  const bin = new Uint8Array(n);
  for (let i = 0; i < n; i++) bin[i] = mask[i] > 0 ? 1 : 0;
  const dOut = distanceTransform(bin, w, h2, false);
  const far = new Uint8Array(n);
  for (let i = 0; i < n; i++) far[i] = dOut[i] <= threshold ? 0 : 1;
  const dIn = distanceTransform(far, w, h2, true);
  const ref = cornerAverage(data, w, h2), refLuma = 0.2126 * ref[0] + 0.7152 * ref[1] + 0.0722 * ref[2];
  const maxPixels = Math.max(64, Math.round(8e-4 * n));
  const out = Uint8ClampedArray.from(mask), seen = new Uint8Array(n), stack = [], comp = [];
  const candidate = (i) => !bin[i] && dIn[i] > threshold;
  for (let s = 0; s < n; s++) {
    if (seen[s] || !candidate(s)) continue;
    comp.length = 0;
    stack.length = 0;
    stack.push(s);
    seen[s] = 1;
    let bright = 0;
    while (stack.length) {
      const i = stack.pop();
      comp.push(i);
      const o = 4 * i;
      if (0.2126 * data[o] + 0.7152 * data[o + 1] + 0.0722 * data[o + 2] >= refLuma - 14) bright++;
      const x = i % w, y = i / w | 0;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1])
        if (j >= 0 && !seen[j] && candidate(j)) {
          seen[j] = 1;
          stack.push(j);
        }
    }
    if (comp.length <= maxPixels && bright >= 0.8 * comp.length) for (const i of comp) out[i] = 255;
  }
  return out;
}
function trimBackgroundEdge(mask, data, w, h2) {
  const ref = cornerAverage(data, w, h2);
  let cur = Uint8ClampedArray.from(mask);
  for (let it = 0; it < 2; it++) {
    const next = Uint8ClampedArray.from(cur);
    for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
      const i = y * w + x;
      if (cur[i] === 0 || distToRef(data, i, ref) >= 10) continue;
      if (x === 0 || y === 0 || x === w - 1 || y === h2 - 1 || cur[i - 1] === 0 || cur[i + 1] === 0 || cur[i - w] === 0 || cur[i + w] === 0) next[i] = 0;
    }
    cur = next;
  }
  return cur;
}
function darkMaskFromReference(maskData, w, h2) {
  const n = w * h2, ref = cornerAverage(maskData, w, h2);
  const refLuma = 0.2126 * ref[0] + 0.7152 * ref[1] + 0.0722 * ref[2];
  const dark = new Uint8ClampedArray(n);
  for (let i = 0; i < n; i++) {
    const o = 4 * i;
    if (refLuma - (0.2126 * maskData[o] + 0.7152 * maskData[o + 1] + 0.0722 * maskData[o + 2]) >= 55 && distToRef(maskData, i, ref) >= 60) dark[i] = 255;
  }
  return largestComponent(dark, w, h2);
}
function alignReferenceMask(darkMask, maskData, whiteMask, w, h2) {
  const a = bbox(whiteMask, w, h2), b = bbox(darkMask, w, h2), n = w * h2;
  if (!a || !b || a.count / n < 0.08 || a.count / n > 0.92) return null;
  const aw = Math.max(1, a.right - a.left), ah = Math.max(1, a.bottom - a.top);
  const bw = Math.max(1, b.right - b.left);
  const shadowRows = Math.max(2, Math.round(3e-3 * Math.max(w, h2)));
  const bBottom = Math.max(b.top + 1, b.bottom - shadowRows), bh = Math.max(1, bBottom - b.top);
  if (Math.abs(Math.log(aw / ah / (bw / bh))) > 0.28) return null;
  const alpha = new Uint8ClampedArray(n), data = new Uint8ClampedArray(4 * n).fill(255);
  for (let y = b.top; y <= bBottom; y++) {
    const sy = Math.round(a.top + (y - b.top) / bh * ah);
    for (let x = b.left; x <= b.right; x++) {
      const si = sy * w + Math.round(a.left + (x - b.left) / bw * aw), di = y * w + x;
      alpha[di] = darkMask[si];
      const so = 4 * si, doff = 4 * di;
      data[doff] = maskData[so];
      data[doff + 1] = maskData[so + 1];
      data[doff + 2] = maskData[so + 2];
      data[doff + 3] = maskData[so + 3];
    }
  }
  const grown = dilate(whiteMask, w, h2, 2);
  for (let i = 0; i < n; i++) if (grown[i] === 0) alpha[i] = 0;
  return { alpha, data };
}
function protectOpenings(mask, data, w, h2) {
  const n = w * h2, minPixels = Math.max(64, Math.round(15e-4 * n)), maxPixels = Math.round(0.25 * n), minDepth = Math.max(6, Math.round(0.02 * Math.max(w, h2)));
  const isDarkNeutral = (i) => {
    const o = 4 * i, r = data[o], g = data[o + 1], b = data[o + 2];
    return !(Math.max(r, g, b) - Math.min(r, g, b) > 14) && 0.2126 * r + 0.7152 * g + 0.0722 * b <= 195;
  };
  const seen = new Uint8Array(n), inComp = new Uint8Array(n), openings = new Uint8ClampedArray(n), comp = [], stack = [];
  let found = false;
  const depthOf = () => {
    const dist = new Int32Array(comp.length).fill(-1), idx = /* @__PURE__ */ new Map();
    for (let k = 0; k < comp.length; k++) idx.set(comp[k], k);
    const queue = [];
    for (let k = 0; k < comp.length; k++) {
      const i = comp[k], x = i % w, y = i / w | 0;
      const interior = x !== 0 && y !== 0 && x !== w - 1 && y !== h2 - 1 && inComp[i - 1] && inComp[i + 1] && inComp[i - w] && inComp[i + w];
      if (!interior) {
        dist[k] = 1;
        queue.push(k);
      }
    }
    let maxD = 1;
    for (let q = 0; q < queue.length; q++) {
      const k = queue[q], i = comp[k], dcur = dist[k];
      if (dcur > maxD) maxD = dcur;
      if (maxD >= minDepth) break;
      const x = i % w, y = i / w | 0;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1]) {
        if (j < 0 || !inComp[j]) continue;
        const kk = idx.get(j);
        if (dist[kk] === -1) {
          dist[kk] = dcur + 1;
          queue.push(kk);
        }
      }
    }
    return maxD;
  };
  for (let s = 0; s < n; s++) {
    if (seen[s] || mask[s] === 0 || !isDarkNeutral(s)) continue;
    comp.length = 0;
    stack.length = 0;
    stack.push(s);
    seen[s] = 1;
    let touchesOutside = false;
    while (stack.length) {
      const i = stack.pop();
      comp.push(i);
      inComp[i] = 1;
      const x = i % w, y = i / w | 0;
      if (x === 0 || y === 0 || x === w - 1 || y === h2 - 1) touchesOutside = true;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1]) {
        if (j < 0) continue;
        if (mask[j] === 0) {
          touchesOutside = true;
          continue;
        }
        if (!seen[j] && isDarkNeutral(j)) {
          seen[j] = 1;
          stack.push(j);
        }
      }
    }
    if (!touchesOutside && comp.length >= minPixels && comp.length <= maxPixels && depthOf() >= minDepth) {
      found = true;
      for (const i of comp) openings[i] = 255;
    }
    for (const i of comp) inComp[i] = 0;
  }
  if (!found) return mask;
  const edge = Math.max(w, h2);
  const erodeR = Math.max(1, Math.round(24e-4 * edge)), growSteps = Math.max(4, Math.round(0.035 * edge));
  const closeR = Math.max(1, Math.round(7e-3 * edge)), padR = Math.max(1, Math.round(4e-3 * edge));
  const eroded = erode(mask, w, h2, erodeR);
  const grown = Uint8ClampedArray.from(openings);
  let frontier = [];
  for (let i = 0; i < n; i++) if (grown[i] !== 0) frontier.push(i);
  for (let step = 0; step < growSteps && frontier.length; step++) {
    const next = [];
    for (const i of frontier) {
      const x = i % w, y = i / w | 0;
      for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1]) {
        if (j < 0 || grown[j] !== 0 || mask[j] === 0 || eroded[j] === 0) continue;
        const o = 4 * j, r = data[o], g = data[o + 1], b = data[o + 2];
        if (!(Math.max(r, g, b) - Math.min(r, g, b) > 18) && !(0.2126 * r + 0.7152 * g + 0.0722 * b > 235)) {
          grown[j] = 255;
          next.push(j);
        }
      }
    }
    frontier = next;
  }
  let closed = dilate(erode(grown, w, h2, closeR), w, h2, closeR);
  for (let i = 0; i < n; i++) if (openings[i] !== 0) closed[i] = 255;
  const padded = dilate(closed, w, h2, padR);
  const out = Uint8ClampedArray.from(mask);
  for (let i = 0; i < n; i++) if (padded[i] !== 0) out[i] = 0;
  return out;
}
function detectHardware(mask, data, luma2, w, h2) {
  const n = w * h2, r = Math.max(2, Math.round(6e-3 * Math.max(w, h2)));
  const minMax = (() => {
    const mn = new Uint8ClampedArray(n), mx = new Uint8ClampedArray(n);
    for (let y = 0; y < h2; y++) {
      const row = y * w;
      for (let x = 0; x < w; x++) {
        let lo = 255, hi = 0;
        for (let k = Math.max(0, x - r); k <= Math.min(w - 1, x + r); k++) {
          const v = luma2[row + k];
          if (v < lo) lo = v;
          if (v > hi) hi = v;
        }
        mn[row + x] = lo;
        mx[row + x] = hi;
      }
    }
    const mn2 = new Uint8ClampedArray(n), mx2 = new Uint8ClampedArray(n);
    for (let y = 0; y < h2; y++) {
      const y0 = Math.max(0, y - r), y1 = Math.min(h2 - 1, y + r);
      for (let x = 0; x < w; x++) {
        let lo = 255, hi = 0;
        for (let k = y0; k <= y1; k++) {
          const i = k * w + x;
          if (mn[i] < lo) lo = mn[i];
          if (mx[i] > hi) hi = mx[i];
        }
        mn2[y * w + x] = lo;
        mx2[y * w + x] = hi;
      }
    }
    return { min: mn2, max: mx2 };
  })();
  const neutral = (i) => {
    const o = 4 * i;
    return Math.max(data[o], data[o + 1], data[o + 2]) - Math.min(data[o], data[o + 1], data[o + 2]) <= 22;
  };
  const seed = new Uint8ClampedArray(n), seen = new Uint8Array(n), stack = [];
  for (let i = 0; i < n; i++) if (mask[i] !== 0 && minMax.max[i] - minMax.min[i] >= 90 && minMax.min[i] <= 140 && neutral(i)) {
    seen[i] = 1;
    seed[i] = 255;
    stack.push(i);
  }
  if (!stack.length) return null;
  while (stack.length) {
    const i = stack.pop(), x = i % w, y = i / w | 0;
    for (const j of [x + 1 < w ? i + 1 : -1, x - 1 >= 0 ? i - 1 : -1, y + 1 < h2 ? i + w : -1, y - 1 >= 0 ? i - w : -1])
      if (j >= 0 && !seen[j] && mask[j] !== 0 && minMax.min[j] <= 165 && minMax.max[j] - minMax.min[j] >= 35 && neutral(j)) {
        seen[j] = 1;
        seed[j] = 255;
        stack.push(j);
      }
  }
  let hw = fillHoles(seed, w, h2);
  hw = fillHoles(erode(dilate(hw, w, h2, 2), w, h2, 1), w, h2);
  let hwCount = 0, maskCount = 0;
  for (let i = 0; i < n; i++) {
    if (hw[i] !== 0) hwCount++;
    if (mask[i] !== 0) maskCount++;
  }
  if (!hwCount || !maskCount || hwCount / maskCount > 0.2) return null;
  const tmp = new Uint16Array(n), out = new Uint8ClampedArray(n);
  for (let y = 0; y < h2; y++) {
    const row = y * w;
    for (let x = 0; x < w; x++) {
      let s = 0, c = 0;
      for (let k = Math.max(0, x - 1); k <= Math.min(w - 1, x + 1); k++) {
        s += hw[row + k];
        c++;
      }
      tmp[row + x] = Math.round(s / c);
    }
  }
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    let s = 0, c = 0;
    for (let k = Math.max(0, y - 1); k <= Math.min(h2 - 1, y + 1); k++) {
      s += tmp[k * w + x];
      c++;
    }
    out[y * w + x] = Math.round(s / c);
  }
  return out;
}
function buildToneMaps(luma2, mask, w, h2) {
  const n = w * h2;
  const smooth = new Uint8ClampedArray(n);
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    const i = y * w + x;
    if (mask[i] === 0) {
      smooth[i] = luma2[i];
      continue;
    }
    let s = 0, c = 0;
    for (let dy = -2; dy <= 2; dy++) {
      const yy = y + dy;
      if (yy < 0 || yy >= h2) continue;
      for (let dx = -2; dx <= 2; dx++) {
        const xx = x + dx;
        if (xx < 0 || xx >= w) continue;
        const j = yy * w + xx;
        if (mask[j] !== 0) {
          s += luma2[j];
          c++;
        }
      }
    }
    smooth[i] = c > 0 ? Math.round(s / c) : luma2[i];
  }
  const hist = new Int32Array(256);
  let count = 0;
  for (let i = 0; i < n; i++) if (mask[i] > 0) {
    hist[smooth[i]]++;
    count++;
  }
  let median = 200;
  if (count > 0) {
    const target = 0.6 * count;
    let acc = 0;
    for (let v = 0; v < 256; v++) {
      acc += hist[v];
      if (acc >= target) {
        median = v;
        break;
      }
    }
  }
  const field = new Float32Array(n);
  for (let i = 0; i < n; i++) field[i] = mask[i] > 0 ? smooth[i] : median;
  const wide = boxBlur(field, w, h2, 45), mid = boxBlur(field, w, h2, 14);
  const shade = new Float32Array(n);
  for (let i = 0; i < n; i++) {
    if (mask[i] === 0) continue;
    shade[i] = 1.9 * (wide[i] - median) + 2.4 * softKnee(mid[i] - wide[i], 4, 44) + 2.2 * softKnee(smooth[i] - mid[i], 6, 36);
  }
  const hist2 = new Int32Array(512);
  let solid = 0;
  for (let i = 0; i < n; i++) {
    if (mask[i] < 250) continue;
    hist2[Math.max(-256, Math.min(255, Math.round(shade[i]))) + 256]++;
    solid++;
  }
  let lowPct = 0;
  if (solid > 0) {
    const target = 0.04 * solid;
    let acc = 0;
    for (let v = 0; v < 512; v++) {
      acc += hist2[v];
      if (acc >= target) {
        lowPct = v - 256;
        break;
      }
    }
  }
  const gain = Math.min(3, Math.max(1, 46 / Math.max(8, -lowPct)));
  const tone = new Uint8ClampedArray(n);
  for (let i = 0; i < n; i++) {
    if (mask[i] === 0) {
      tone[i] = smooth[i];
      continue;
    }
    let t = 0.5 + 112 * Math.tanh((shade[i] < 0 ? gain : 1 + (gain - 1) * 0.55) * shade[i] / 112) / 255;
    t = t < 0 ? 0 : t > 1 ? 1 : t;
    tone[i] = Math.round(255 * t);
  }
  return { tone, seamLow: mid };
}
function prepareGarmentFromRgba(data, w, h2, maskData, sourceAlpha, protect) {
  const total = w * h2;
  const whiteMask = (() => {
    const out = new Uint8ClampedArray(total).fill(255), seen = new Uint8Array(total), stack = [], ref = cornerAverage(data, w, h2);
    const push = (x, y) => {
      if (x < 0 || y < 0 || x >= w || y >= h2) return;
      const i = y * w + x;
      if (seen[i]) return;
      seen[i] = 1;
      const o = 4 * i, dr = data[o] - ref[0], dg = data[o + 1] - ref[1], db = data[o + 2] - ref[2];
      if (Math.sqrt(dr * dr + dg * dg + db * db) < 5) {
        out[i] = 0;
        stack.push(i);
      }
    };
    for (let x = 0; x < w; x++) {
      push(x, 0);
      push(x, h2 - 1);
    }
    for (let y = 0; y < h2; y++) {
      push(0, y);
      push(w - 1, y);
    }
    while (stack.length) {
      const i = stack.pop(), x = i % w, y = i / w | 0;
      push(x + 1, y);
      push(x - 1, y);
      push(x, y + 1);
      push(x, y - 1);
    }
    return out;
  })();
  const aligned = maskData?.length === data.length ? alignReferenceMask(darkMaskFromReference(maskData, w, h2), maskData, whiteMask, w, h2) : null;
  const hasRef = aligned != null;
  let mask = aligned?.alpha ?? whiteMask;
  mask = majority3x3(mask, w, h2);
  mask = majority3x3(mask, w, h2);
  if (!hasRef) mask = carveBackgroundPockets(fillHoles(mask, w, h2), data, w, h2);
  mask = dropSmallComponents(mask, w, h2);
  if (!hasRef) mask = reclaimInnerPockets(mask, data, w, h2);
  if (hasRef) mask = dilate(erode(mask, w, h2, 2), w, h2, 2);
  mask = largestComponent(trimBackgroundEdge(erode(dilate(mask, w, h2, 2), w, h2, 3), aligned?.data ?? data, w, h2), w, h2);
  if (protect?.openings) mask = protectOpenings(mask, data, w, h2);
  const finalMask = mask;
  const luma2 = new Uint8ClampedArray(total);
  let kept = 0;
  for (let i = 0; i < total; i++) {
    const o = 4 * i;
    luma2[i] = Math.round(0.2126 * data[o] + 0.7152 * data[o + 1] + 0.0722 * data[o + 2]);
    if (mask[i] > 0) kept++;
  }
  const keptFrac = kept / total;
  const { tone, seamLow } = buildToneMaps(luma2, mask, w, h2);
  const texture = (() => {
    const out = new Uint8ClampedArray(total);
    for (let i = 0; i < total; i++) {
      if (mask[i] === 0) {
        out[i] = 128;
        continue;
      }
      let t = Math.pow(luma2[i] / Math.max(1, seamLow[i]), 1.75);
      t = t < 0.5 ? 0.5 : t > 1.5 ? 1.5 : t;
      out[i] = Math.round(128 * t);
    }
    return out;
  })();
  const lumaOut = inpaintOutward(tone, finalMask, w, h2, 5);
  const texOut = inpaintOutward(texture, finalMask, w, h2, 5);
  const alphaOut = (() => {
    let f = new Float32Array(total);
    for (let i = 0; i < total; i++) f[i] = finalMask[i] > 0 ? 1 : 0;
    f = blur3(f, w, h2, true);
    f = blur3(f, w, h2, false);
    const out = new Uint8ClampedArray(total);
    for (let i = 0; i < total; i++) {
      const e = Math.max(0, Math.min(1, (f[i] - 0.25) / 0.5));
      out[i] = Math.round(255 * (e * e * (3 - 2 * e)));
    }
    return out;
  })();
  let keep, keepRgb;
  if (protect?.hardware) {
    const hw = detectHardware(finalMask, data, luma2, w, h2);
    if (hw) {
      keep = hw;
      keepRgb = new Uint8ClampedArray(3 * total);
      for (let i = 0; i < total; i++) {
        if (hw[i] === 0) continue;
        const o = 4 * i, k = 3 * i;
        keepRgb[k] = data[o];
        keepRgb[k + 1] = data[o + 1];
        keepRgb[k + 2] = data[o + 2];
      }
    }
  }
  return { width: w, height: h2, luma: lumaOut, alpha: alphaOut, tex: texOut, ok: keptFrac > 0.08 && keptFrac < 0.92, ...keep && keepRgb ? { keep, keepRgb } : {} };
}
function recolorPreparedRgba(prepared2, hex) {
  const { width: w, height: h2, luma: luma2, alpha, tex, keep, keepRgb } = prepared2;
  const out = new Uint8ClampedArray(w * h2 * 4), lut = buildRecolorLut(hex);
  for (let i = 0; i < w * h2; i++) {
    const a = alpha[i], o = 4 * i;
    if (a < RECOLOR_ALPHA_OUTPUT_CUTOFF) {
      out[o] = out[o + 1] = out[o + 2] = out[o + 3] = 0;
      continue;
    }
    const l = luma2[i];
    let t = tex[i];
    if (a < 48) t = Math.round(128 + a / 48 * (t - 128));
    let r = lut.r[l] * t >> 7, g = lut.g[l] * t >> 7, b = lut.b[l] * t >> 7;
    const k = keep?.[i] ?? 0;
    if (k > 0 && keepRgb) {
      const e = 3 * i;
      r = Math.round(r + (keepRgb[e] - r) * k / 255);
      g = Math.round(g + (keepRgb[e + 1] - g) * k / 255);
      b = Math.round(b + (keepRgb[e + 2] - b) * k / 255);
    }
    out[o] = r;
    out[o + 1] = g;
    out[o + 2] = b;
    out[o + 3] = a;
  }
  return out;
}

// piobox-customizer/core/recolor.js
var PROTECT_BY_FILE = {
  "six-panel-cap-back-white.webp": { openings: true },
  "lanyard-white.webp": { hardware: true },
  "heart-pin-white.webp": { hardware: true }
};
var extraProtect = {};
function registerPhotoProtection(map) {
  extraProtect = { ...extraProtect, ...map };
}
var fileOf = (src) => src?.split("?")[0].split("#")[0].split("/").pop();
var protectFor = (src) => {
  const f = fileOf(src);
  return f ? extraProtect[f] ?? PROTECT_BY_FILE[f] : void 0;
};
function loadImage2(src) {
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.crossOrigin = "anonymous";
    img.onload = () => resolve(img);
    img.onerror = () => reject(new Error(`image failed to load: ${src}`));
    img.src = src;
  });
}
function readPixels(img) {
  const scale = Math.min(1, RECOLOR_MAX_EDGE / Math.max(img.naturalWidth, img.naturalHeight));
  const w = Math.max(1, Math.round(img.naturalWidth * scale)), h2 = Math.max(1, Math.round(img.naturalHeight * scale));
  const canvas = document.createElement("canvas");
  canvas.width = w;
  canvas.height = h2;
  const ctx = canvas.getContext("2d", { willReadFrequently: true });
  if (!ctx) return null;
  ctx.imageSmoothingEnabled = true;
  ctx.imageSmoothingQuality = "high";
  ctx.drawImage(img, 0, 0, w, h2);
  try {
    const raw = ctx.getImageData(0, 0, w, h2);
    const total = w * h2, alpha = new Uint8ClampedArray(total);
    let translucent = 0, opaque = 0;
    for (let i = 0; i < total; i++) {
      const a = raw.data[4 * i + 3];
      alpha[i] = a;
      if (a < 250) translucent++;
      if (a >= 128) opaque++;
    }
    ctx.globalCompositeOperation = "destination-over";
    ctx.fillStyle = "#ffffff";
    ctx.fillRect(0, 0, w, h2);
    ctx.globalCompositeOperation = "source-over";
    const flat = ctx.getImageData(0, 0, w, h2);
    const hasCutout = translucent / total > 0.01 && opaque / total > 0.01 && opaque / total < 0.98;
    return { data: flat.data, width: w, height: h2, sourceAlpha: hasCutout ? alpha : void 0 };
  } catch {
    return null;
  }
}
function readMaskPixels(img, w, h2) {
  const canvas = document.createElement("canvas");
  canvas.width = w;
  canvas.height = h2;
  const ctx = canvas.getContext("2d", { willReadFrequently: true });
  if (!ctx) return null;
  ctx.imageSmoothingEnabled = true;
  ctx.imageSmoothingQuality = "high";
  ctx.fillStyle = "#ffffff";
  ctx.fillRect(0, 0, w, h2);
  ctx.drawImage(img, 0, 0, w, h2);
  try {
    return ctx.getImageData(0, 0, w, h2).data;
  } catch {
    return null;
  }
}
var worker = null;
var workerBroken = false;
var nextId = 1;
var pending = /* @__PURE__ */ new Map();
function getWorker() {
  if (workerBroken || typeof Worker === "undefined") return null;
  if (worker) return worker;
  try {
    worker = new Worker(new URL("./recolor.worker.js", import.meta.url), { type: "module" });
    worker.onmessage = (e) => {
      const { id, error, ok, width, height, luma: luma2, alpha, tex, keep, keepRgb } = e.data;
      const p = pending.get(id);
      if (!p) return;
      pending.delete(id);
      if (error) {
        p.reject(new Error(error));
        return;
      }
      p.resolve({ width, height, ok, luma: new Uint8ClampedArray(luma2), alpha: new Uint8ClampedArray(alpha), tex: new Uint8ClampedArray(tex), ...keep && keepRgb ? { keep: new Uint8ClampedArray(keep), keepRgb: new Uint8ClampedArray(keepRgb) } : {} });
    };
    worker.onerror = () => {
      workerBroken = true;
      for (const p of pending.values()) p.reject(new Error("worker error"));
      pending.clear();
      worker?.terminate();
      worker = null;
    };
    return worker;
  } catch {
    workerBroken = true;
    return null;
  }
}
function prepareInWorker(pixels, protect) {
  const w = getWorker();
  if (!w) return null;
  const id = nextId++;
  const buffer = new Uint8ClampedArray(pixels.data).buffer;
  const maskBuffer = pixels.maskData ? new Uint8ClampedArray(pixels.maskData).buffer : void 0;
  const sourceAlphaBuffer = pixels.sourceAlpha ? new Uint8ClampedArray(pixels.sourceAlpha).buffer : void 0;
  const promise = new Promise((resolve, reject) => {
    const timer = setTimeout(() => {
      if (!pending.has(id)) return;
      pending.delete(id);
      workerBroken = true;
      worker?.terminate();
      worker = null;
      for (const p of pending.values()) p.reject(new Error("worker timeout"));
      pending.clear();
      reject(new Error("worker timeout"));
    }, 12e3);
    pending.set(id, { resolve: (v) => {
      clearTimeout(timer);
      resolve(v);
    }, reject: (e) => {
      clearTimeout(timer);
      reject(e);
    } });
  });
  const transfer = [buffer];
  if (maskBuffer) transfer.push(maskBuffer);
  if (sourceAlphaBuffer) transfer.push(sourceAlphaBuffer);
  w.postMessage({ id, buffer, maskBuffer, sourceAlphaBuffer, width: pixels.width, height: pixels.height, protect }, transfer);
  return promise;
}
function lru(map, key, value, max) {
  map.delete(key);
  map.set(key, value);
  while (map.size > max) {
    const k = map.keys().next().value;
    if (k === void 0) break;
    map.delete(k);
  }
}
var prepared = /* @__PURE__ */ new Map();
var preparing = /* @__PURE__ */ new Map();
var keyOf2 = (src, mask) => `${src}|mask:${mask ?? ""}`;
async function prepare(src, maskSrc) {
  const key = keyOf2(src, maskSrc);
  const hit = prepared.get(key);
  if (hit) {
    lru(prepared, key, hit, 6);
    return hit;
  }
  const inflight = preparing.get(key);
  if (inflight) return inflight;
  const job = (async () => {
    let img;
    try {
      img = await loadImage2(src);
    } catch {
      return null;
    }
    const pixels = readPixels(img);
    if (!pixels) return null;
    if (maskSrc) {
      try {
        const m = await loadImage2(maskSrc);
        pixels.maskData = readMaskPixels(m, pixels.width, pixels.height) ?? void 0;
      } catch {
      }
    }
    const protect = protectFor(src);
    let result = null;
    try {
      result = await prepareInWorker(pixels, protect);
    } catch {
      result = null;
    }
    if (!result) {
      try {
        result = prepareGarmentFromRgba(pixels.data, pixels.width, pixels.height, pixels.maskData, pixels.sourceAlpha, protect);
      } catch {
        return null;
      }
    }
    lru(prepared, key, result, 6);
    return result;
  })();
  preparing.set(key, job);
  try {
    return await job;
  } finally {
    preparing.delete(key);
  }
}
function toDataUrl(canvas) {
  const webp = canvas.toDataURL("image/webp", 0.92);
  return webp.startsWith("data:image/webp") ? webp : canvas.toDataURL("image/png");
}
function paint(preparedGarment, hex) {
  const { width: w, height: h2 } = preparedGarment;
  const canvas = document.createElement("canvas");
  canvas.width = w;
  canvas.height = h2;
  const ctx = canvas.getContext("2d");
  if (!ctx) return null;
  const img = ctx.createImageData(w, h2);
  img.data.set(recolorPreparedRgba(preparedGarment, hex));
  ctx.putImageData(img, 0, 0);
  return toDataUrl(canvas);
}
var rendered = /* @__PURE__ */ new Map();
function prewarmGarment(src, maskSrc) {
  if (src && !prepared.has(keyOf2(src, maskSrc)) && !preparing.has(keyOf2(src, maskSrc))) prepare(src, maskSrc);
}
async function recolorGarment(src, hex, maskSrc) {
  const key = `${hex}|${keyOf2(src, maskSrc)}`;
  const hit = rendered.get(key);
  if (hit) {
    lru(rendered, key, hit, 24);
    return hit;
  }
  const p = await prepare(src, maskSrc);
  if (!p || !p.ok) return null;
  const out = paint(p, hex);
  if (out) lru(rendered, key, out, 24);
  return out;
}
function peekRecolored(src, hex, maskSrc) {
  return rendered.get(`${hex}|${keyOf2(src, maskSrc)}`) ?? null;
}
var capCache = /* @__PURE__ */ new Map();
async function prepareCap(src) {
  const hit = capCache.get(src);
  if (hit) return hit;
  let img;
  try {
    img = await loadImage2(src);
  } catch {
    return null;
  }
  const pixels = readPixels(img);
  if (!pixels) return null;
  let p;
  try {
    p = prepareGarmentFromRgba(pixels.data, pixels.width, pixels.height, void 0, pixels.sourceAlpha);
  } catch {
    return null;
  }
  if (!p.ok) return null;
  const { width: w, height: h2 } = pixels;
  let top = h2, bottom = -1;
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) if (p.alpha[y * w + x] >= 200) {
    if (y < top) top = y;
    bottom = y;
    break;
  }
  const capMask = new Uint8Array(w * h2);
  if (bottom > top) {
    const limit = Math.min(h2 - 1, Math.ceil(top + (bottom - top) * 0.1));
    const seen = new Uint8Array(w * h2), queue = new Int32Array(w * h2);
    let qh = 0, qt = 0;
    const push = (i) => {
      if (!seen[i]) {
        seen[i] = 1;
        queue[qt++] = i;
      }
    };
    for (let x = 0; x < w; x++) {
      const i = top * w + x;
      if (p.alpha[i] >= 200) push(i);
    }
    while (qh < qt) {
      const i = queue[qh++];
      if (p.alpha[i] < 200) continue;
      capMask[i] = 1;
      const x = i % w, y = (i - x) / w;
      if (x > 0) push(i - 1);
      if (x < w - 1) push(i + 1);
      if (y > top) push(i - w);
      if (y < limit) push(i + w);
    }
  }
  const entry = { width: w, height: h2, base: new Uint8ClampedArray(pixels.data), prepared: p, capMask };
  lru(capCache, src, entry, 3);
  return entry;
}
async function recolorCapGarment(src, hex) {
  const key = `cap|${hex}|${src}`;
  const hit = rendered.get(key);
  if (hit) return hit;
  const c = await prepareCap(src);
  if (!c) return null;
  const { width: w, height: h2, base, prepared: p, capMask } = c;
  const painted = recolorPreparedRgba(p, hex), out = new Uint8ClampedArray(base);
  for (let i = 0; i < w * h2; i++) {
    const o = 4 * i, a = p.alpha[i];
    if (a < RECOLOR_ALPHA_OUTPUT_CUTOFF) {
      out[o] = out[o + 1] = out[o + 2] = out[o + 3] = 0;
      continue;
    }
    out[o + 3] = a;
    if (capMask[i]) {
      out[o] = painted[o];
      out[o + 1] = painted[o + 1];
      out[o + 2] = painted[o + 2];
      out[o + 3] = painted[o + 3];
    }
  }
  const canvas = document.createElement("canvas");
  canvas.width = w;
  canvas.height = h2;
  const ctx = canvas.getContext("2d");
  if (!ctx) return null;
  const img = ctx.createImageData(w, h2);
  img.data.set(out);
  ctx.putImageData(img, 0, 0);
  const url = toDataUrl(canvas);
  lru(rendered, key, url, 24);
  return url;
}

// piobox-customizer/core/preflight.js
var ascii = (u8, n) => {
  let s = "";
  for (let i = 0; i < Math.min(u8.length, n); i++) s += String.fromCharCode(u8[i]);
  return s;
};
var luma = (r, g, b) => 0.2126 * r + 0.7152 * g + 0.0722 * b;
var NON_PAINT = /* @__PURE__ */ new Set(["none", "inherit", "currentcolor", "transparent", "context-fill", "context-stroke", "url"]);
function sniffKind(u8) {
  if (u8.length >= 8 && u8[0] === 137 && u8[1] === 80 && u8[2] === 78 && u8[3] === 71) return "png";
  if (u8.length >= 3 && u8[0] === 255 && u8[1] === 216 && u8[2] === 255) return "jpeg";
  if (u8.length >= 6 && u8[0] === 71 && u8[1] === 73 && u8[2] === 70 && u8[3] === 56) return "gif";
  if (u8.length >= 12 && u8[0] === 82 && u8[1] === 73 && u8[2] === 70 && u8[3] === 70 && u8[8] === 87 && u8[9] === 69 && u8[10] === 66 && u8[11] === 80) return "webp";
  if (u8.length >= 5 && u8[0] === 37 && u8[1] === 80 && u8[2] === 68 && u8[3] === 70) return "pdf";
  if (ascii(u8, 512).toLowerCase().includes("<svg")) return "svg";
  return "unknown";
}
function pngDpi(u8) {
  let p = 8;
  while (p + 12 <= u8.length) {
    const len = u8[p] << 24 | u8[p + 1] << 16 | u8[p + 2] << 8 | u8[p + 3];
    const type = ascii(u8.subarray(p + 4, p + 8), 4);
    if (type === "pHYs" && len === 9 && p + 12 + 9 <= u8.length) {
      const o = p + 8, ppu = (u8[o] << 24 | u8[o + 1] << 16 | u8[o + 2] << 8 | u8[o + 3]) >>> 0;
      if (u8[o + 8] === 1 && ppu > 0) return Math.round(0.0254 * ppu);
      break;
    }
    if (type === "IDAT" || type === "IEND" || len < 0) break;
    p += 12 + len;
  }
  return null;
}
function jpegDpi(u8) {
  let p = 2;
  while (p + 4 <= u8.length && u8[p] === 255) {
    const m = u8[p + 1];
    if (m === 216 || m >= 208 && m <= 217) {
      p += 2;
      continue;
    }
    if (m === 218) break;
    const len = u8[p + 2] << 8 | u8[p + 3];
    if (len < 2) break;
    if (m === 224 && p + 4 + len - 2 <= u8.length) {
      const o = p + 4;
      if (ascii(u8.subarray(o, o + 5), 5) === "JFIF\0" && len >= 16) {
        const units = u8[o + 7], x = u8[o + 8] << 8 | u8[o + 9];
        if (x > 0) {
          if (units === 1) return x;
          if (units === 2) return Math.round(2.54 * x);
        }
        break;
      }
    }
    p += 2 + len;
  }
  return null;
}
function svgFacts(text) {
  const embeddedRasterCount = (text.match(/<image[\s/>]/gi) ?? []).length;
  const hasLiveText = /<text[\s/>]/i.test(text), hasGradient = /<(?:linear|radial)Gradient[\s/>]/i.test(text);
  const paints = /* @__PURE__ */ new Set(), re = /(?:fill|stroke)\s*[:=]\s*["']?\s*(#[0-9a-f]{3,8}|rgba?\([^)"']*\)|[a-z][a-z-]*)/gi;
  let m;
  while ((m = re.exec(text)) !== null) {
    const v = m[1].toLowerCase().trim();
    if (!v.startsWith("url(") && !NON_PAINT.has(v)) paints.add(v);
  }
  return { embeddedRasterCount, hasLiveText, hasGradient, paintCount: paints.size };
}
function computeStats(d, w, h2) {
  const total = w * h2;
  let hasAlpha = false;
  for (let i = 3; i < d.length; i += 4) if (d[i] < 250) {
    hasAlpha = true;
    break;
  }
  const solid = hasAlpha ? null : (() => {
    let sr = 0, sg = 0, sb = 0, n = 0;
    const ring = Math.min(2, Math.floor(Math.min(w, h2) / 2));
    const add = (x, y) => {
      const i = (y * w + x) * 4;
      sr += d[i];
      sg += d[i + 1];
      sb += d[i + 2];
      n++;
    };
    for (let y = 0; y < h2; y++) {
      if (y < ring || y >= h2 - ring) for (let x = 0; x < w; x++) add(x, y);
      else {
        for (let x = 0; x < ring; x++) add(x, y);
        for (let x = w - ring; x < w; x++) add(x, y);
      }
    }
    if (n === 0) return null;
    const r2 = sr / n, g = sg / n, b2 = sb / n;
    let match = 0, count = 0;
    for (let y = 0; y < h2; y++) {
      const edgeRow = y < ring || y >= h2 - ring;
      for (let x = 0; x < w; x++) {
        if (!edgeRow && x >= ring && x < w - ring) continue;
        const i = (y * w + x) * 4;
        count++;
        if (Math.abs(d[i] - r2) <= 22 && Math.abs(d[i + 1] - g) <= 22 && Math.abs(d[i + 2] - b2) <= 22) match++;
      }
    }
    return count === 0 || match / count < 0.9 ? null : { r: Math.round(r2), g: Math.round(g), b: Math.round(b2) };
  })();
  const isInk = (i) => hasAlpha ? d[i + 3] > 8 : !solid || Math.abs(d[i] - solid.r) > 22 || Math.abs(d[i + 1] - solid.g) > 22 || Math.abs(d[i + 2] - solid.b) > 22;
  const ink = new Uint8Array(total);
  let inkCount = 0, semi = 0, opaque = 0, opaqueLuma = 0, semiLuma = 0, nearWhite = 0, nearBlack = 0;
  let l = w, t = h2, r = -1, b = -1;
  const bins = new Uint32Array(4096), binSum = new Float64Array(12288);
  for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
    const p = y * w + x, i = 4 * p;
    if (!isInk(i)) continue;
    ink[p] = 1;
    inkCount++;
    if (x < l) l = x;
    if (x > r) r = x;
    if (y < t) t = y;
    if (y > b) b = y;
    const R = d[i], G = d[i + 1], B = d[i + 2];
    if ((hasAlpha ? d[i + 3] : 255) < 200) {
      semi++;
      semiLuma += luma(R, G, B);
      continue;
    }
    opaque++;
    opaqueLuma += luma(R, G, B);
    if (R >= 232 && G >= 232 && B >= 232) nearWhite++;
    if (R <= 45 && G <= 45 && B <= 45) nearBlack++;
    const bin = R >> 4 << 8 | G >> 4 << 4 | B >> 4;
    bins[bin]++;
    binSum[3 * bin] += R;
    binSum[3 * bin + 1] += G;
    binSum[3 * bin + 2] += B;
  }
  const inkBox = r >= 0 ? { x: l, y: t, w: r - l + 1, h: b - t + 1 } : null;
  let soft = 0, pairs = 0;
  const step = Math.max(1, Math.floor(h2 / 256));
  for (let y = 0; y < h2; y += step) for (let x = 1; x < w; x++) {
    const p = y * w + x;
    if (!ink[p] || !ink[p - 1]) continue;
    const i = 4 * p, j = i - 4;
    if (hasAlpha && (d[i + 3] < 200 || d[j + 3] < 200)) continue;
    const delta = Math.max(Math.abs(d[i] - d[j]), Math.abs(d[i + 1] - d[j + 1]), Math.abs(d[i + 2] - d[j + 2]));
    pairs++;
    if (delta >= 3 && delta <= 26) soft++;
  }
  const softRatio = pairs > 0 ? soft / pairs : 0;
  const { colors, distinctBins } = opaque > 0 ? clusterColors(bins, binSum, opaque) : { colors: [], distinctBins: 0 };
  const semiFrac = inkCount > 0 ? semi / inkCount : 0, meanInkLuma = opaque > 0 ? opaqueLuma / opaque : 0, meanSemiLuma = semi > 0 ? semiLuma / semi : 0;
  const whiteHalo = hasAlpha && semiFrac > 4e-3 && opaque > 0 && meanInkLuma < 140 && meanSemiLuma - meanInkLuma > 60;
  const thin = [0, 0, 0];
  if (inkCount > 0) {
    const tmp = new Uint8Array(total);
    let cur = ink.slice(), remaining = inkCount;
    for (let k = 0; k < 3; k++) {
      let n = 0;
      for (let y = 0; y < h2; y++) for (let x = 0; x < w; x++) {
        const p = y * w + x;
        let v = cur[p];
        if (v) {
          if (x === 0 || x === w - 1 || y === 0 || y === h2 - 1) v = 0;
          else for (let dy = -1; dy <= 1 && v; dy++) {
            const q = p + dy * w;
            if (!(cur[q - 1] && cur[q] && cur[q + 1])) v = 0;
          }
        }
        tmp[p] = v;
        if (v) n++;
      }
      cur.set(tmp);
      remaining = n;
      thin[k] = (inkCount - remaining) / inkCount;
    }
  }
  return {
    analysisW: w,
    analysisH: h2,
    hasAlpha,
    inkFraction: total > 0 ? inkCount / total : 0,
    semiTransparentFraction: semiFrac,
    solidBackground: solid ? { hex: rgbToHex(solid), nearWhite: solid.r >= 240 && solid.g >= 240 && solid.b >= 240 } : null,
    whiteHalo,
    colors,
    colorCount: colors.length,
    continuousTone: softRatio > 0.4 || distinctBins > 220,
    nearWhiteFraction: opaque > 0 ? nearWhite / opaque : 0,
    nearBlackFraction: opaque > 0 ? nearBlack / opaque : 0,
    meanInkLuma,
    thinInkFraction: thin,
    inkBox
  };
}
function clusterColors(bins, binSum, opaque) {
  const list = [];
  let distinct = 0;
  const minPop = Math.max(3, 2e-4 * opaque);
  for (let b = 0; b < 4096; b++) {
    const pop = bins[b];
    if (pop === 0) continue;
    if (pop >= minPop) distinct++;
    list.push({ pop, r: binSum[3 * b] / pop, g: binSum[3 * b + 1] / pop, b: binSum[3 * b + 2] / pop });
  }
  list.sort((a, b) => b.pop - a.pop);
  const clusters = [];
  const labOf = (r, g, b) => hexToLab(rgbToHex({ r: Math.round(r), g: Math.round(g), b: Math.round(b) }));
  for (let i = 0; i < Math.min(list.length, 400); i++) {
    const e = list[i], lab = labOf(e.r, e.g, e.b);
    let best = null, bestD = Infinity;
    for (const c of clusters) {
      const d = deltaE2000(lab, c.lab);
      if (d < bestD) {
        bestD = d;
        best = c;
      }
    }
    if (best && bestD < 16) {
      best.pop += e.pop;
      best.sr += e.r * e.pop;
      best.sg += e.g * e.pop;
      best.sb += e.b * e.pop;
    } else if (clusters.length < 20) clusters.push({ pop: e.pop, sr: e.r * e.pop, sg: e.g * e.pop, sb: e.b * e.pop, lab });
  }
  clusters.sort((a, b) => b.pop - a.pop);
  return { colors: clusters.filter((c) => c.pop / opaque >= 0.015).slice(0, 8).map((c) => ({ hex: rgbToHex({ r: Math.round(c.sr / c.pop), g: Math.round(c.sg / c.pop), b: Math.round(c.sb / c.pop) }), fraction: c.pop / opaque })), distinctBins: distinct };
}
var cache = /* @__PURE__ */ new WeakMap();
function analyzeArtwork(artwork) {
  let p = cache.get(artwork.file);
  if (!p) {
    p = analyze(artwork);
    cache.set(artwork.file, p);
  }
  return p;
}
async function analyze(art) {
  let head = new Uint8Array(0);
  try {
    head = new Uint8Array(await art.file.slice(0, 65536).arrayBuffer());
  } catch {
  }
  let svgText = null;
  if (art.isVector && art.file.size <= 3145728) {
    try {
      svgText = await art.file.text();
    } catch {
      svgText = null;
    }
  }
  const kind = sniffKind(head);
  const meta = {
    kind,
    metadataDpi: kind === "png" ? pngDpi(head) : kind === "jpeg" ? jpegDpi(head) : null,
    vectorSource: kind === "svg" ? "svg" : /\.pdf\.png$/i.test(art.file.name) || kind === "pdf" ? "pdf" : /\.ai\.png$/i.test(art.file.name) ? "ai" : null,
    svg: kind === "svg" && svgText ? svgFacts(svgText) : null
  };
  const base = { fileName: art.file.name, fileBytes: art.file.size, sourceW: art.naturalW, sourceH: art.naturalH, isVector: art.isVector, meta, stats: null };
  try {
    const img = await new Promise((res, rej) => {
      const i = new Image();
      i.onload = () => res(i);
      i.onerror = () => rej(new Error("decode failed"));
      i.src = art.url;
    });
    const sw = Math.max(1, art.naturalW), sh = Math.max(1, art.naturalH), edge = Math.max(sw, sh);
    const k = art.isVector ? 1024 / edge : Math.min(1, 1024 / edge);
    const w = Math.max(1, Math.round(sw * k)), h2 = Math.max(1, Math.round(sh * k));
    const canvas = document.createElement("canvas");
    canvas.width = w;
    canvas.height = h2;
    const ctx = canvas.getContext("2d", { willReadFrequently: true });
    if (!ctx) return base;
    ctx.drawImage(img, 0, 0, w, h2);
    return { ...base, stats: computeStats(ctx.getImageData(0, 0, w, h2).data, w, h2) };
  } catch {
    return base;
  }
}
function garmentContrastLevel(colors, meanInkLuma, garmentHex) {
  if (colors.length === 0) return "good";
  const maxDelta = colors.reduce((m, c) => Math.max(m, hexDeltaE(c.hex, garmentHex)), 0);
  const { r, g, b } = hexToRgb(garmentHex), gl = luma(r, g, b);
  return maxDelta < 12 || gl < 32 && meanInkLuma < 42 || gl > 216 && meanInkLuma > 210 ? "low" : "good";
}
function suggestContrastingGarment(colors, meanInkLuma, candidates) {
  const ok = candidates.filter((c) => garmentContrastLevel(colors, meanInkLuma, c.hex) === "good");
  if (ok.length === 0) return null;
  const score = (hex) => colors.reduce((m, c) => Math.max(m, hexDeltaE(c.hex, hex)), 0);
  return ok.reduce((best, c) => score(c.hex) > score(best.hex) ? c : best);
}
var METHOD_LABEL = { screen: "Screen print", embroidery: "Embroidery", dtg: "DTG" };
var MAX_AREA_IN = { screen: { w: 14, h: 16 }, dtg: { w: 13, h: 16 }, embroidery: { w: 12, h: 15 } };
var SEVERITY_ORDER = { fail: 0, warn: 1, info: 2, pass: 3 };
var widthIn = (v) => Math.max(0.5, v);
function findings(a, method, printedWidthIn, garmentHex) {
  const out = [];
  if (a.isVector) {
    out.push({ id: "vector", severity: "pass", title: "Vector artwork, stays sharp at any print size", short: "vector" });
    if (a.meta.svg) {
      if (a.meta.svg.embeddedRasterCount > 0) out.push({ id: "svg-embedded-raster", severity: "warn", title: "This SVG embeds raster images, so it won't scale like a true vector", detail: "Export the artwork as pure paths, or send the original layered file.", short: "raster inside SVG" });
      if (a.meta.svg.hasLiveText) out.push({ id: "svg-live-text", severity: "warn", title: "Live text detected in the SVG", detail: "Convert text to outlines before production so fonts can't substitute.", short: "live text in SVG" });
    }
  } else {
    const dpi = Math.round(a.sourceW / widthIn(printedWidthIn)), at = `${widthIn(printedWidthIn).toFixed(1)} in wide`;
    const need = `A file about ${Math.ceil(300 * widthIn(printedWidthIn)).toLocaleString("en-US")} px wide would print crisply at this size.`;
    if (a.meta.vectorSource === "pdf" || a.meta.vectorSource === "ai") out.push({ id: "vector-source", severity: "pass", title: `Vector print file (${a.meta.vectorSource.toUpperCase()}), converted at high resolution`, detail: "We'll use your original vector for production.", short: "vector source" });
    else if (dpi >= 300) out.push({ id: "resolution", severity: "pass", title: `Excellent resolution (${dpi} DPI at ${at})`, short: "high resolution" });
    else if (dpi >= 150) out.push({ id: "resolution", severity: "pass", title: `Sharp at this size (${dpi} DPI at ${at})`, short: "sharp resolution" });
    else if (dpi >= 90) out.push({ id: "resolution", severity: "info", title: `Workable resolution (${dpi} DPI at ${at})`, detail: "Fine for most prints. 150+ DPI is ideal; shrink the print or send larger art for crisper edges.", short: "medium resolution" });
    else if (dpi >= 45) out.push({ id: "resolution", severity: "warn", title: `Low resolution (${dpi} DPI at ${at})`, detail: `Edges will soften at this size. ${need} A vector also prints sharp at any size, or print it smaller.`, short: "low resolution" });
    else out.push({ id: "resolution", severity: "fail", title: `Resolution far too low (${dpi} DPI at ${at})`, detail: `This will print visibly pixelated. ${need} A vector version prints sharp at any size.`, short: "very low resolution" });
  }
  const s = a.stats;
  if (s) {
    if (s.solidBackground) out.push({ id: "solid-background", severity: "warn", title: s.solidBackground.nearWhite ? "Solid white background will print as part of the design" : "Solid background will print as part of the design", detail: "Send a transparent PNG or vector if you only want the logo. We can also remove it for you on proof.", short: s.solidBackground.nearWhite ? "white background" : "solid background" });
    else if (s.hasAlpha) out.push({ id: "transparent-bg", severity: "pass", title: "Transparent background", short: "transparent background" });
    if (garmentHex && s.colorCount > 0 && !s.continuousTone) {
      if (garmentContrastLevel(s.colors, s.meanInkLuma, garmentHex) === "low") out.push({ id: "garment-contrast", severity: "warn", title: "Low contrast against the garment color, so the art won't stand out", detail: "Pick a more contrasting garment shade, or add an outline or contrasting color to the art.", short: "low garment contrast" });
      else out.push({ id: "garment-contrast", severity: "pass", title: "Good contrast against the chosen garment color", short: "good garment contrast" });
    } else if (!garmentHex && s.nearWhiteFraction > 0.45) out.push({ id: "mostly-light", severity: "info", title: "Mostly light artwork, reads best on darker garments", short: "light artwork" });
    else if (!garmentHex && s.nearBlackFraction > 0.6) out.push({ id: "mostly-dark", severity: "info", title: "Mostly dark artwork, reads best on lighter garments", short: "dark artwork" });
    const colorsLabel = `${s.colorCount} ${s.colorCount === 1 ? "color" : "colors"}`;
    if (method === "screen") {
      if (s.continuousTone) out.push({ id: "screen-tone", severity: "warn", title: "Gradients or photo detail print as halftone dots in screen printing", detail: "Looks intentional on some art. For exact full-color reproduction, DTG is the better fit.", short: "gradients halftone" });
      else if (s.colorCount > 8) out.push({ id: "screen-colors", severity: "warn", title: `${colorsLabel} detected, a lot of screens for one print`, detail: "Each color adds a screen and cost. Simplify the palette, or switch to DTG.", short: `${s.colorCount} colors` });
      else if (s.colorCount > 0) out.push({ id: "screen-colors", severity: s.colorCount === 1 ? "pass" : "info", title: s.colorCount === 1 ? "Single-color design, the most economical screen print" : `${colorsLabel} detected (each color adds a screen)`, short: colorsLabel });
      if (s.semiTransparentFraction > 0.1) out.push({ id: "screen-transparency", severity: "warn", title: "Soft shadows or semi-transparent areas won't reproduce with opaque inks", detail: "Flatten transparency effects, or choose DTG which prints them faithfully.", short: "soft transparency" });
      if (s.whiteHalo) out.push({ id: "halo", severity: "warn", title: "Light halo around the edges (usually left over from background removal)", detail: "We can clean the edges on proof, or re-export the art with a clean cutout.", short: "edge halo" });
    }
    if (method === "dtg") {
      if (s.continuousTone) out.push({ id: "dtg-tone", severity: "pass", title: "Full-color artwork, exactly what DTG is for", short: "full color" });
      if (s.whiteHalo) out.push({ id: "halo", severity: "warn", title: "Light halo around the edges may show on dark garments", detail: "We can clean the edges on proof, or re-export the art with a clean cutout.", short: "edge halo" });
    }
    if (method === "embroidery") {
      if (s.continuousTone) out.push({ id: "emb-tone", severity: "fail", title: "Gradients and photo detail can't be stitched", detail: "Embroidery uses solid thread colors. Simplify to flat shapes, or choose a print method.", short: "gradients can't stitch" });
      const thin = s.inkFraction === 0 ? null : s.thinInkFraction[Math.min(3, Math.max(1, Math.round(1 / (2 * (25.4 * widthIn(printedWidthIn) / s.analysisW))))) - 1] ?? null;
      if (thin != null && thin > 0.55) out.push({ id: "emb-thin", severity: "fail", title: "Mostly fine lines, too thin to embroider at this size", detail: "Stitches need lines about 1 mm and up. Thicken the strokes or embroider it larger.", short: "lines too thin" });
      else if (thin != null && thin > 0.32) out.push({ id: "emb-thin", severity: "warn", title: "Fine lines sit near the ~1 mm stitch minimum", detail: "Small text and hairlines may fill in. We'll adjust the digitizing on proof.", short: "fine lines" });
      if (!s.continuousTone && s.colorCount > 9) out.push({ id: "emb-colors", severity: "warn", title: `${colorsLabel} means many thread changes`, detail: "Most embroidery looks best under 9 thread colors.", short: `${s.colorCount} thread colors` });
      if (widthIn(printedWidthIn) > 10) out.push({ id: "emb-size", severity: "warn", title: `Very large stitch area (~${widthIn(printedWidthIn).toFixed(0)} in wide)`, detail: "Big fills mean high stitch counts and cost. Chest-size embroidery is the sweet spot.", short: "large stitch area" });
      if (s.semiTransparentFraction > 0.1 && !s.continuousTone) out.push({ id: "emb-transparency", severity: "warn", title: "Soft transparency effects can't be stitched", detail: "Embroidery is solid thread. Flatten the art to hard shapes.", short: "soft transparency" });
    }
  }
  if (a.sourceW > 0 && a.sourceH > 0) {
    const wIn = widthIn(printedWidthIn), hIn = wIn * (a.sourceH / a.sourceW), max = MAX_AREA_IN[method];
    if (!(wIn <= max.w && hIn <= max.h)) out.push({ id: "print-area", severity: "warn", title: `Larger than the standard ${METHOD_LABEL[method].toLowerCase()} area (~${wIn.toFixed(0)} \xD7 ${hIn.toFixed(0)} in)`, detail: `Most ${METHOD_LABEL[method].toLowerCase()} tops out around ${max.w} \xD7 ${max.h} in. We'll confirm oversize options, or size it down, on your proof.`, short: "over max print size" });
  }
  return out.sort((x, y) => SEVERITY_ORDER[x.severity] - SEVERITY_ORDER[y.severity]);
}
function evaluatePreflight(analysis, { method, printedWidthIn, garmentHex }) {
  const own = findings(analysis, method, printedWidthIn, garmentHex);
  const methodFits = ["screen", "dtg", "embroidery"].map((m) => {
    const f = m === method ? own : findings(analysis, m, printedWidthIn, garmentHex);
    const top = f[0];
    const status2 = top && top.severity === "fail" ? "avoid" : top && top.severity === "warn" ? "caution" : "ready";
    return { method: m, label: METHOD_LABEL[m], status: status2, note: status2 === "ready" ? void 0 : f.find((x) => x.severity !== "pass")?.short };
  }).sort((a, b) => a.method === method ? -1 : b.method === method ? 1 : 0);
  const fail = own.find((f) => f.severity === "fail"), warn = own.find((f) => f.severity === "warn");
  const status = fail ? "fix" : warn ? "review" : "ready";
  const facts = [];
  if (analysis.isVector) facts.push("Vector SVG");
  else {
    facts.push(`${analysis.sourceW} \xD7 ${analysis.sourceH} px`);
    if (analysis.meta.vectorSource) facts.push(`Vector source (${analysis.meta.vectorSource.toUpperCase()})`);
    else facts.push(`${Math.round(analysis.sourceW / widthIn(printedWidthIn))} DPI at ${widthIn(printedWidthIn).toFixed(1)} in`);
  }
  const s = analysis.stats;
  if (s) {
    facts.push(s.continuousTone ? "Full-color art" : `${s.colorCount} ${s.colorCount === 1 ? "color" : "colors"}`);
    if (s.hasAlpha && !s.solidBackground) facts.push("Transparent background");
    else if (s.solidBackground?.nearWhite) facts.push("White background");
    else if (s.solidBackground) facts.push("Solid background");
  }
  return {
    status,
    headline: status === "fix" ? `Needs fix: ${fail.short}` : status === "review" ? `Check: ${warn.short}` : "Print ready",
    facts: facts.slice(0, 4),
    findings: own,
    methodFits,
    dpi: analysis.isVector ? null : Math.round(analysis.sourceW / widthIn(printedWidthIn)),
    colorCount: s?.colorCount ?? null,
    flags: own.filter((f) => f.severity === "warn" || f.severity === "fail").map((f) => f.short)
  };
}

// piobox-customizer/core/compose.js
function drawGarment(ctx, img, x, size, mirrored = false, pad = 0) {
  const inner = Math.max(1, size - 2 * pad);
  const k = Math.min(inner / img.naturalWidth, inner / img.naturalHeight);
  const w = img.naturalWidth * k, h2 = img.naturalHeight * k;
  ctx.save();
  if (mirrored) {
    ctx.translate(x + size / 2, 0);
    ctx.scale(-1, 1);
    ctx.translate(-(x + size / 2), 0);
  }
  ctx.drawImage(img, x + (size - w) / 2, (size - h2) / 2, w, h2);
  ctx.restore();
}
function drawLayer(ctx, img, layer, x, size) {
  const { artwork, placement } = layer;
  const aspect = artwork.naturalW > 0 && artwork.naturalH > 0 ? artwork.naturalH / artwork.naturalW : 1;
  const w = placement.width * size, h2 = w * aspect;
  ctx.save();
  if (layer.clipRect) {
    const c = layer.clipRect;
    ctx.beginPath();
    ctx.rect(x + c.x * size, c.y * size, c.w * size, c.h * size);
    ctx.clip();
  }
  ctx.translate(x + placement.cx * size, placement.cy * size);
  ctx.rotate(placement.rotation * Math.PI / 180);
  if (placement.flipX) ctx.scale(-1, 1);
  ctx.drawImage(img, -w / 2, -h2 / 2, w, h2);
  ctx.restore();
}
async function composeMockupPng(mockupSrc, layers, { mirrored = false, badge } = {}) {
  try {
    const [garment, arts] = await Promise.all([loadImage(mockupSrc), Promise.all(layers.map((l) => loadImage(l.artwork.url)))]);
    const S = 2e3, canvas = document.createElement("canvas");
    canvas.width = S;
    canvas.height = S;
    const ctx = canvas.getContext("2d");
    if (!ctx) return null;
    ctx.imageSmoothingEnabled = true;
    ctx.imageSmoothingQuality = "high";
    ctx.fillStyle = "#ffffff";
    ctx.fillRect(0, 0, S, S);
    drawGarment(ctx, garment, 0, S, mirrored, S * STAGE_PAD_FRAC);
    layers.forEach((l, i) => drawLayer(ctx, arts[i], l, 0, S));
    if (badge) {
      ctx.font = "600 60px ui-sans-serif, system-ui, -apple-system, 'Segoe UI', sans-serif";
      ctx.textAlign = "left";
      ctx.textBaseline = "middle";
      const w = ctx.measureText(badge).width + 88, r = 55;
      ctx.beginPath();
      ctx.moveTo(70 + r, 70);
      ctx.arcTo(70 + w, 70, 70 + w, 180, r);
      ctx.arcTo(70 + w, 180, 70, 180, r);
      ctx.arcTo(70, 180, 70, 70, r);
      ctx.arcTo(70, 70, 70 + w, 70, r);
      ctx.closePath();
      ctx.fillStyle = "rgba(17,17,17,0.85)";
      ctx.fill();
      ctx.fillStyle = "#ffffff";
      ctx.fillText(badge, 114, 126);
    }
    return await new Promise((res) => canvas.toBlob(res, "image/png"));
  } catch {
    return null;
  }
}
async function composeFinalPreviewPng(sides) {
  const used = sides.filter((s) => s.layers.length > 0);
  if (used.length === 0) return null;
  try {
    const tile = 1600, gap = 48, W = 80 + tile * used.length + gap * (used.length - 1), H = 1704;
    const loaded = await Promise.all(used.map(async (s) => ({ side: s, garment: await loadImage(s.mockupSrc), arts: await Promise.all(s.layers.map((l) => loadImage(l.artwork.url))) })));
    const canvas = document.createElement("canvas");
    canvas.width = W;
    canvas.height = H;
    const ctx = canvas.getContext("2d");
    if (!ctx) return null;
    ctx.imageSmoothingEnabled = true;
    ctx.imageSmoothingQuality = "high";
    ctx.fillStyle = "#ffffff";
    ctx.fillRect(0, 0, W, H);
    loaded.forEach(({ side, garment, arts }, i) => {
      const x = 40 + (tile + gap) * i;
      drawGarment(ctx, garment, x, tile, side.mirrored, tile * STAGE_PAD_FRAC);
      side.layers.forEach((l, k) => drawLayer(ctx, arts[k], l, x, tile));
      ctx.fillStyle = "#111111";
      ctx.font = "600 30px ui-sans-serif, system-ui, -apple-system, 'Segoe UI', sans-serif";
      ctx.textAlign = "center";
      ctx.textBaseline = "middle";
      ctx.fillText(side.label, x + tile / 2, 1672);
    });
    return await new Promise((res) => canvas.toBlob(res, "image/png"));
  } catch {
    return null;
  }
}
async function blobToPreviewDataUrl(blob, maxWidth = 1200, maxChars = 21e5) {
  try {
    const bmp = await createImageBitmap(blob);
    const k = Math.min(1, maxWidth / bmp.width), w = Math.max(1, Math.round(bmp.width * k)), h2 = Math.max(1, Math.round(bmp.height * k));
    const canvas = document.createElement("canvas");
    canvas.width = w;
    canvas.height = h2;
    const ctx = canvas.getContext("2d");
    if (!ctx) return null;
    ctx.drawImage(bmp, 0, 0, w, h2);
    bmp.close();
    const toBlob = (type, q) => new Promise((res) => canvas.toBlob(res, type, q));
    const webp = await toBlob("image/webp", 0.9);
    const out = webp && webp.type === "image/webp" ? webp : await toBlob("image/png");
    if (!out) return null;
    const dataUrl = await new Promise((res) => {
      const r = new FileReader();
      r.onload = () => res(String(r.result));
      r.onerror = () => res(null);
      r.readAsDataURL(out);
    });
    return !dataUrl || dataUrl.length > maxChars ? null : dataUrl;
  } catch {
    return null;
  }
}

// piobox-customizer/core/pricing.js
var SETUP_HEAVY_HINT = "Setup is heavy at this qty \u2014 around 25 pieces the unit price starts making sense.";
var SEPARATIONS_NOTE = "Colour separations quoted separately.";
var STITCH_NOTE = "Stitch count confirmed on proof; extra 1K stitches are R6 each (sheet).";
var INNER_NECK_NOTE = "Supplier inner-neck MOQ is 25; we still take qty 1.";
var OVERSIZED_QUOTE = "Oversized DTG (over A3 / 40\xD750 cm) is a quote.";
var DEFAULT_PRICING = {
  currency: "ZAR",
  locale: "en-ZA",
  minimumQty: 1,
  roundUnitTo: 0,
  // Catalog did not give ZAR blank sell prices. Skip garment lines until Shopify Ops passes real ones.
  blankSellPrices: false,
  sizeBands: [
    { id: "a5", maxWidthIn: 5.83, label: "A5" },
    { id: "a4", maxWidthIn: 8.27, label: "A4" },
    { id: "a3", maxWidthIn: 11.69, label: "A3" },
    { id: "oversized", maxWidthIn: 99, label: "Oversized" }
  ],
  methods: {
    dtg: {
      label: "DTG",
      setupPerScreen: 0,
      nextBreaks: [6, 16, 26, 51, 101],
      // Qty band × A5/A4/A3. Unlimited colours. No setup.
      white: [
        { minQty: 1, a5: 6e3, a4: 7900, a3: 8600 },
        { minQty: 6, a5: 5300, a4: 6800, a3: 7800 },
        { minQty: 16, a5: 4300, a4: 5800, a3: 6600 },
        { minQty: 26, a5: 4e3, a4: 5400, a3: 6100 },
        { minQty: 51, a5: 3700, a4: 4900, a3: 5800 },
        { minQty: 101, a5: 3500, a4: 4600, a3: 5500 }
      ],
      colour: [
        { minQty: 1, a5: 8600, a4: 13900, a3: 18500 },
        { minQty: 6, a5: 7300, a4: 12400, a3: 17e3 },
        { minQty: 16, a5: 5800, a4: 11e3, a3: 14900 },
        { minQty: 26, a5: 5400, a4: 10200, a3: 13700 },
        { minQty: 51, a5: 4900, a4: 9500, a3: 11800 },
        { minQty: 101, a5: 4600, a4: 9200, a3: 11e3 }
      ]
    },
    dtf: {
      label: "DTF",
      setupPerScreen: 0,
      nextBreaks: [6, 16, 26, 51, 101],
      innerNeckPerPrint: 1500,
      // R15. Supplier MOQ 25 is a note, not a blocker.
      // All colour garments, print + application. No setup except inner-neck sheet rate.
      bands: [
        { minQty: 1, a5: 4500, a4: 7e3, a3: 11800 },
        { minQty: 6, a5: 3900, a4: 6300, a3: 10800 },
        { minQty: 16, a5: 3600, a4: 6e3, a3: 9800 },
        { minQty: 26, a5: 3300, a4: 5700, a3: 9300 },
        { minQty: 51, a5: 3100, a4: 5400, a3: 8800 },
        { minQty: 101, a5: 2900, a4: 5100, a3: 8400 }
      ]
    },
    screen: {
      label: "Screen print",
      maxColors: 8,
      setupPerScreen: 35e3,
      // R350 per screen (per colour per placement). Do not waive.
      nextBreaks: [25, 50, 100, 200, 500],
      // Qty 1–24 uses the 25–49 column (do not invent a 1–24 cell).
      bands: [
        { minQty: 25, perColor: [null, 3400, 3650, 3900, 4400, 4900, 5150, 5900, 6400] },
        { minQty: 50, perColor: [null, 2700, 2950, 3200, 3700, 4200, 4450, 5300, 5700] },
        { minQty: 100, perColor: [null, 2140, 2390, 2640, 3140, 3640, 3890, 4640, 5140] },
        { minQty: 200, perColor: [null, 1863, 2088, 2313, 2763, 3213, 3438, 4113, 4563] },
        { minQty: 500, perColor: [null, 1628, 1828, 2028, 2428, 2828, 3028, 3628, 4028] }
      ]
    },
    embroidery: {
      label: "Embroidery",
      setupPerPlacement: 3e4,
      // R300 digitisation per artwork / placement. Do not waive.
      nextBreaks: [25, 50, 100],
      // Unknown stitch count → 0–5K band only. Do not invent +1K charges.
      bands: [
        { minQty: 25, base: 4800 },
        { minQty: 50, base: 3600 },
        { minQty: 100, base: 3e3 }
      ]
    }
  },
  // Catalog did not give a custom-colour garment surcharge. Keep 0; no min-qty blocker.
  customColor: { surchargePerUnit: 0, label: "Custom garment colour" }
};
var roundTo = (v, step) => step > 0 ? Math.max(step, Math.round(v / step) * step) : v;
var bandFor = (bands, qty) => {
  if (!bands?.length) return null;
  const sorted = [...bands].sort((a, b) => a.minQty - b.minQty);
  return sorted.reduce((best, b) => qty >= b.minQty ? b : best, sorted[0]);
};
var sizeBandFor = (pricing, widthIn2) => {
  const bands = pricing.sizeBands ?? [];
  if (!bands.length) return { id: "a5", label: "A5", maxWidthIn: 5.83 };
  return bands.find((b) => widthIn2 <= b.maxWidthIn) ?? bands[bands.length - 1];
};
function formatMoney(minor, pricing) {
  try {
    return new Intl.NumberFormat(pricing.locale ?? "en-ZA", { style: "currency", currency: pricing.currency ?? "ZAR" }).format(minor / 100);
  } catch {
    return `${(minor / 100).toFixed(2)}`;
  }
}
function inkColorsForLayer(layer, analysis, assumed = 4) {
  if (layer.text) return layer.text.outline ? 2 : 1;
  const stats = analysis?.stats;
  if (!stats) return assumed;
  if (stats.continuousTone) return Infinity;
  return Math.max(1, stats.colorCount || assumed);
}
function hexLooksWhite(hex) {
  const s = String(hex ?? "").trim().replace(/^#/, "").toLowerCase();
  const h2 = s.length === 3 ? `${s[0]}${s[0]}${s[1]}${s[1]}${s[2]}${s[2]}` : s;
  if (!/^[0-9a-f]{6}$/.test(h2)) return false;
  if (h2 === "ffffff" || h2 === "f4f4f5") return true;
  const r = parseInt(h2.slice(0, 2), 16), g = parseInt(h2.slice(2, 4), 16), b = parseInt(h2.slice(4, 6), 16);
  return Math.max(r, g, b) - Math.min(r, g, b) <= 16 && Math.min(r, g, b) >= 240;
}
function garmentIsWhiteFromSelection(sel) {
  if (!sel) return false;
  if (sel.kind === "pantone") return false;
  const id = String(sel.color?.id ?? "").toLowerCase();
  const name = String(sel.color?.name ?? sel.name ?? "").toLowerCase();
  if (sel.kind === "stocked") {
    if (/(pfd-white|\bpfd\b|white)/.test(id) || /pfd|\bwhite\b/.test(name)) return true;
    return false;
  }
  const hex = sel.kind === "custom" ? sel.hex : sel.color?.hex;
  return hexLooksWhite(hex);
}
function isInnerNeckPlacement(p) {
  const hay = `${p.id ?? ""} ${p.label ?? ""} ${p.area ?? ""}`.toLowerCase();
  const stripped = hay.replace(/crew[\s_-]*neck/g, " ");
  return /\binner[\s_-]*neck\b/.test(stripped) || /\bback[\s_-]*neck\b/.test(stripped) || /\bneck\b/.test(stripped);
}
function aKey(sizeBand) {
  const id = sizeBand?.id;
  return id === "a5" || id === "a4" || id === "a3" ? id : null;
}
function pushNote(notes, level, text) {
  if (!text || notes.some((n) => n.text === text)) return;
  notes.push({ level, text });
}
function priceDesign({ quantity, garmentUnitPrice = 0, garment = null, method, placements = [], customColor = false, garmentIsWhite = false, pricing = DEFAULT_PRICING, _skipNextBreak = false }) {
  const qty = Math.max(0, Math.trunc(quantity) || 0);
  const lookupQty = Math.max(1, qty);
  const m = pricing.methods[method] ?? pricing.methods.dtg ?? pricing.methods.screen;
  const methodId = pricing.methods[method] ? method : pricing.methods.dtg ? "dtg" : "screen";
  const lines = [];
  const notes = [];
  const blockers = [];
  const includeBlank = pricing.blankSellPrices === true;
  if (includeBlank) {
    if (garment && garment.length) {
      for (const g of garment) if (g.qty > 0 && g.unit > 0) lines.push({ id: `garment-${g.label}`, kind: "garment", label: `Garment \xB7 ${g.label}`, unit: g.unit, qty: g.qty, total: g.unit * g.qty });
    } else if (garmentUnitPrice > 0) {
      lines.push({ id: "garment", kind: "garment", label: "Garment", unit: garmentUnitPrice, qty, total: garmentUnitPrice * qty });
    }
  }
  let setupTotal = 0;
  let decorationUnit = 0;
  const step = pricing.roundUnitTo ?? 0;
  for (const p of placements) {
    const sizeBand = sizeBandFor(pricing, p.widthIn ?? 0);
    const colorsRaw = p.colors;
    const finiteColors = Number.isFinite(colorsRaw);
    const colors = finiteColors ? Math.max(1, colorsRaw) : colorsRaw;
    const label = p.label ?? p.id ?? "Print";
    if (methodId === "dtg" || methodId === "dtf") {
      const innerNeck = methodId === "dtf" && isInnerNeckPlacement(p);
      const sizeKey = aKey(sizeBand);
      if (!innerNeck && !sizeKey) {
        pushNote(notes, "warn", OVERSIZED_QUOTE);
        blockers.push({ id: `oversized-${p.id}`, text: OVERSIZED_QUOTE, quoteInstead: true });
        continue;
      }
      let unit;
      let meta;
      if (innerNeck) {
        unit = roundTo(m.innerNeckPerPrint ?? 1500, step);
        meta = "Inner neck";
        pushNote(notes, "info", INNER_NECK_NOTE);
      } else {
        const table = methodId === "dtg" ? garmentIsWhite ? m.white : m.colour : m.bands;
        const band2 = bandFor(table, lookupQty);
        unit = roundTo(band2?.[sizeKey] ?? 0, step);
        meta = methodId === "dtg" ? `${sizeBand.label} \xB7 ${garmentIsWhite ? "white garment" : "colour garment"} \xB7 unlimited colours` : `${sizeBand.label} \xB7 print + application`;
      }
      decorationUnit += unit;
      lines.push({
        id: `print-${p.id}`,
        kind: "decoration",
        label: `${m.label} \xB7 ${label}`,
        meta,
        unit,
        qty,
        total: unit * qty
      });
    } else if (methodId === "screen") {
      pushNote(notes, "info", SEPARATIONS_NOTE);
      if (!finiteColors) {
        pushNote(notes, "warn", `\u201C${label}\u201D is full-colour / continuous-tone artwork. Screen printing has no full-colour rate on the sheet \u2014 switch to DTG or DTF, or we\u2019ll quote a halftone.`);
        blockers.push({ id: `screen-fullcolour-${p.id}`, text: `\u201C${label}\u201D needs a quote for screen (no full-colour rate). Try DTG or DTF.`, quoteInstead: true });
        continue;
      }
      if (colors > (m.maxColors ?? 8)) {
        pushNote(notes, "warn", `\u201C${label}\u201D has ${colors} colours, over the ${m.maxColors}-screen limit. Simplify the artwork or choose DTG/DTF. No 9th-colour rate on the sheet.`);
        blockers.push({ id: `screen-colors-${p.id}`, text: `\u201C${label}\u201D has ${colors} colours (max ${m.maxColors} on the sheet). Simplify or choose DTG/DTF.`, quoteInstead: true });
        continue;
      }
      const band2 = bandFor(m.bands, lookupQty);
      const nScreens = Math.min(colors, m.maxColors ?? 8);
      const unit = roundTo(band2?.perColor?.[nScreens] ?? 0, step);
      decorationUnit += unit;
      lines.push({
        id: `print-${p.id}`,
        kind: "decoration",
        label: `${m.label} \xB7 ${label}`,
        meta: `${nScreens} ${nScreens === 1 ? "colour" : "colours"}`,
        unit,
        qty,
        total: unit * qty
      });
      const setup = roundTo((m.setupPerScreen ?? 0) * nScreens, step);
      if (setup > 0) {
        setupTotal += setup;
        lines.push({ id: `setup-${p.id}`, kind: "setup", label: `Screens \xB7 ${label}`, unit: setup, qty: 1, total: setup, oneOff: true });
      }
    } else if (methodId === "embroidery") {
      pushNote(notes, "info", STITCH_NOTE);
      const band2 = bandFor(m.bands, lookupQty);
      const unit = roundTo(band2?.base ?? 0, step);
      decorationUnit += unit;
      lines.push({
        id: `print-${p.id}`,
        kind: "decoration",
        label: `${m.label} \xB7 ${label}`,
        meta: "0\u20135K stitches",
        unit,
        qty,
        total: unit * qty
      });
      const setup = roundTo(m.setupPerPlacement ?? 0, step);
      if (setup > 0) {
        setupTotal += setup;
        lines.push({ id: `setup-${p.id}`, kind: "setup", label: `Digitising \xB7 ${label}`, unit: setup, qty: 1, total: setup, oneOff: true });
      }
    }
  }
  if ((methodId === "screen" || methodId === "embroidery") && qty > 0 && qty < 25 && placements.length > 0) {
    pushNote(notes, "info", SETUP_HEAVY_HINT);
  }
  let customColorTotal = 0;
  const cc = pricing.customColor;
  const ccUnit = roundTo(cc?.surchargePerUnit ?? 0, step);
  if (customColor && cc && ccUnit > 0) {
    customColorTotal = ccUnit * qty;
    lines.push({ id: "custom-color", kind: "surcharge", label: cc.label ?? "Custom garment colour", unit: ccUnit, qty, total: customColorTotal });
    if (cc.minQty && qty > 0 && qty < cc.minQty) {
      blockers.push({ id: "custom-color-min", text: `A custom garment colour needs ${cc.minQty}+ pieces. Pick a stocked colour, or request a quote and we\u2019ll spec it with you.`, quoteInstead: true });
    }
  }
  const subtotal = lines.reduce((n, l) => n + l.total, 0);
  const unitPrice = qty > 0 ? Math.round(subtotal / qty) : garmentUnitPrice + decorationUnit;
  if (qty > 0 && qty < (pricing.minimumQty ?? 1)) blockers.push({ id: "min-qty", text: `Minimum order is ${pricing.minimumQty} pieces.` });
  let nextBreak = null;
  if (!_skipNextBreak && placements.length > 0) {
    const thresholds = m.nextBreaks ?? (m.bands ?? m.white ?? []).map((b) => b.minQty);
    for (const nextQty of thresholds) {
      if (!(nextQty > qty)) continue;
      const scale = qty > 0 ? nextQty / qty : 1;
      const at = priceDesign({
        quantity: nextQty,
        garmentUnitPrice: includeBlank ? garmentUnitPrice : 0,
        garment: includeBlank ? garment?.map((g) => ({ ...g, qty: Math.round(g.qty * scale) })) ?? null : null,
        method: methodId,
        placements,
        customColor,
        garmentIsWhite,
        pricing,
        _skipNextBreak: true
      });
      if (at.unitPrice < unitPrice) {
        nextBreak = { qty: nextQty, unitPrice: at.unitPrice, saving: unitPrice - at.unitPrice };
        break;
      }
    }
  }
  const band = methodId === "dtg" ? bandFor(garmentIsWhite ? m.white : m.colour, lookupQty) : bandFor(m.bands, lookupQty);
  return { lines, subtotal, unitPrice, quantity: qty, setupTotal, decorationUnit, garmentUnitPrice: includeBlank ? garmentUnitPrice : 0, method: methodId, currency: pricing.currency, notes, blockers, nextBreak, band };
}

// piobox-customizer/customizer.js
var SIDES = ["front", "back"];
var SIDE_LABEL2 = { front: "Front", back: "Back" };
var clamp5 = (v, lo, hi) => Math.min(hi, Math.max(lo, v));
var uid = 0;
var newId = (p) => `${p}-${++uid}-${Math.random().toString(36).slice(2, 8)}`;
var DEFAULT_LABELS = {
  blanks: "Blanks",
  colorTab: "Color",
  designTab: "Artwork & text",
  quoteTab: "Request a quote",
  orderTab: "Size & price",
  submit: "Get pricing for this blank",
  submitWithDesign: "Get pricing for this design",
  addToCart: "Add to cart",
  addingToCart: "Adding to cart...",
  sizeLabel: "How many of each size?",
  sizeHint: "Pick your size run. Pricing updates as the quantity grows.",
  quantityLabel: "How many pieces?",
  quantityHint: "Quantity cutoffs where per-piece pricing usually improves. We confirm exact pricing on your written quote.",
  emailLabel: "Save your progress (optional)",
  emailHint: "We'll email you a link so you can pick up this design later. That's all it does.",
  noArtworkHint: "No artwork yet? Continue with the colored blank and send your design later.",
  submitFootnote: "Last step: add your contact details on the next screen and we'll send pricing and a production proof. No payment now.",
  colorDisclaimer: "On-screen swatches and garment previews are visual approximations. Final color is confirmed against a physical Pantone reference or approved production sample."
};
function mergePricing(custom) {
  const base = DEFAULT_PRICING;
  return {
    ...base,
    ...custom,
    sizeBands: custom.sizeBands ?? base.sizeBands,
    blankSellPrices: custom.blankSellPrices ?? base.blankSellPrices,
    customColor: custom.customColor === null ? null : { ...base.customColor ?? {}, ...custom.customColor ?? {} },
    methods: Object.fromEntries(Object.entries({ ...base.methods, ...custom.methods ?? {} }).map(([k, v]) => [k, { ...base.methods[k] ?? {}, ...v }]))
  };
}
function mount(host, config = {}) {
  const cfg = {
    products: config.products ?? DEFAULT_CATALOG,
    photoBase: (config.photoBase ?? "assets/photos").replace(/\/$/, ""),
    fontBase: (config.fontBase ?? "assets/fonts").replace(/\/$/, ""),
    photoFrames: { ...PHOTO_FRAMES, ...config.photoFrames ?? {} },
    fonts: config.fonts ?? [],
    allowFontUpload: config.allowFontUpload ?? true,
    sampleLogo: config.sampleLogo ?? null,
    draftKey: config.draftKey ?? "piobox-customizer-draft-v1",
    persistDraft: config.persistDraft ?? true,
    initialProductId: config.initialProductId ?? new URLSearchParams(location.search).get("style") ?? null,
    initialColor: config.initialColor ?? new URLSearchParams(location.search).get("color") ?? null,
    labels: { ...DEFAULT_LABELS, ...config.labels ?? {} },
    onSubmit: config.onSubmit ?? null,
    onShare: config.onShare ?? null,
    onChange: config.onChange ?? null,
    onEvent: config.onEvent ?? null,
    pdfjsUrl: config.pdfjsUrl,
    pdfjsWorkerUrl: config.pdfjsWorkerUrl,
    defaultQuantity: String(config.defaultQuantity ?? 1),
    defaultMethod: config.defaultMethod ?? "dtg",
    // "cart" shows live pricing and an Add to cart button; "quote" keeps the enquiry flow.
    mode: config.mode ?? (config.pricing ? "cart" : "quote"),
    pricing: config.pricing ? mergePricing(config.pricing) : DEFAULT_PRICING,
    modal: config.modal ?? false,
    onClose: config.onClose ?? null
  };
  if (config.photoProtection) registerPhotoProtection(config.photoProtection);
  const fonts = new FontRegistry({ fontBase: cfg.fontBase, fonts: cfg.fonts, allowUpload: cfg.allowFontUpload });
  const emit = (name, data) => {
    try {
      cfg.onEvent?.(name, data);
    } catch {
    }
  };
  const products = cfg.products;
  const findProduct = (idOrCode) => {
    if (!idOrCode) return null;
    const q = String(idOrCode).toLowerCase();
    return products.find((p) => p.id.toLowerCase() === q || p.styleNumber.toLowerCase() === q || p.styleNumber.toLowerCase().replace(/[^a-z0-9]+/g, "-") === q) ?? null;
  };
  const initialProduct = findProduct(cfg.initialProductId) ?? products[0];
  const S = {
    products,
    productId: initialProduct.id,
    product: initialProduct,
    designs: [{ id: newId("design"), productId: initialProduct.id, colorSel: defaultStockedSelection(initialProduct), stash: null, pending: null }],
    activeDesignId: null,
    colorSel: defaultStockedSelection(initialProduct),
    colorUi: { mode: "library", query: "", family: "all", code: "", hsv: null, hexDraft: null, recent: [] },
    sides: { front: [], back: [] },
    side: "front",
    activeLayerId: null,
    method: cfg.defaultMethod,
    quantity: cfg.defaultQuantity,
    notes: "",
    email: "",
    sizes: {},
    mode: cfg.mode,
    pricing: cfg.pricing,
    open: !cfg.modal,
    // Collapse the blanks column when the product is already chosen (one product, or a pop-out).
    blanksCollapsed: config.collapseBlanks ?? (config.products?.length === 1 || !!config.modal),
    tab: "design",
    blanksQuery: "",
    blanksCategory: "all",
    blanksExpanded: false,
    error: null,
    notice: null,
    largeArtNotice: false,
    preview: { open: false, busy: false, images: [], failed: false },
    result: null,
    share: "idle",
    shareExpires: null,
    shareLink: null,
    saving: false,
    submitting: false,
    undo: { layer: null, design: null },
    preflight: null,
    preflightOpen: false,
    fontManager: false,
    labels: cfg.labels,
    fonts,
    allowFontUpload: cfg.allowFontUpload,
    sampleLogo: cfg.sampleLogo
  };
  S.activeDesignId = S.designs[0].id;
  const mirrors = /* @__PURE__ */ new Map();
  const analyses = /* @__PURE__ */ new Map();
  const objectUrls = /* @__PURE__ */ new Set();
  let generation = 0, textTimer = null, textToken = 0, undoLayerTimer = null, undoDesignTimer = null, handedOff = false;
  const recolorState = /* @__PURE__ */ new Map();
  const photoUrl = (name) => name ? /^(https?:|data:|blob:|\/)/.test(name) ? name : `${cfg.photoBase}/${name}` : null;
  const frameFor = (name) => name ? cfg.photoFrames[name.split("?")[0].split("/").pop()] : void 0;
  const hex = () => selectionHex(S.colorSel);
  function stockedPhoto(product, side) {
    const sel = S.colorSel;
    const map = side === "back" ? product.backColorwayImages : product.colorwayImages;
    if (sel.kind === "stocked" && map?.[sel.color.id]) return map[sel.color.id];
    const kind = stockedKindForHex(selectionHex(sel));
    if (kind === "white") return side === "back" ? product.photos.back : product.photos.front;
    if (kind === "black") return side === "back" ? product.photos.backBlack : product.photos.frontBlack;
    return null;
  }
  const basePhoto = (product, side) => side === "back" ? product.photos.back : product.photos.front;
  const framePhoto = (product, side) => stockedPhoto(product, side) ?? basePhoto(product, side) ?? product.photos.front;
  const hasBack = (product) => !!product.photos.back;
  function layoutFor(product, side) {
    return garmentPrintLayout({ category: product.category, name: product.name, fit: product.fit, side, frame: frameFor(framePhoto(product, side)), mirrored: side === "back" && !hasBack(product) });
  }
  function mockupFor(side) {
    const p = S.product, stocked = stockedPhoto(p, side);
    if (stocked) return photoUrl(stocked);
    const base = basePhoto(p, side);
    if (!base) return null;
    const src = photoUrl(base), mask = side === "front" && p.photos.frontMask ? photoUrl(p.photos.frontMask) : void 0, color = hex();
    const key = `${p.recolorMode ?? "garment"}|${color}|${src}|${mask ?? ""}`;
    const cached = recolorState.get(key) ?? (p.recolorMode === "cap" ? null : peekRecolored(src, color, mask));
    if (cached) return cached;
    if (!recolorState.has(key)) {
      recolorState.set(key, null);
      const job = p.recolorMode === "cap" ? recolorCapGarment(src, color) : recolorGarment(src, color, mask);
      job.then((url) => {
        recolorState.set(key, url ?? src);
        schedule();
      }).catch(() => {
        recolorState.set(key, src);
        schedule();
      });
    }
    return src;
  }
  const layerTotal = () => (hasBack(S.product) ? SIDES : ["front"]).reduce((n, side) => n + S.sides[side].filter((l) => !l.text || !isTextEmpty(l.text)).length, 0);
  const activeLayer = () => S.sides[S.side].find((l) => l.id === S.activeLayerId) ?? null;
  const quantityNumber = () => {
    const n = Number.parseInt(S.quantity.replace(/[,_\s]/g, ""), 10);
    return Number.isFinite(n) ? n : null;
  };
  const submitLabel = () => {
    if (S.mode !== "cart") return layerTotal() > 0 ? cfg.labels.submitWithDesign : cfg.labels.submit;
    return S.submitting ? cfg.labels.addingToCart : cfg.labels.addToCart;
  };
  const sizeList = () => S.product.sizes ?? [];
  const variantFor = (size) => {
    const vs = S.product.shopify?.variants;
    if (!vs) return null;
    const colorId = S.colorSel.kind === "stocked" ? S.colorSel.color.id : null;
    return vs.find((v) => v.size === size && (colorId ? v.colorwayId === colorId : true)) ?? null;
  };
  function sizeRows() {
    return sizeList().map((size) => {
      const v = variantFor(size);
      const blank = cfg.pricing.blankSellPrices === true;
      return { size, quantity: Math.max(0, Math.trunc(S.sizes[size] ?? 0)), variantId: v?.id ?? null, unit: blank ? v?.price ?? S.product.price ?? 0 : 0, available: v ? v.available : true };
    });
  }
  const sizeTotal = () => sizeRows().reduce((n, r) => n + r.quantity, 0);
  const orderQty = () => S.mode === "cart" && sizeList().length ? sizeTotal() : quantityNumber() ?? 0;
  function placementsForPricing() {
    const groups = /* @__PURE__ */ new Map();
    for (const side of hasBack(S.product) ? SIDES : ["front"]) {
      const layout = layoutFor(S.product, side);
      for (const l of S.sides[side]) {
        if (l.text && isTextEmpty(l.text)) continue;
        const area = resolvePlacementArea(layout.areas, l.placement);
        const key = `${side}:${area?.id ?? side}`;
        let g = groups.get(key);
        if (!g) {
          g = { id: key, side, label: placementAreaLabel(layout, l.placement) ?? SIDE_LABEL2[side], left: 1, right: 0, inks: /* @__PURE__ */ new Set(), imageColors: 0, fullColor: false };
          groups.set(key, g);
        }
        const half = l.placement.width / 2;
        g.left = Math.min(g.left, l.placement.cx - half);
        g.right = Math.max(g.right, l.placement.cx + half);
        if (l.text) {
          g.inks.add(l.text.color.toLowerCase());
          if (l.text.outline) g.inks.add(l.text.outline.toLowerCase());
        } else {
          const n = inkColorsForLayer(l, analyses.get(l.artwork.url), cfg.pricing.assumedColors ?? 4);
          if (!Number.isFinite(n)) g.fullColor = true;
          else g.imageColors += n;
        }
      }
    }
    return [...groups.values()].map((g) => ({
      id: g.id,
      side: g.side,
      label: g.label,
      widthIn: Math.max(0, g.right - g.left) * STAGE_INCHES,
      colors: g.fullColor ? Infinity : Math.max(1, g.inks.size + g.imageColors)
    }));
  }
  function computePrice() {
    const rows = sizeRows();
    const useSizes = S.mode === "cart" && rows.length > 0;
    const allowBlank = cfg.pricing.blankSellPrices === true;
    return priceDesign({
      quantity: orderQty(),
      garment: allowBlank && useSizes ? rows.filter((r) => r.quantity > 0).map((r) => ({ label: r.size, qty: r.quantity, unit: r.unit })) : null,
      garmentUnitPrice: allowBlank ? useSizes ? 0 : S.product.price ?? 0 : 0,
      method: S.method,
      placements: placementsForPricing(),
      customColor: S.colorSel.kind !== "stocked",
      garmentIsWhite: garmentIsWhiteFromSelection(S.colorSel),
      pricing: cfg.pricing
    });
  }
  const root = document.createElement("div");
  root.className = "pc-shell";
  host.innerHTML = "";
  let overlay = null, lastFocus = null;
  if (cfg.modal) {
    host.classList.add("pc-host--modal");
    overlay = document.createElement("div");
    overlay.className = "pc-overlay";
    overlay.hidden = true;
    overlay.setAttribute("role", "dialog");
    overlay.setAttribute("aria-modal", "true");
    overlay.setAttribute("aria-label", `Design ${initialProduct.name}`);
    const backdrop = document.createElement("div");
    backdrop.className = "pc-overlay__backdrop";
    backdrop.addEventListener("click", () => closeModal());
    const panel2 = document.createElement("div");
    panel2.className = "pc-overlay__panel";
    const close = document.createElement("button");
    close.type = "button";
    close.className = "pc-overlay__close";
    close.setAttribute("aria-label", "Close design studio");
    close.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>';
    close.addEventListener("click", () => closeModal());
    panel2.append(close, root);
    overlay.append(backdrop, panel2);
    host.appendChild(overlay);
  } else {
    host.appendChild(root);
  }
  function openModal() {
    if (!overlay || S.open) return;
    lastFocus = document.activeElement;
    overlay.hidden = false;
    S.open = true;
    document.documentElement.classList.add("pc-scroll-lock");
    requestAnimationFrame(() => {
      overlay.querySelector(".pc-overlay__close")?.focus({ preventScroll: true });
      schedule();
    });
  }
  function closeModal() {
    if (!overlay || !S.open) return;
    overlay.hidden = true;
    S.open = false;
    document.documentElement.classList.remove("pc-scroll-lock");
    lastFocus?.focus?.({ preventScroll: true });
    cfg.onClose?.(getDesign());
  }
  let rendered2 = false, frame = null;
  const stage = createStage({
    onSelect: (id) => {
      S.activeLayerId = id;
      S.tab = "design";
      schedule();
    },
    onDeselect: () => {
      S.activeLayerId = null;
      schedule();
    },
    onPlacementChange: (id, fn) => updatePlacement(id, fn),
    onDelete: (id) => removeLayer(id),
    onFiles: (files) => files.forEach(acceptFile),
    onTextChange: (id, content) => {
      patchLayer(id, (l) => l.text ? { ...l, text: { ...l.text, content } } : l);
      scheduleTextRender(id);
      schedule();
    },
    onTextCommit: (id) => {
      if (textTimer) {
        clearTimeout(textTimer);
        textTimer = null;
      }
      rerenderText(id, ++textToken);
    }
  });
  function schedule() {
    if (frame) return;
    frame = requestAnimationFrame(() => {
      frame = null;
      render();
    });
  }
  function view() {
    const product = S.product, layout = layoutFor(product, S.side);
    const layer = activeLayer();
    const sideList = hasBack(product) ? SIDES : ["front"];
    const mockup = { front: mockupFor("front"), back: hasBack(product) ? mockupFor("back") : null };
    const preflightReport = layer && !layer.text && S.preflight?.url === layer.artwork.url ? evaluatePreflight(S.preflight.analysis, { method: S.method, printedWidthIn: layer.placement.width * STAGE_INCHES, garmentHex: hex() }) : null;
    let contrastSuggestion = null;
    if (preflightReport?.flags.includes("low garment contrast") && S.preflight?.analysis.stats) {
      const st = S.preflight.analysis.stats;
      const c = suggestContrastingGarment(st.colors, st.meanInkLuma, stockedPicksForProduct(product).map((p) => ({ hex: p.color.hex, label: p.label, color: p.color })));
      if (c && c.hex.toLowerCase() !== hex().toLowerCase()) contrastSuggestion = c;
    }
    const price = computePrice();
    const rows = sizeRows();
    return { ...S, layout, activeLayer: layer, sideList, mockup, price, sizeRows: rows, sizeTotal: sizeTotal(), orderQty: orderQty(), money: (v) => formatMoney(v, cfg.pricing), mirrored: { front: false, back: false }, layerTotal: layerTotal(), quantityNumber: quantityNumber(), activeDesignIndex: Math.max(0, S.designs.findIndex((d) => d.id === S.activeDesignId)), preflightReport, contrastSuggestion, quality: layer ? estimateArtworkQuality(layer.artwork.naturalW, layer.placement.width, layer.artwork.isVector) : null };
  }
  function render() {
    const s = view();
    const stageHolder = h("div", { class: "pc-stage-holder", "data-keep": "" });
    const tree = h(
      "div",
      { class: ["pc-shell", S.blanksCollapsed && "pc-shell--blanks-collapsed"] },
      renderDesignsRail(s, A),
      h(
        "section",
        { class: "pc-center", id: "pc-preview-panel", "aria-label": "Design preview" },
        renderAlerts(s, A),
        h("div", { class: "pc-center__row" }, renderLayersRail(s, A), stageHolder, renderSideTabs(s, A))
      ),
      renderBlanks(s, A),
      renderRightPanel(s, A),
      renderBottomBar(s, A),
      renderPreviewModal(s, A),
      renderResultModal(s, A),
      renderToasts(s, A)
    );
    if (!rendered2) {
      root.replaceWith(tree);
      rootRef = tree;
      rendered2 = true;
    } else morph(rootRef, tree);
    const holder = rootRef.querySelector(".pc-stage-holder");
    if (holder && stage.el.parentNode !== holder) holder.appendChild(stage.el);
    const areaLabel = s.layout.areas.find((ar) => ar.id === (s.activeLayer?.placement.area ?? S.side))?.label;
    const captioned = !!areaLabel && !/^(front|back)$/i.test(areaLabel.trim());
    stage.update({
      mockupSrc: s.mockup[S.side] ?? s.mockup.front,
      mockupAlt: s.product.name,
      mirrored: false,
      areas: s.layout.areas,
      side: S.side,
      category: s.product.category,
      layers: S.sides[S.side].map((l) => ({ id: l.id, url: l.artwork.url, naturalW: l.artwork.naturalW, naturalH: l.artwork.naturalH, placement: l.placement, text: l.text ? { content: l.text.content, color: l.text.color, fontFamily: fonts.canvasFamily(l.text.fontId), fontWeight: Number(fonts.weight(l.text.fontId)) } : void 0 })),
      activeLayerId: S.activeLayerId,
      disabled: S.submitting,
      textMaxChars: TEXT_MAX_CHARS,
      preparing: S.preparing > 0,
      empty: { title: S.side === "back" ? "Drop your back graphic here" : `Drop your art on the ${s.product.category === "Bottoms" ? "garment" : s.product.category === "Headwear" || s.product.category === "Accessories" ? s.layout.areas[0]?.label.toLowerCase() ?? "front panel" : "chest"}` },
      caption: captioned ? `${{ screen: "Screen print", embroidery: "Embroidery", dtg: "DTG", dtf: "DTF" }[S.method] ?? S.method} preview \xB7 ${areaLabel}` : null
    });
    cfg.onChange?.(getDesign());
    scheduleDraft();
  }
  let rootRef = root;
  function patchLayer(id, fn) {
    for (const side of SIDES) if (S.sides[side].some((l) => l.id === id)) S.sides[side] = S.sides[side].map((l) => l.id === id ? fn(l) : l);
  }
  const sideOf = (id) => SIDES.find((side) => S.sides[side].some((l) => l.id === id)) ?? S.side;
  function updatePlacement(id, fn) {
    patchLayer(id, (l) => {
      const aspect = l.artwork.naturalW > 0 && l.artwork.naturalH > 0 ? l.artwork.naturalH / l.artwork.naturalW : 1;
      return { ...l, placement: constrainPlacementToAreas(fn(l.placement), layoutFor(S.product, sideOf(id)).areas, aspect) };
    });
    schedule();
  }
  function addLayer(artwork, opts = {}) {
    const side = opts.side ?? S.side, id = newId("layer");
    objectUrls.add(artwork.url);
    const aspect = artwork.naturalW > 0 && artwork.naturalH > 0 ? artwork.naturalH / artwork.naturalW : 1;
    const layout = layoutFor(S.product, side);
    const layer = { id, artwork, placement: constrainPlacementToAreas(opts.placement ?? defaultPlacementForLayout(layout), layout.areas, aspect), text: opts.text };
    S.sides[side] = [...S.sides[side], layer];
    if (opts.select !== false) {
      S.side = side;
      S.activeLayerId = id;
      S.tab = "design";
    }
    if (!opts.text) analyzeArtwork(artwork).then((a) => {
      analyses.set(artwork.url, a);
      schedule();
    }).catch(() => {
    });
    if (opts.persisted !== void 0) mirrors.set(id, opts.persisted);
    else fileToDraftArtwork(artwork.file).then((d) => {
      mirrors.set(id, d);
      if (d === null) {
        S.largeArtNotice = true;
        schedule();
      }
    });
    schedule();
    return id;
  }
  function removeLayer(id) {
    const side = sideOf(id), layer = S.sides[side].find((l) => l.id === id);
    if (!layer) return;
    clearLayerUndo();
    S.undo.layer = { layer, side, mirror: mirrors.get(id) ?? null };
    mirrors.delete(id);
    S.sides[side] = S.sides[side].filter((l) => l.id !== id);
    if (S.activeLayerId === id) S.activeLayerId = S.sides[side][S.sides[side].length - 1]?.id ?? null;
    undoLayerTimer = setTimeout(clearLayerUndo, 6e3);
    schedule();
  }
  function clearLayerUndo() {
    const u = S.undo.layer;
    if (u) {
      URL.revokeObjectURL(u.layer.artwork.url);
      objectUrls.delete(u.layer.artwork.url);
      S.undo.layer = null;
    }
    if (undoLayerTimer) {
      clearTimeout(undoLayerTimer);
      undoLayerTimer = null;
    }
  }
  function undoRemoveLayer() {
    const u = S.undo.layer;
    if (!u) return;
    if (undoLayerTimer) clearTimeout(undoLayerTimer);
    undoLayerTimer = null;
    S.undo.layer = null;
    mirrors.set(u.layer.id, u.mirror);
    S.sides[u.side] = [...S.sides[u.side], u.layer];
    S.side = u.side;
    S.activeLayerId = u.layer.id;
    schedule();
  }
  function duplicateLayer(id) {
    const side = sideOf(id), l = S.sides[side].find((x) => x.id === id);
    if (!l) return;
    const art = { ...l.artwork, url: URL.createObjectURL(l.artwork.file) };
    addLayer(art, { side, placement: { ...l.placement, cx: clamp5(l.placement.cx + 0.035, 0.05, 0.95), cy: clamp5(l.placement.cy + 0.035, 0.05, 0.95) }, text: l.text ? { ...l.text } : void 0 });
    emit("duplicate", { kind: l.text ? "text" : "image" });
  }
  function reorderLayer(delta) {
    const id = S.activeLayerId;
    if (!id) return;
    const side = sideOf(id), arr = S.sides[side].slice(), i = arr.findIndex((l) => l.id === id), j = i + delta;
    if (i < 0 || j < 0 || j >= arr.length) return;
    [arr[i], arr[j]] = [arr[j], arr[i]];
    S.sides[side] = arr;
    schedule();
  }
  S.preparing = 0;
  function acceptFile(file) {
    if (!isAcceptedArtwork(file.name)) {
      S.error = "That file type won't preview here. Use a PNG, JPG, WebP, SVG, or PDF/AI file.";
      schedule();
      return;
    }
    if (file.size > MAX_ARTWORK_BYTES) {
      S.error = `Artwork is too large (max ${Math.floor(MAX_ARTWORK_BYTES / 1048576)} MB). Export a smaller PNG and re-upload.`;
      schedule();
      return;
    }
    S.error = null;
    const convert = isConvertibleArtwork(file.name);
    if (convert) {
      S.preparing++;
      schedule();
    }
    const gen = generation;
    probeAnyArtworkFile(file, cfg).then((art) => {
      if (gen !== generation) return;
      addLayer(art);
      emit("upload", { type: convert ? "print-file" : file.type || "unknown" });
    }).catch((e) => {
      S.error = e instanceof PrintFileError && e.message ? e.message : "We couldn't read that image. Try a different file.";
    }).finally(() => {
      if (convert) S.preparing--;
      schedule();
    });
  }
  function inkForGarment() {
    return relativeLuminance(hex()) < 0.45 ? "#FFFFFF" : "#111111";
  }
  async function addText() {
    const text = { content: "", fontId: fonts.defaultId(), color: inkForGarment() };
    const base = defaultPlacementForLayout(layoutFor(S.product, S.side));
    const placement = { ...base, width: clamp5(1.25 * base.width, 0.04, 0.5) };
    try {
      const art = await renderTextLayer(text, fonts);
      const id = addLayer(art, { side: S.side, placement, text });
      requestAnimationFrame(() => requestAnimationFrame(() => stage.startTextEdit(id)));
      emit("text_add", { font: text.fontId });
    } catch {
      S.error = "Couldn't add text. Try again in a moment.";
      schedule();
    }
  }
  async function applyStarter(starter) {
    const color = inkForGarment(), specs = starter.layers.map((l) => ({ ...l.text, color }));
    try {
      const arts = await Promise.all(specs.map((sp) => renderTextLayer(sp, fonts)));
      arts.forEach((art, i) => {
        const l = starter.layers[i];
        addLayer(art, { side: S.side, placement: { cx: l.cx, cy: l.cy, width: l.width, rotation: 0, flipX: false }, text: specs[i], select: i === 0 });
      });
      emit("starter", { starter: starter.id });
    } catch {
      S.error = "Couldn't load that starter. Try again in a moment.";
      schedule();
    }
  }
  async function addSampleLogo() {
    if (!cfg.sampleLogo) return;
    try {
      const blob = await (await fetch(cfg.sampleLogo)).blob();
      const art = await probeArtworkFile(new File([blob], "sample-logo.png", { type: blob.type || "image/png" }));
      const spots = layoutFor(S.product, S.side).spots, sp = spots.find((s) => s.id === "center-chest") ?? spots[0];
      addLayer(art, { placement: sp ? { cx: sp.cx, cy: sp.cy, width: sp.width, rotation: 0, flipX: false, area: sp.areaId } : void 0 });
    } catch {
      S.error = "Couldn't load that sample. Try uploading your own art.";
      schedule();
    }
  }
  function scheduleTextRender(id) {
    if (textTimer) clearTimeout(textTimer);
    const token = ++textToken;
    textTimer = setTimeout(() => rerenderText(id, token), 160);
  }
  async function rerenderText(id, token) {
    const l = SIDES.flatMap((s) => S.sides[s]).find((x) => x.id === id);
    if (!l?.text) return;
    try {
      const art = await renderTextLayer(l.text, fonts);
      const cur = SIDES.flatMap((s) => S.sides[s]).find((x) => x.id === id);
      if (token !== textToken || !cur) {
        URL.revokeObjectURL(art.url);
        return;
      }
      const old = cur.artwork.url;
      objectUrls.add(art.url);
      const oldAspect = cur.artwork.naturalW > 0 ? cur.artwork.naturalH / cur.artwork.naturalW : 1, newAspect = art.naturalW > 0 ? art.naturalH / art.naturalW : 1;
      patchLayer(id, (x) => ({ ...x, artwork: art, placement: { ...x.placement, width: clamp5(x.placement.width * (oldAspect / newAspect), 0.04, 0.92) } }));
      fileToDraftArtwork(art.file).then((d) => mirrors.set(id, d));
      setTimeout(() => {
        URL.revokeObjectURL(old);
        objectUrls.delete(old);
      }, 1500);
      schedule();
    } catch {
    }
  }
  function setText(id, patch) {
    patchLayer(id, (l) => l.text ? { ...l, text: { ...l.text, ...patch } } : l);
    scheduleTextRender(id);
    schedule();
  }
  async function flushText() {
    if (textTimer) {
      clearTimeout(textTimer);
      textTimer = null;
    }
    textToken++;
    for (const side of SIDES) {
      S.sides[side] = await Promise.all(S.sides[side].map(async (l) => {
        if (!l.text || isTextEmpty(l.text)) return l;
        const art = await renderTextLayer(l.text, fonts);
        const old = l.artwork.url;
        objectUrls.add(art.url);
        fileToDraftArtwork(art.file).then((d) => mirrors.set(l.id, d));
        setTimeout(() => {
          URL.revokeObjectURL(old);
          objectUrls.delete(old);
        }, 1500);
        return { ...l, artwork: art };
      }));
    }
    schedule();
  }
  async function uploadFont(file, layerId) {
    try {
      const f = await fonts.addFile(file);
      if (layerId) setText(layerId, { fontId: f.id });
      emit("font_upload", { name: file.name });
    } catch (e) {
      S.error = e?.message ?? "Couldn't load that font.";
    }
    schedule();
  }
  function selectProduct(id) {
    const next = findProduct(id);
    if (!next || next.id === S.productId) return;
    for (const side of SIDES) {
      const from = layoutFor(S.product, side), to = layoutFor(next, side);
      S.sides[side] = S.sides[side].map((l) => {
        const aspect = l.artwork.naturalW > 0 ? l.artwork.naturalH / l.artwork.naturalW : 1;
        return { ...l, placement: remapPlacementBetweenLayouts(l.placement, from, to, aspect) };
      });
    }
    S.productId = next.id;
    S.product = next;
    if (S.colorSel.kind === "stocked" && !stockedPicksForProduct(next).some((p) => p.color.id === S.colorSel.color.id)) S.colorSel = deserializeSelection(serializeSelection(S.colorSel), next) ?? defaultStockedSelection(next);
    if (S.side === "back" && !hasBack(next)) {
      S.side = "front";
      S.activeLayerId = null;
    }
    prewarm(next);
    try {
      history.replaceState(null, "", `?style=${encodeURIComponent(next.id)}`);
    } catch {
    }
    emit("style", { style: next.styleNumber });
    schedule();
  }
  function prewarm(p) {
    if (p.recolorMode !== "cap") {
      prewarmGarment(photoUrl(p.photos.front), p.photos.frontMask ? photoUrl(p.photos.frontMask) : void 0);
      if (p.photos.back) prewarmGarment(photoUrl(p.photos.back));
    }
  }
  function setColor(sel) {
    S.colorSel = sel;
    S.colorUi.hsv = null;
    S.colorUi.hexDraft = null;
    schedule();
  }
  function setCustomHex(v, remember) {
    const n = normalizeHex(v);
    if (!n) return;
    S.colorSel = { kind: "custom", hex: n };
    S.colorUi.hsv = hexToHsv(n);
    if (remember) pushRecent(n);
    schedule();
  }
  function pushRecent(n) {
    S.colorUi.recent = [n, ...S.colorUi.recent.filter((r) => r !== n)].slice(0, 8);
  }
  function thumbSrc(p) {
    const colorway = p.colors.find((c) => /pfd|white/i.test(c.id))?.id ?? p.colors[0]?.id;
    return photoUrl(p.colorwayImages?.[colorway] ?? p.photos.front);
  }
  const design = () => S.designs.find((d) => d.id === S.activeDesignId) ?? S.designs[0];
  const serializeLayers = (layers) => layers.map((l) => ({ placement: l.placement, artwork: mirrors.get(l.id) ?? null, text: l.text ?? null }));
  function serializeDesign(d, isActive) {
    if (isActive) return { styleId: S.productId, pantone: serializeSelection(S.colorSel), sides: { front: serializeLayers(S.sides.front), back: serializeLayers(S.sides.back) } };
    if (d.pending) return d.pending;
    return { styleId: d.productId, pantone: serializeSelection(d.colorSel), sides: d.stash ? { front: serializeLayers(d.stash.front), back: serializeLayers(d.stash.back) } : { front: [], back: [] } };
  }
  function stashActive() {
    const d = design();
    d.productId = S.productId;
    d.colorSel = S.colorSel;
    d.stash = { front: S.sides.front, back: S.sides.back };
    d.pending = null;
  }
  function loadDesignSlot(d) {
    const gen = ++generation;
    const p = findProduct(d.productId) ?? S.product;
    S.productId = p.id;
    S.product = p;
    S.colorSel = d.colorSel;
    S.side = S.side === "back" && hasBack(p) ? "back" : "front";
    if (d.stash) {
      S.sides = { front: d.stash.front, back: d.stash.back };
      S.activeLayerId = S.sides[S.side][S.sides[S.side].length - 1]?.id ?? null;
    } else {
      S.sides = { front: [], back: [] };
      S.activeLayerId = null;
      if (d.pending) {
        const pend = d.pending;
        d.pending = null;
        restoreSides(pend.sides, gen);
      }
    }
    prewarm(p);
  }
  function switchDesign(id) {
    if (id === S.activeDesignId) return;
    const target = S.designs.find((d) => d.id === id);
    if (!target) return;
    clearLayerUndo();
    clearDesignUndo();
    stashActive();
    S.activeDesignId = id;
    loadDesignSlot(target);
    emit("design", { action: "switch", designs: S.designs.length });
    schedule();
  }
  function newDesign() {
    if (S.designs.length >= DESIGN_RAIL_MAX) return;
    clearLayerUndo();
    clearDesignUndo();
    stashActive();
    ++generation;
    const d = { id: newId("design"), productId: S.productId, colorSel: S.colorSel, stash: null, pending: null };
    S.designs.push(d);
    S.activeDesignId = d.id;
    S.sides = { front: [], back: [] };
    S.activeLayerId = null;
    S.side = "front";
    emit("design", { action: "new", designs: S.designs.length });
    schedule();
  }
  function duplicateDesign() {
    if (S.designs.length >= DESIGN_RAIL_MAX) return;
    clearLayerUndo();
    clearDesignUndo();
    ++generation;
    const clone = (layers) => layers.map((l) => {
      const id = newId("layer"), url = URL.createObjectURL(l.artwork.file);
      objectUrls.add(url);
      mirrors.set(id, mirrors.get(l.id) ?? null);
      return { ...l, id, artwork: { ...l.artwork, url }, text: l.text ? { ...l.text } : void 0 };
    });
    const copy = { front: clone(S.sides.front), back: clone(S.sides.back) };
    stashActive();
    const cur = S.designs.findIndex((d2) => d2.id === S.activeDesignId);
    const d = { id: newId("design"), productId: S.productId, colorSel: S.colorSel, stash: null, pending: null };
    S.designs.splice(cur + 1, 0, d);
    S.activeDesignId = d.id;
    S.sides = copy;
    S.activeLayerId = S.sides[S.side][S.sides[S.side].length - 1]?.id ?? null;
    emit("design", { action: "duplicate", designs: S.designs.length });
    schedule();
  }
  function removeDesign(id) {
    if (S.designs.length <= 1) return;
    const i = S.designs.findIndex((d) => d.id === id);
    if (i < 0) return;
    clearLayerUndo();
    clearDesignUndo();
    const wasActive = id === S.activeDesignId;
    if (wasActive) stashActive();
    S.undo.design = { slot: S.designs[i], index: i };
    S.designs = S.designs.filter((d) => d.id !== id);
    if (wasActive) {
      const next = S.designs[i] ?? S.designs[i - 1];
      S.activeDesignId = next.id;
      loadDesignSlot(next);
    }
    undoDesignTimer = setTimeout(clearDesignUndo, 6e3);
    emit("design", { action: "remove", designs: S.designs.length });
    schedule();
  }
  function clearDesignUndo() {
    const u = S.undo.design;
    if (u) {
      for (const side of SIDES) for (const l of u.slot.stash?.[side] ?? []) {
        URL.revokeObjectURL(l.artwork.url);
        objectUrls.delete(l.artwork.url);
        mirrors.delete(l.id);
      }
      S.undo.design = null;
    }
    if (undoDesignTimer) {
      clearTimeout(undoDesignTimer);
      undoDesignTimer = null;
    }
  }
  function undoRemoveDesign() {
    const u = S.undo.design;
    if (!u) return;
    if (undoDesignTimer) clearTimeout(undoDesignTimer);
    undoDesignTimer = null;
    S.undo.design = null;
    if (!S.designs.some((d) => d.id === u.slot.id)) S.designs.splice(Math.min(u.index, S.designs.length), 0, u.slot);
    schedule();
  }
  function restoreSides(sides, gen) {
    const get = (side) => Array.isArray(sides?.[side]) ? sides[side] : [];
    const last = get("front").length - 1;
    for (const side of SIDES) get(side).forEach((raw, i) => {
      if (!raw || typeof raw !== "object") return;
      const select = side === "front" && i === last;
      const text = raw.text ? sanitizeTextSpec(raw.text, fonts) : null;
      if (text) renderTextLayer(text, fonts).then((art) => {
        if (gen === generation) addLayer(art, { side, placement: sanitizePlacement(raw.placement) ?? void 0, text, select });
      }).catch(() => {
      });
      else if (raw.artwork && typeof raw.artwork.dataUrl === "string") {
        const blob = dataUrlToBlob(raw.artwork.dataUrl);
        if (!blob) return;
        probeAnyArtworkFile(new File([blob], raw.artwork.name || "artwork", { type: raw.artwork.type || blob.type }), cfg).then((art) => {
          if (gen === generation) addLayer(art, { side, placement: sanitizePlacement(raw.placement) ?? void 0, persisted: raw.artwork, select });
        }).catch(() => {
        });
      }
    });
  }
  let draftTimer = null;
  function scheduleDraft() {
    if (!cfg.persistDraft || handedOff) return;
    if (draftTimer) clearTimeout(draftTimer);
    draftTimer = setTimeout(() => {
      const designs = S.designs.map((d) => serializeDesign(d, d.id === S.activeDesignId));
      const idx = Math.max(0, S.designs.findIndex((d) => d.id === S.activeDesignId));
      writeDraft(cfg.draftKey, { styleId: S.productId, pantone: serializeSelection(S.colorSel), method: S.method, quantity: S.quantity, notes: S.notes, email: S.email, sides: designs[idx]?.sides, designs, activeDesign: idx, colorUi: { recent: S.colorUi.recent } });
    }, 400);
  }
  function restoreDraft() {
    const shared = location.hash.match(/#pc=([A-Za-z0-9_-]+)/);
    if (shared) {
      const d = decodeShare(shared[1]);
      if (d) {
        loadSharedDesign(d);
        return;
      }
    }
    const draft = readDraft(cfg.draftKey);
    if (!draft) return;
    const p = !cfg.initialProductId && draft.styleId ? findProduct(draft.styleId) : null;
    if (p) {
      S.productId = p.id;
      S.product = p;
    }
    const sel = deserializeSelection(draft.pantone, S.product);
    if (sel) S.colorSel = sel;
    if (draft.method && (cfg.pricing.methods[draft.method] || ["screen", "embroidery", "dtg", "dtf"].includes(draft.method))) S.method = draft.method;
    if (typeof draft.quantity === "string") S.quantity = draft.quantity;
    if (typeof draft.notes === "string") S.notes = draft.notes;
    if (typeof draft.email === "string") S.email = draft.email;
    if (Array.isArray(draft.colorUi?.recent)) S.colorUi.recent = draft.colorUi.recent.filter((x) => normalizeHex(x)).slice(0, 8);
    const list = Array.isArray(draft.designs) ? draft.designs.filter((d) => d && typeof d === "object").slice(0, DESIGN_RAIL_MAX) : [];
    if (list.length === 0) {
      if (draft.sides) restoreSides(draft.sides, generation);
      return;
    }
    const active = clamp5(Math.trunc(draft.activeDesign ?? 0), 0, list.length - 1);
    S.designs = list.map((d) => {
      const prod2 = findProduct(d.styleId) ?? products[0];
      return { id: newId("design"), productId: prod2.id, colorSel: deserializeSelection(d.pantone, prod2) ?? defaultStockedSelection(prod2), stash: null, pending: d };
    });
    const slot = S.designs[active];
    S.activeDesignId = slot.id;
    const prod = findProduct(slot.productId) ?? products[0];
    S.productId = prod.id;
    S.product = prod;
    S.colorSel = slot.colorSel;
    const pend = slot.pending;
    slot.pending = null;
    restoreSides(pend?.sides, generation);
  }
  function loadSharedDesign(d) {
    const p = findProduct(d.styleId);
    if (p) {
      S.productId = p.id;
      S.product = p;
    }
    const sel = deserializeSelection(d.pantone, S.product);
    if (sel) S.colorSel = sel;
    if (d.method && (cfg.pricing.methods[d.method] || ["screen", "embroidery", "dtg", "dtf"].includes(d.method))) S.method = d.method;
    restoreSides(d.sides, generation);
    if (d.omittedImages) S.notice = "This shared link contains the text layers. Uploaded images can't travel in a link, so re-upload them if needed.";
  }
  const sidesForExport = () => (hasBack(S.product) ? SIDES : ["front"]).map((side) => {
    const layout = layoutFor(S.product, side);
    return { side, label: SIDE_LABEL2[side], mockupSrc: mockupFor(side), mirrored: false, layers: S.sides[side].filter((l) => !l.text || !isTextEmpty(l.text)).map((l) => ({ artwork: l.artwork, placement: l.placement, clipRect: resolvePlacementArea(layout.areas, l.placement)?.rect })) };
  });
  async function waitForMockups() {
    for (let i = 0; i < 40; i++) {
      const pendingJobs = [...recolorState.values()].some((v) => v === null);
      if (!pendingJobs) break;
      await new Promise((r) => setTimeout(r, 100));
    }
  }
  async function openPreview() {
    S.preview = { open: true, busy: true, images: [], failed: false };
    schedule();
    try {
      await flushText();
      await waitForMockups();
      const images = [];
      for (const s of sidesForExport()) {
        if (s.layers.length === 0) continue;
        const blob = await composeMockupPng(s.mockupSrc, s.layers, { mirrored: s.mirrored });
        if (blob) images.push({ label: s.label, url: URL.createObjectURL(blob) });
      }
      S.preview = { open: true, busy: false, images, failed: false };
    } catch {
      S.preview = { open: true, busy: false, images: [], failed: true };
    }
    schedule();
  }
  function closePreview() {
    S.preview.images.forEach((i) => URL.revokeObjectURL(i.url));
    S.preview = { open: false, busy: false, images: [], failed: false };
    schedule();
  }
  async function renderFinal() {
    await flushText();
    await waitForMockups();
    return composeFinalPreviewPng(sidesForExport());
  }
  async function download(source) {
    if (S.saving) return;
    S.saving = true;
    schedule();
    try {
      const blob = await renderFinal();
      if (!blob) {
        S.error = "Couldn't render the design image to save. Try again in a moment.";
        return;
      }
      const url = URL.createObjectURL(blob), a = document.createElement("a");
      a.href = url;
      a.download = `${S.product.styleNumber.toLowerCase()}-mockup.png`;
      a.click();
      URL.revokeObjectURL(url);
      emit("save", { style: S.product.styleNumber, source, designs: layerTotal() });
    } catch {
      S.error = "Couldn't render the design image to save. Try again in a moment.";
    } finally {
      S.saving = false;
      schedule();
    }
  }
  async function share() {
    if (S.share === "working") return;
    S.share = "working";
    S.shareExpires = null;
    S.shareLink = null;
    schedule();
    const fail = () => {
      S.share = "failed";
      setTimeout(() => {
        S.share = "idle";
        schedule();
      }, 3e3);
      schedule();
    };
    try {
      const d = serializeDesign(design(), true);
      let url;
      if (cfg.onShare) {
        const blob = await renderFinal();
        const preview = blob ? await blobToPreviewDataUrl(blob) : null;
        const res = await cfg.onShare({ design: { ...d, styleName: S.product.name, styleNumber: S.product.styleNumber, method: S.method }, previews: preview ? [preview] : [] });
        if (!res?.url) return fail();
        url = res.url;
        if (res.expiresAt) {
          const t = Date.parse(res.expiresAt);
          S.shareExpires = Number.isFinite(t) ? new Date(t).toLocaleDateString(void 0, { year: "numeric", month: "long", day: "numeric" }) : true;
        } else S.shareExpires = true;
      } else {
        const strip = (layers) => layers.filter((l) => l.text && l.text.content.trim()).map((l) => ({ placement: l.placement, text: l.text }));
        const omitted = d.sides.front.some((l) => !l.text) || d.sides.back.some((l) => !l.text);
        const payload = { styleId: d.styleId, pantone: d.pantone, method: S.method, sides: { front: strip(d.sides.front), back: strip(d.sides.back) }, omittedImages: omitted };
        url = `${location.origin}${location.pathname}?style=${encodeURIComponent(S.productId)}#pc=${encodeShare(payload)}`;
        S.shareExpires = true;
      }
      S.shareLink = null;
      try {
        await navigator.clipboard.writeText(url);
      } catch {
        S.shareLink = url;
      }
      S.share = "copied";
      setTimeout(() => {
        S.share = "idle";
        schedule();
      }, 2500);
      emit("share", { style: S.product.styleNumber });
    } catch {
      return fail();
    }
    schedule();
  }
  async function submit() {
    if (S.submitting) return;
    const price = computePrice();
    const qty = price.quantity;
    if (qty < 1) {
      S.error = S.mode === "cart" && sizeList().length ? "Choose how many of each size you need." : "Enter how many pieces you need.";
      S.tab = "quote";
      schedule();
      return;
    }
    const blocker = price.blockers[0];
    if (S.mode === "cart" && blocker) {
      S.error = blocker.text;
      S.tab = "quote";
      schedule();
      return;
    }
    S.error = null;
    S.submitting = true;
    schedule();
    try {
      await flushText();
      await waitForMockups();
      const layers = (hasBack(S.product) ? SIDES : ["front"]).flatMap((side) => S.sides[side].filter((l) => !l.text || !isTextEmpty(l.text)).map((l) => ({ layer: l, side })));
      const layout = Object.fromEntries(SIDES.map((s) => [s, layoutFor(S.product, s)]));
      const lines = layers.map(({ layer, side }, i) => {
        const area = placementAreaLabel(layout[side], layer.placement), generic = area === SIDE_LABEL2[side];
        let line = `${i + 1}. ${SIDE_LABEL2[side]} placement${area && !generic ? ` \xB7 ${area.toLowerCase()}` : ""}`;
        if (layer.text) {
          const f = fonts.get(layer.text.fontId), bits = [f.name, `ink ${layer.text.color}`];
          if (layer.text.outline) bits.push(`outline ${layer.text.outline}`);
          if (layer.text.arc) bits.push(`curve ${Math.round(100 * layer.text.arc)}%`);
          if (layer.text.spacing) bits.push(`tracking ${Math.round(100 * layer.text.spacing)}%`);
          line += ` \xB7 text: "${layer.text.content.trim().replace(/\s+/g, " ").slice(0, 80)}" (${bits.join(", ")})`;
        }
        return line;
      });
      const others = S.designs.filter((d) => d.id !== S.activeDesignId).map((d, i) => {
        const p = findProduct(d.productId);
        const n = d.pending ? ["front", "back"].reduce((k, s) => k + (d.pending.sides?.[s]?.length ?? 0), 0) : d.stash ? d.stash.front.length + d.stash.back.length : 0;
        return `${i + 1}. ${p?.name ?? d.productId} \xB7 ${selectionNotesLabel(d.colorSel)} \xB7 ${n} placement${n === 1 ? "" : "s"}`;
      });
      const notes = [S.notes.trim(), `Garment color: ${selectionNotesLabel(S.colorSel)}`, layers.length > 0 ? `Placements (${layers.length}):
${lines.join("\n")}` : "Artwork: not supplied yet. Quote the selected colored blank; the customer will send artwork later.", others.length ? `Other designs saved in the studio (artwork not attached to this request):
${others.join("\n")}` : ""].filter(Boolean).join("\n");
      const usedFonts = [...new Set(layers.filter((x) => x.layer.text).map((x) => x.layer.text.fontId))].map((id) => fonts.exportUploaded(id)).filter(Boolean);
      const mockups = [];
      for (const s of sidesForExport()) {
        if (s.layers.length === 0) continue;
        const blob = await composeMockupPng(s.mockupSrc, s.layers, { mirrored: s.mirrored });
        if (blob) mockups.push({ side: s.side, label: s.label, blob, file: new File([blob], `${S.product.styleNumber.toLowerCase()}-${s.side}.png`, { type: "image/png" }) });
      }
      const final = await composeFinalPreviewPng(sidesForExport());
      const payload = {
        product: { id: S.product.id, name: S.product.name, styleNumber: S.product.styleNumber, category: S.product.category },
        color: { selection: serializeSelection(S.colorSel), hex: hex(), name: selectionName(S.colorSel), label: selectionNotesLabel(S.colorSel) },
        method: S.method,
        quantity: qty,
        notes,
        email: S.email.trim(),
        layers: layers.map(({ layer, side }) => ({ side, placement: layer.placement, areaLabel: placementAreaLabel(layout[side], layer.placement), text: layer.text ?? null, file: layer.artwork.file, fileName: layer.artwork.file.name, isVector: layer.artwork.isVector, naturalW: layer.artwork.naturalW, naturalH: layer.artwork.naturalH })),
        fonts: usedFonts,
        mockups,
        finalPreview: final,
        design: serializeDesign(design(), true),
        price,
        sizes: sizeRows().filter((r) => r.quantity > 0),
        priceMethodLabel: cfg.pricing.methods[S.method]?.label ?? S.method,
        otherDesigns: S.designs.filter((d) => d.id !== S.activeDesignId).map((d) => serializeDesign(d, false))
      };
      emit("submit", { style: S.product.styleNumber, qty, designs: layers.length });
      if (cfg.onSubmit) {
        handedOff = true;
        try {
          await cfg.onSubmit(payload, api);
        } catch (err) {
          handedOff = false;
          throw err;
        }
        handedOff = false;
      } else {
        const previewUrl = final ? URL.createObjectURL(final) : null;
        S.result = { title: "Request prepared", message: "No onSubmit handler is configured, so here is the payload your site would receive. Wire config.onSubmit to send it to your cart, checkout or quote form.", previewUrl, details: JSON.stringify({ ...payload, layers: payload.layers.map((l) => ({ ...l, file: `[File ${l.fileName}]` })), mockups: payload.mockups.map((m) => `[PNG ${m.label}]`), finalPreview: final ? "[PNG]" : null, fonts: payload.fonts.map((f) => f.name), design: void 0, otherDesigns: void 0, notes: void 0 }, null, 2) + `

Notes:
${notes}` };
      }
    } catch (e) {
      S.error = e?.message || (S.mode === "cart" ? "We couldn't add this to your cart. Please try again." : "Something went wrong preparing your design. Try again, or request a quote and attach your artwork there.");
    } finally {
      S.submitting = false;
      schedule();
    }
  }
  function getDesign() {
    return { productId: S.productId, product: S.product, color: serializeSelection(S.colorSel), method: S.method, quantity: S.quantity, notes: S.notes, sides: { front: S.sides.front.map((l) => ({ id: l.id, placement: l.placement, text: l.text ?? null, fileName: l.artwork.file.name })), back: S.sides.back.map((l) => ({ id: l.id, placement: l.placement, text: l.text ?? null, fileName: l.artwork.file.name })) } };
  }
  const api = {
    getDesign,
    getState: () => S,
    setProduct: selectProduct,
    setColor: (hint) => {
      const sel = typeof hint === "string" ? resolveColorHint(hint, S.product) : hint;
      if (sel) setColor(sel);
    },
    addFiles: (files) => files.forEach(acceptFile),
    addText,
    applyStarter,
    setMethod: (m) => {
      S.method = m;
      schedule();
    },
    setQuantity: (q) => {
      S.quantity = String(q);
      schedule();
    },
    openPreview,
    download,
    share,
    submit,
    fonts,
    open: openModal,
    close: closeModal,
    isOpen: () => S.open,
    setSizes: (map) => {
      S.sizes = { ...map };
      schedule();
    },
    getPrice: () => computePrice(),
    loadDesign: (d) => {
      ++generation;
      S.sides = { front: [], back: [] };
      S.activeLayerId = null;
      loadSharedDesign(d);
      schedule();
    },
    destroy() {
      for (const u of objectUrls) URL.revokeObjectURL(u);
      document.removeEventListener("keydown", onKey);
      host.innerHTML = "";
    }
  };
  const A = {
    selectProduct,
    thumbSrc,
    setColor,
    setColorMode: (m) => {
      S.colorUi.mode = m;
      if (m === "custom") {
        S.colorUi.hsv = hexToHsv(hex());
        if (S.colorSel.kind !== "custom") S.colorSel = S.colorSel.kind === "pantone" ? S.colorSel : { kind: "custom", hex: hex() };
      }
      schedule();
    },
    setColorUi: (patch) => {
      Object.assign(S.colorUi, patch);
      schedule();
    },
    setHsv: (hsv) => {
      S.colorUi.hsv = hsv;
      S.colorUi.hexDraft = null;
      S.colorSel = { kind: "custom", hex: hsvToHex(hsv) };
      schedule();
    },
    pickSV: (e) => {
      const r = e.currentTarget.getBoundingClientRect();
      const hsv = S.colorUi.hsv ?? hexToHsv(hex());
      A.setHsv({ ...hsv, s: Math.round(100 * clamp5((e.clientX - r.left) / r.width, 0, 1)), v: Math.round((1 - clamp5((e.clientY - r.top) / r.height, 0, 1)) * 100) });
    },
    setHexDraft: (v) => {
      const t = v.replace(/[^0-9a-fA-F]/g, "").slice(0, 6);
      S.colorUi.hexDraft = t;
      if (normalizeHex(t)) {
        S.colorSel = { kind: "custom", hex: normalizeHex(t) };
        S.colorUi.hsv = hexToHsv(normalizeHex(t));
      }
      schedule();
    },
    commitCustomHex: () => {
      S.colorUi.hexDraft = null;
      if (S.colorSel.kind === "custom") pushRecent(S.colorSel.hex);
      schedule();
    },
    setCustomHex,
    setPantoneCode: (v) => {
      S.colorUi.code = v;
      const c = findPantoneByCode(v);
      if (c) {
        S.colorSel = { kind: "pantone", color: c };
        S.colorUi.hsv = hexToHsv(c.hex);
      }
      schedule();
    },
    eyeDrop: async () => {
      try {
        const { sRGBHex } = await new window.EyeDropper().open();
        setCustomHex(sRGBHex, true);
      } catch {
      }
    },
    setTab: (t) => {
      S.tab = t;
      schedule();
    },
    toggleBlanks: (v) => {
      S.blanksCollapsed = v ?? !S.blanksCollapsed;
      schedule();
    },
    jumpTo: (t) => {
      if (t === "blank") {
        if (S.blanksCollapsed) {
          S.blanksCollapsed = false;
          schedule();
        }
        rootRef.querySelector("#pc-section-blank")?.scrollIntoView({ behavior: "smooth", block: "start" });
        return;
      }
      S.tab = t;
      schedule();
      rootRef.querySelector(".pc-panel--right")?.scrollIntoView({ behavior: "smooth", block: "nearest" });
    },
    setBlanksQuery: (q) => {
      S.blanksQuery = q;
      schedule();
    },
    setBlanksCategory: (c) => {
      S.blanksCategory = c;
      schedule();
    },
    expandBlanks: () => {
      S.blanksExpanded = true;
      schedule();
    },
    setSide: (side) => {
      S.side = side;
      S.activeLayerId = S.sides[side][S.sides[side].length - 1]?.id ?? null;
      schedule();
    },
    selectLayer: (id) => {
      S.activeLayerId = id;
      S.tab = "design";
      schedule();
    },
    deselect: () => {
      S.activeLayerId = null;
      schedule();
    },
    updateActivePlacement: (fn) => {
      if (S.activeLayerId) updatePlacement(S.activeLayerId, fn);
    },
    resetPlacement: () => {
      if (S.activeLayerId) updatePlacement(S.activeLayerId, () => defaultPlacementForLayout(layoutFor(S.product, S.side)));
    },
    applySpot: (sp) => {
      if (S.activeLayerId) updatePlacement(S.activeLayerId, (p) => ({ ...p, cx: sp.cx, cy: sp.cy, width: sp.width, area: sp.areaId }));
    },
    duplicateLayer,
    removeLayer,
    undoRemoveLayer,
    reorderLayer,
    addText,
    applyStarter,
    addSampleLogo,
    openFilePicker: () => stage.openFilePicker({ multiple: true }),
    setText,
    uploadFont,
    removeFont: async (id) => {
      await fonts.remove(id);
      for (const side of SIDES) S.sides[side].forEach((l) => {
        if (l.text?.fontId === id) setText(l.id, { fontId: fonts.defaultId() });
      });
      schedule();
    },
    setFontManager: (v) => {
      S.fontManager = v;
      schedule();
    },
    setPreflightOpen: (v) => {
      S.preflightOpen = v;
      schedule();
    },
    setMethod: (m) => {
      S.method = m;
      schedule();
    },
    setQuantity: (q) => {
      S.quantity = q;
      schedule();
    },
    setNotes: (n) => {
      S.notes = n;
      schedule();
    },
    setEmail: (e) => {
      S.email = e;
      schedule();
    },
    setSize: (size, qty) => {
      const n = Math.max(0, Math.trunc(Number(qty) || 0));
      if (n === 0) delete S.sizes[size];
      else S.sizes[size] = n;
      schedule();
    },
    bumpSize: (size, delta) => {
      const n = Math.max(0, (S.sizes[size] ?? 0) + delta);
      if (n === 0) delete S.sizes[size];
      else S.sizes[size] = n;
      schedule();
    },
    clearSizes: () => {
      S.sizes = {};
      schedule();
    },
    spreadSizes: (total) => {
      const list = sizeRows().filter((r) => r.available).map((r) => r.size);
      if (!list.length) return;
      const each = Math.floor(total / list.length);
      let rest = total - each * list.length;
      S.sizes = {};
      list.forEach((size, i) => {
        const n = each + (i < rest ? 1 : 0);
        if (n > 0) S.sizes[size] = n;
      });
      schedule();
    },
    close: () => closeModal(),
    requestQuoteInstead: () => {
      S.mode = "quote";
      S.error = null;
      schedule();
    },
    newDesign,
    duplicateDesign,
    switchDesign,
    removeDesign,
    undoRemoveDesign,
    openPreview,
    closePreview,
    download,
    share,
    submit,
    submitLabel,
    closeResult: () => {
      if (S.result?.previewUrl) URL.revokeObjectURL(S.result.previewUrl);
      S.result = null;
      schedule();
    },
    dismissError: () => {
      S.error = null;
      schedule();
    },
    dismissNotice: () => {
      S.notice = null;
      schedule();
    },
    dismissLargeArt: () => {
      S.largeArtNotice = false;
      schedule();
    }
  };
  function onKey(e) {
    if (e.key === "Escape" && S.preview.open) {
      closePreview();
      return;
    }
    if (e.key === "Escape" && overlay && S.open && !S.result) {
      closeModal();
      return;
    }
    if (!S.activeLayerId || !(e.metaKey || e.ctrlKey) || e.altKey) return;
    const t = e.target;
    if (t && (t.tagName === "INPUT" || t.tagName === "TEXTAREA" || t.tagName === "SELECT" || t.isContentEditable)) return;
    if (e.key.toLowerCase() === "d") {
      e.preventDefault();
      duplicateLayer(S.activeLayerId);
    } else if (e.key === "]") {
      e.preventDefault();
      reorderLayer(1);
    } else if (e.key === "[") {
      e.preventDefault();
      reorderLayer(-1);
    }
  }
  document.addEventListener("keydown", onKey);
  let preflightUrl = null;
  function watchPreflight() {
    const l = activeLayer();
    if (!l || l.text) return;
    if (S.preflight?.url === l.artwork.url || preflightUrl === l.artwork.url) return;
    preflightUrl = l.artwork.url;
    analyzeArtwork(l.artwork).then((analysis) => {
      analyses.set(l.artwork.url, analysis);
      if (preflightUrl === l.artwork.url) {
        S.preflight = { url: l.artwork.url, analysis };
        schedule();
      }
    });
  }
  const _render = render;
  render = function() {
    _render();
    watchPreflight();
  };
  (async () => {
    if (cfg.initialColor) {
      const sel = resolveColorHint(cfg.initialColor, S.product);
      if (sel) {
        S.colorSel = sel;
        S.designs[0].colorSel = sel;
      }
    }
    await fonts.restoreUploaded().catch(() => {
    });
    fonts.onChange(() => schedule());
    restoreDraft();
    prewarm(S.product);
    render();
  })();
  render();
  return api;
}
var customizer_default = { mount };
export {
  DEFAULT_LABELS,
  customizer_default as default,
  mount
};
