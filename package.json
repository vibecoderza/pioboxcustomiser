{
  "name": "piobox-customizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Embeddable garment design studio. Hosts the customizer (ES modules + fonts) and handles design-file uploads to Vercel Blob.",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npx vercel --prod",
    "deploy:preview": "npx vercel",
    "dev": "python3 devserver.py"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
